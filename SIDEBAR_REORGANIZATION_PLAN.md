# GeoSpectrum 侧边栏功能收纳补充计划

- 编制日期：2026-08-18（Asia/Shanghai）
- 状态：`implemented`，S22 已完成实现与机器/浏览器验收，等待用户明确确认
- 计划性质：`PLAN.md` 的界面信息架构与桌面输出可靠性补充；除已记录的 S17 人工复验状态外，不改变 S00–S21 的既有范围和验收结论
- 根目录归属：用户已明确要求将本补充计划放在项目主目录；该文件属于项目级 UI 治理文档，不是临时文件
- 主要事实依据：`Spec2.02功能研究/03_Spec2.02功能说明与截图文档.md`、`Spec Source/Source/Main/FmMain.pas/.dfm`、`Spec Source/Source/Common/uMethod.pas`
- 当前实现依据：`app/frontend/src/App.tsx`、`app/frontend/src/styles.css`、`app/backend/app/modules/manifest.py`、`app/manifest.generated.json`

## 1. 目标与问题定义

当前侧边栏把工作台、方法、三类迁移、谱图、后处理、样品队列、设备调试、三类采集/校准、分析、报告、维护、帮助、权限和审计等入口平铺在同一层。入口数量多，日常分析、设备调试、旧数据迁移和系统管理相互穿插，用户难以从业务流程判断应该进入哪个页面。

本补充计划的目标是：

1. 参考旧版 SpecDirect 的“光谱方法—分析条件—分析测试—数据处理—工具—帮助”功能树组织方式。
2. 保留新版新增的角色、审计、备份维护和只读迁移能力，但将其放入独立、合理的业务分组。
3. 侧边栏默认只显示少量顶层功能组；组内功能只有在鼠标悬停、键盘聚焦或点击后才展开。
4. 不因收纳而删除、隐藏或改变现有业务能力；每个现有页面和内部主要视图都必须有可追踪入口。
5. 不把日常分析、设备调试、旧版迁移和系统管理混入同一组。
6. 继承旧版“先打开当前分析方法，再启用方法相关操作”的运行逻辑；权限不足与运行前置条件不足必须采用不同呈现方式。

## 2. 事实依据与旧版组织逻辑

旧版功能说明明确采用以下顶层功能树：

1. 工作台与运行基础
2. 光谱方法
3. 分析条件
4. 分析测试
5. 数据处理
6. 工具
7. 帮助

其中：

- “光谱方法”负责方法生命周期、页面与打印机设置。
- “分析条件”负责方法参数、分析谱线和参数打印。
- “分析测试”按业务链覆盖样号准备、色散/蒸发/样品摄谱、谱图查看、样品分析、重复性质控和标准曲线。
- “数据处理”负责既有结果重算、报告打印/导出和强度导出。
- “工具”负责软件/设备参数、调试对中、方法管理、备份维护和蒸发文件导入。
- “帮助”只负责帮助主题与关于。

新版不照搬旧技术实现，但应继承这种“按用户任务收纳，而不是按代码模块平铺”的组织原则。新版新增的账户、角色、审计、SQLite 维护和多类只读迁移不应强行塞回旧菜单，应增加独立的“系统管理”，并在“工具”中保留与设备、校准和兼容迁移直接相关的入口。

### 2.1 旧版当前方法与动作启用逻辑

旧版启动时会连接安装目录中的方法库 `DIRECT.MTD`，但不会自动选中一个方法。主窗体以全局当前方法 `G_SdMtd.Enabled` 控制动作是否灰置，而该值的源码定义只是 `MtdId > 0`。因此，用户必须先完成以下任一路径：

1. “光谱方法 → 打开方法”，从 `DIRECT.MTD` 方法库选择并载入一个已有方法。
2. “光谱方法 → 新建方法”，在方法参数窗口通过校验并保存，使新方法取得数据库记录 ID。

当前方法有效后，窗口标题显示方法名，样品/蒸发摄谱、样品分析、查看谱图、方法参数、分析谱线、参数打印、预录样号和重新计算等动作解除灰置。关闭、重置或载入方法失败后，这些动作重新灰置。

以下例外必须保留：

- “色散摄谱”和“色散曲线”不依赖当前方法，旧版在主窗口可操作时始终启用。
- “导出全时数据”除当前方法外，还要求当前方法启用全时保存模式 `ExpSegMode`。
- 报告打印、报告导出和强度导出不以当前方法作为主菜单启用条件，而是在进入操作后选择相应 `.dat/.pdt` 数据文件。
- 调试模式中的“导入蒸发摄谱文件”是方法启用后的下游转换工具，不是解锁其他功能所需的导入入口。

新版的等价条件采用后端 `/api/v1/methods/current` 返回的 `currentMethod.method_id != null`。由于新版只允许已发布方法设为当前运行方法，因此“已选择当前方法”在新版同时意味着已存在可追溯的已发布版本；不得退化为仅凭前端选中一条草稿记录解锁。

## 3. 范围与非目标

### 3.1 本计划包含

- 侧边栏顶层分组、组内入口、排序和中文命名。
- 悬停临时展开、点击固定展开、键盘展开和关闭规则。
- 当前页面、当前组、权限过滤及外部依赖状态的显示规则。
- 方法页、分析页和后处理页内部视图的定向打开方式。
- 模块清单中的导航元数据及架构约束。
- 前端构建、权限角色和浏览器交互验收。
- Tauri 桌面壳中的统一文件保存入口，以及浏览器开发模式的下载兼容回退。

### 3.2 本计划不包含

- 不修改任何采集、分析、迁移、报告、权限或设备业务规则。
- 不修改 API 载荷、数据库业务表或历史数据。
- 不重做页面主体布局，不改变现有配色和组件视觉语言。
- 不借侧边栏整理拆分或合并后端业务模块。
- 不补做 S14/S15 真实硬件协议和现场验收。
- 不提前实现与侧边栏无关的新功能。
- 不改变 PDF、CSV、TXT、SAM 等文件的业务内容、格式口径或后端生成逻辑；只统一文件落盘和用户反馈。

## 4. 信息架构设计原则

1. **按任务分组**：用户先判断“我要设计方法、做分析、处理结果，还是维护系统”，再选择具体功能。
2. **遵循工作流顺序**：方法设计在前，采集与分析居中，结果处理在后，工具和系统管理靠后。
3. **日常功能与低频功能分离**：旧版迁移、审计、备份等低频入口不得插入样品分析主链。
4. **设备调试与正式采集分离**：设备参数/实时调试属于工具；色散、样品和自动转角任务属于分析测试。
5. **一项业务一个规范入口**：同一内部功能不在多个组重复出现；一个页面包含多项业务时，用 `view/section` 精确落到相应内部视图。
6. **最多一层可点击子菜单**：顶层组打开后，在组项右侧显示单层面板；可以用“准备、采集、查看与分析”等静态小标题分段，但不再创建需要继续展开的第三层菜单。
7. **权限先过滤、分组后呈现**：用户没有权限的入口不显示；过滤后没有任何子项的组不显示。
8. **功能状态不等于导航层级**：`deferred_external` 用状态徽标说明，不单独建立“未完成”菜单。
9. **模块清单仍是事实来源**：权限、路由和模块可用性由模块清单提供，不继续扩大 `App.tsx` 中的硬编码条件链。
10. **权限与运行条件分离**：无权限入口直接过滤；有权限但未选择当前方法或未开启所需模式的入口保留原位置并灰置，明确说明缺少的前置条件。

## 5. 建议的顶层侧边栏

侧边栏常态只显示以下八个入口：

```text
工作台                 直接进入，不展开
光谱方法               悬停/点击展开
分析条件               悬停/点击展开
分析测试               悬停/点击展开
数据处理               悬停/点击展开
工具                   悬停/点击展开
系统管理               悬停/点击展开
帮助                   悬停/点击展开
```

本机服务状态、当前用户和版本信息继续固定在侧边栏底部或顶栏，不作为业务菜单项参与展开。

## 6. 完整功能收纳树

### 6.1 工作台

- **工作台概览**：系统状态、当前方法、运行消息、常用入口。

工作台是唯一不带子菜单的顶层入口，单击直接进入 `/workspace`。

### 6.2 光谱方法

#### 方法文件

- **方法库与当前方法**：新建、打开、复制/另存、重命名、启停、删除、发布和当前方法切换。
- **页面与打印机设置**：纸张、方向、边距、布局、打印机及打印参数。

这组回答“使用哪个方法、如何保存和打印方法”，不放分析条件编辑、采集或旧数据迁移。

“方法库与当前方法”始终可进入，是解除其他功能灰置的主入口。顶部或子菜单内应持续显示当前方法名；未选择时显示“尚未选择当前方法”，并提供“选择方法”操作。

### 6.3 分析条件

#### 条件配置

- **方法参数**：CCD 布局、色散引用、参考波长、激发、重复、质控阈值和分转角保存区间。
- **分析谱线**：基线、分析线、内标线、定位线、检测范围、峰值、内标、拟合和标准点。
- **参数预览与打印**：方法条件、谱线和标准点预览、PDF 与打印。

这组回答“当前方法怎样测、测哪些谱线、采用什么计算条件”，与旧版“分析条件”保持一致。

本组全部入口依赖当前方法。未选择当前方法时保留组和子项，但子项灰置，不得把整组隐藏成“系统没有这些功能”。

### 6.4 分析测试

#### 准备

- **样号与样品队列**：预录样号、重复展开、SAM 导入导出和采集后命名。

#### 摄谱与校准

- **色散摄谱与校准**：色散采集、谱线定位、拟合、发布和方法绑定。
- **蒸发与样品摄谱**：蒸发/样品任务、平均或全时保存、重复、暂停/继续/停止和收尾。
- **自动转角采集**：短波到长波、关键波段优先、异常重试、人工接管和安全停止。

#### 查看与分析

- **谱图查看**：单/多样品、转角/CCD、谱线定位、十字线、横纵缩放和可见范围输出。
- **定量分析与慢进**：分析运行、峰位周边谱图、人工调整、单/多样品原始结果。
- **重复性质控**：均值、极差、标准差、RSD/ID、预警、接受、剔除和恢复。
- **标准曲线与样品结果**：拟合方式、坐标、标准强度调整、曲线发布、样品结果与合并保存。

分析测试组严格按照“准备样号→摄谱→看谱→分析→质控/曲线”的顺序排列。设备档案编辑、串口诊断、汞灯专用调试和旧版迁移不放入本组。

其中“色散摄谱与校准”入口不依赖当前方法；其“绑定到方法”内部操作可在需要时单独要求已发布方法。其余样号准备、蒸发/样品摄谱、自动转角、谱图查看、定量分析、重复性质控和标准曲线入口均依赖当前方法。

### 6.5 数据处理

#### 全时与重算

- **全时区间处理**：全时区间查看、区间明细/平均导出和旧 EDT 转换。
- **结果重算与矩阵导出**：方法/时间校验、版本绑定重算、强度和结果矩阵导出。

#### 报告

- **分析报告**：报告编号、模板、排列、筛选、预览、确认、打印和 TXT/CSV/Excel/PDF 导出。

这组只处理已经存在的数据和最终输出，不提供采集入口，也不放旧数据的首次只读迁移。

本组允许在没有当前方法时进入报告、导出和既有数据浏览页面，由页面提示选择数据文件。只有“重新计算”控制要求当前方法；“导出全时数据”控制要求当前方法且其保存模式为全时。不得因为一个内部按钮缺少前置条件而把整个“数据处理”组隐藏或全部禁用。

### 6.6 工具

#### 设备与光学校准

- **设备参数与实时调试**：设备档案、串口/波特率、连接诊断、CCD 实时曲线和独立调试。
- **汞灯与光学校准**：汞线选线、峰位偏移、校正建议、应用、回滚和安全关闭状态。

#### 旧版兼容迁移

- **旧方法与配置迁移**：`DIRECT.MTD/CFG/OPT` 只读暂存和提交。
- **旧谱数据迁移**：`.cdt/.cmt/.edt/.wdt` 只读迁移。
- **旧结果迁移**：`.pdt/.dat` 只读迁移。

迁移入口统一放在一个静态小标题下，不再散落在方法、谱图和报告入口之间。迁移仍保持三个独立页面和独立权限，不合并业务实现。

### 6.7 系统管理

#### 系统配置与维护

- **软件设置**：目录、显示、打印、时间、日志及运行选项。
- **备份与维护**：在线备份、验证、恢复演练、WAL/空间维护、日志和临时内容清理。

#### 身份与审计

- **用户与权限**：本地账户、角色、授权和启用状态。
- **审计记录**：登录、权限、采集、分析、导出和维护事件查询。

#### 扩展（仅测试构建）

- 测试扩展模块只在测试构建且用户具备全部所需权限时显示；正式构建不显示空的“扩展”分段。

### 6.8 帮助

- **离线帮助**：业务流程、错误码、旧格式兼容和常见问题。
- **关于与诊断**：版本、构建信息、SQLite、模块清单和本地服务诊断。

帮助组不放设置、维护或权限操作。

## 7. 当前入口到新结构的映射

| 当前入口/Page | 新顶层组 | 新入口 | 定向视图 |
| --- | --- | --- | --- |
| `workspace` | 工作台 | 工作台概览 | 默认 |
| `methods` | 光谱方法 | 方法库与当前方法 | `lifecycle` |
| `methods` | 光谱方法 | 页面与打印机设置 | `print-settings` |
| `methods` | 分析条件 | 方法参数 | `conditions` |
| `methods` | 分析条件 | 分析谱线 | `lines` |
| `methods` | 分析条件 | 参数预览与打印 | `print-preview` |
| `samples` | 分析测试 | 样号与样品队列 | 默认 |
| `dispersion` | 分析测试 | 色散摄谱与校准 | 默认 |
| `sample-acquisition` | 分析测试 | 蒸发与样品摄谱 | 默认 |
| `hardware-acquisition` | 分析测试 | 自动转角采集 | 默认，显示“需真实设备”状态 |
| `spectra` | 分析测试 | 谱图查看 | 默认 |
| `analysis` | 分析测试 | 定量分析与慢进 | `raw` |
| `analysis` | 分析测试 | 重复性质控 | `quality` |
| `analysis` | 分析测试 | 标准曲线与样品结果 | `curve`，页内继续切换标准点/样品结果 |
| `postprocessing` | 数据处理 | 全时区间处理 | `interval` |
| `postprocessing` | 数据处理 | 结果重算与矩阵导出 | `recalculate-export` |
| `reports` | 数据处理 | 分析报告 | 默认 |
| `acquisition` | 工具 | 设备参数与实时调试 | 默认 |
| `mercury-calibration` | 工具 | 汞灯与光学校准 | 默认，显示“需真实设备”状态 |
| `migration` | 工具 | 旧方法与配置迁移 | 默认 |
| `spectrum-migration` | 工具 | 旧谱数据迁移 | 默认 |
| `result-migration` | 工具 | 旧结果迁移 | 默认 |
| `settings` | 系统管理 | 软件设置 | 默认 |
| `maintenance` | 系统管理 | 备份与维护 | 默认 |
| `users` | 系统管理 | 用户与权限 | 默认 |
| `audit` | 系统管理 | 审计记录 | 默认 |
| `extension` | 系统管理 | 扩展 | 由扩展清单决定 |
| `help` | 帮助 | 离线帮助 | 默认 |
| `about` | 帮助 | 关于与诊断 | 默认 |

表中的 `view/section` 是导航目标，不代表新建后端模块。它只要求现有页面在被导航打开时选择正确的内部页签或内容区。

## 8. 展开与收起交互规则

### 8.1 默认状态

- 首次进入时所有可展开组均收起，只显示八个顶层入口。
- 工作台单击直接导航，不出现空子菜单。
- 当前页面所属顶层组保持高亮，但不会仅因高亮自动展开。
- 当前方法、本地服务状态、用户和版本保持固定位置，不随菜单滚动消失。

### 8.2 鼠标悬停

- 指针在可展开组上停留约 `250 ms` 后，在该组项右侧展开子菜单。
- 从组按钮移动到子菜单时提供约 `200 ms` 的容错关闭延迟，避免交互过程误关。
- 仅悬停打开的子菜单在指针离开组与子菜单后关闭。
- 同一时间最多显示一个组。

### 8.3 鼠标点击

- 单击顶层组打开并固定该组右侧子菜单；再次单击同一组、单击侧边栏外部或按 `Esc` 关闭。
- 单击其他组时直接切换到新组，不同时保留两个子菜单。
- 点击固定子菜单内的功能后保持该组打开，便于连续切换同组功能；用户主动关闭或切换顶层组后再收起。
- 点击悬停临时子菜单内的功能后完成导航并自动关闭临时子菜单。

### 8.4 键盘与触控

- `Tab` 可依次聚焦顶层组和已展开子项。
- `Enter`/`Space` 打开或固定组；上下方向键移动子项；`Esc` 关闭并把焦点返回顶层组。
- 触控设备没有悬停，第一次点击打开，第二次点击同一组关闭；子项点击直接导航。
- 子菜单应具备 `aria-expanded`、`aria-controls`、`aria-current` 和清晰焦点样式，不能只依赖颜色表达状态。

### 8.5 子菜单布局

- 子菜单固定在顶层组项右侧，使用与侧栏一致的深色背景和独立边界；卡片垂直中心默认与正在展开的顶层组按钮中心对齐，不遮挡左侧菜单；正式视觉验收目标为 `1920×1080`、`2560×1440` 和 `3840×2160`，`1280×720` 只作为最低窗口安全回归。
- 子菜单直接继承侧栏背景，不重复显示组标题或固定徽标；使用静态分段标题和功能入口，分段标题不可点击。
- 子项说明最多一行，长内容通过完整 `title` 或可访问说明提供，不让菜单高度失控。
- 分析测试等较长分组在右侧面板内部滚动，展开内容不改变导航区高度；底部服务状态和当前方法信息保持可见。
- `deferred_external` 页面只显示“需真实设备”小徽标，不禁用模拟软件入口。

### 8.6 当前方法与运行前置条件

导航先执行权限过滤，再执行运行条件判定。两者的结果不得混淆：

| 状态 | 侧边栏表现 | 用户反馈 |
| --- | --- | --- |
| 无访问权限 | 不显示入口；过滤后为空的组也不显示 | 直接访问仍由后端返回 401/403 |
| 有权限且前置条件满足 | 正常显示并可进入 | 显示当前方法名及必要状态 |
| 有权限但无当前方法 | 入口保留、灰置、不可导航 | 悬停/聚焦说明“请先选择已发布的当前方法”，子菜单提供“选择方法”快捷操作 |
| 有当前方法但未开启全时模式 | 普通方法功能可用；仅全时导出控制灰置 | 说明“当前方法未启用全时保存模式” |
| 需要选择业务数据文件 | 导航入口保持可用 | 进入页面后显示文件选择器或空状态，不把“未选文件”当作全局灰置条件 |
| 真实硬件尚不可用 | 模拟闭环入口保持可用并显示“需真实设备” | 页面内拦截真实设备命令，遵守既有硬件安全规则 |

运行条件键至少包括：

```text
none                        不依赖当前方法
current_method              要求 currentMethod.method_id != null
current_method_exp_seg      要求当前方法存在且启用全时保存模式
page_scoped                 导航可进入，由页面内具体操作检查数据文件、分析运行或硬件状态
```

旧版动作到新版入口的最低对应规则如下：

| 新版入口/内部操作 | 最低运行条件 | 旧版对应依据 |
| --- | --- | --- |
| 方法库与当前方法 | `none` | 新建方法、打开方法始终是解锁入口 |
| 页面与打印机设置 | `none` | 不依赖当前方法 |
| 方法参数、分析谱线、参数预览与打印 | `current_method` | `ActMtdCfg/ActMtdLines/ActMtdPrint` |
| 色散摄谱与色散曲线 | `none` | `ActAcqWdt/ActAnaWdt` 始终启用 |
| 样号与样品队列 | `current_method` | `ActInputSamp` |
| 蒸发与样品摄谱、自动转角采集 | `current_method` | `ActAcqEdt/ActAcqSam`；自动转角属于正式方法采集链 |
| 谱图查看、定量分析、质控和标准曲线 | `current_method` | `ActAnaMulti/ActAnaSam/ActAnaEdt` 及其后续页面 |
| 重新计算 | `current_method` | `ActReCal`，进入后再选择匹配的 `.pdt` |
| 导出全时数据 | `current_method_exp_seg` | `ActOutCmtSeg` |
| 报告打印/导出、强度导出 | `page_scoped` | 主菜单不按当前方法灰置，进入后选择 `.dat/.pdt` |
| 旧版只读迁移、软件设置、维护、权限、审计、帮助 | `none` 或既有页面级条件 | 新版独立能力，不错误继承方法门禁 |

状态变化必须即时反映：设为当前方法后无需刷新应用即可解锁；暂停、删除、清除当前方法、退出登录或当前方法接口返回无值时立即重新灰置。正在运行的采集或分析任务不得仅因导航状态变化被静默终止，任务终止仍走各模块既有安全流程。

## 9. 导航数据模型与模块契约

当前 `App.tsx` 使用平面 `navItems` 并串联多个权限 `filter`。实施时应改为一个可验证的导航模型，避免以后增加模块时再次回到平铺和分支堆叠。

建议采用以下概念结构：

```text
NavigationGroup
  id              稳定分组键
  label           中文顶层名称
  icon            顶层图标
  order           固定顺序
  directTarget    仅工作台使用

NavigationEntry
  key             全局唯一入口键
  group           所属分组
  sectionLabel    子菜单内静态小标题，可空
  label           用户可见名称
  description     一行功能说明
  route/page      现有页面
  view            页面内部目标，可空
  requiredAny     至少具备其一的读取权限
  order           组内顺序
  status          normal/deferred_external/test_only
  requiredContext none/current_method/current_method_exp_seg/page_scoped
  disabledReason  前置条件不足时的用户说明键，可空
```

### 9.1 模块清单

- 为 `ModuleManifest` 增加向后兼容的可选 `navigation_entries`，不修改既有 `route`、权限、审计和能力字段含义。
- 一个模块可以声明多个内部导航目标，例如方法模块声明生命周期、条件、谱线和打印视图；分析模块声明原始分析、质控和曲线视图。
- 清单生成器把导航条目写入 `manifest.generated.json`；前端按清单和当前用户权限构建分组。
- 架构测试检查：入口键唯一、分组键合法、顺序确定、路由存在、读取权限属于模块、内部视图名称可识别、运行条件键属于允许集合。
- 测试扩展未声明分组时只能进入“系统管理/扩展”，不得自动出现在日常分析组。

运行条件由统一 `NavigationContext` 求值器处理，当前方法状态必须来自受鉴权的当前方法 API，不得从方法页面局部选中项、浏览器存储或标题文字反推。前端灰置用于解释和引导，后端仍须在方法相关业务 API 上执行同等或更严格的当前方法、已发布版本及设备安全校验。

### 9.2 页面内部视图

- `MethodsPage` 接受 `initialSection`，支持 `lifecycle/conditions/lines/print-settings/print-preview`。
- `AnalysisPage` 接受 `initialView`，复用现有 `raw/quality/curve/standards/samples` 状态。
- `PostProcessingPage` 接受 `initialView`，区分全时区间与重算/导出。
- 同页切换后应更新当前导航目标，使侧边栏高亮与页内状态一致。
- 不要求引入新的后端路由；可以使用前端 `{page, view}` 状态。若采用 URL hash，必须保证刷新和前进/后退可恢复同一视图。

### 9.3 权限过滤

- 每个子项按读取或进入该功能所需的最小权限过滤。
- 用户只要看得到组内任一子项，就显示该顶层组。
- 组内不显示无权限条目的标题、占位符或数量，避免泄漏不可访问模块。
- 页面 API 继续执行后端 401/403 校验；导航隐藏不能代替权限控制。

## 10. 预计代码影响范围

实施时预计只涉及下列范围：

- `app/frontend/src/App.tsx`：替换平面侧边栏渲染、传递页面内部视图。
- `app/frontend/src/navigation.ts`：新增导航分组、规范化、权限过滤和活动项解析；该文件属于正式前端源码，不放在根目录。
- `app/frontend/src/styles.css`：顶层组、右侧中心对齐子菜单、固定/临时状态、焦点和窄窗口样式。
- `app/frontend/src/AnalysisPage.tsx`、`PostProcessingPage.tsx` 及方法页相关代码：接受并同步内部视图目标。
- `app/frontend/src/api.ts` 及现有 Blob 下载调用点：提供统一的桌面保存/浏览器下载适配器，禁止各页面继续复制临时链接下载代码。
- `app/src-tauri/src/main.rs`：提供受控的原生“另存为”命令，校验文件名、允许的扩展名、内容和大小后写入用户确认的路径。
- `app/backend/app/modules/manifest.py`：增加可选导航元数据。
- `app/tools/generate_manifest.py`、`app/manifest.generated.json`：生成并校验导航清单。
- `app/tests/test_architecture.py`、`app/tests/test_api.py`：模块清单与导航元数据契约。

除非实现中发现事实冲突，不应修改采集、分析、迁移、报告和设备服务。

## 11. 实施步骤与门禁

本补充计划按单独的 **S22 — 侧边栏信息架构与功能收纳** 实施；S22 内部按以下顺序完成，但只形成一个外部验收门槛。

### S22-N1 — 锁定导航清单

- 将本文件第 7 章映射转为机器可检查清单。
- 将第 8.6 节的当前方法和运行条件同步写入导航清单，确认混合页面采用入口级还是页内控件级门禁。
- 确认所有现有页面和主要内部视图均有入口。
- 确认八个顶层入口和组内顺序，不在编码过程中随意换组。

失败处理：发现业务归属争议时停在清单阶段，不先写临时分组代码。

### S22-N2 — 导航模型和清单契约

- 增加导航条目数据模型及清单生成。
- 用单一权限过滤函数代替 `App.tsx` 的串联条件。
- 增加统一运行条件求值器，复用现有 `currentMethod` 状态并区分隐藏、灰置和页面内检查。
- 增加唯一性、权限和路由架构测试。

失败处理：保持现有平面导航可运行，不提交半套清单和半套硬编码。

### S22-N3 — 子菜单与交互

- 实现悬停临时打开、点击固定、外部点击/Esc 关闭、单组互斥。
- 实现鼠标容错延迟、键盘焦点和触控行为。
- 实现方法依赖项的灰置、原因提示、`aria-disabled` 语义和“选择方法”快捷入口。
- 保留现有主题、密度和响应式布局。

失败处理：悬停闪烁、焦点丢失或遮挡主功能时不得进入下一内部步骤。

### S22-N4 — 页面内部定向

- 完成方法、分析和后处理的内部视图定向。
- 保证从新菜单进入后准确落到目标页签，而不是只打开页面默认视图。
- 页内切换时同步活动菜单状态。

失败处理：任何入口落错页签或破坏用户正在编辑的数据时回退该定向变更。

### S22-N5 — 角色、扩展和回归验收

- 验证四类内置角色看到的分组和入口符合权限。
- 验证未选择方法、选择已发布方法、清除当前方法和全时模式开关四类运行状态。
- 验证正式构建不显示测试扩展，测试构建按权限收纳到系统管理。
- 完成前端构建、架构契约、浏览器交互和当前页面回归。
- 报告写入 `docs/acceptance-reports/S22/验收报告.md`，并更新验收索引；在 S22 获明确确认前不继续其他新 UI 重构。

### S22-N6 — 统一桌面下载与文件保存

- 将 S17 已使用的原生 PDF“另存为”能力提取为统一下载适配器，覆盖运行消息日志、方法参数 PDF、谱图 CSV/PDF、样品队列 SAM，以及后续新增的前端生成文件。
- Tauri 桌面模式必须打开 Windows 原生保存对话框并由 Rust 写入文件；浏览器开发模式保留标准下载回退，并在元素挂载后触发、异步释放 Blob URL。
- 原生命令不得接受调用方直接指定任意落盘路径；只能写入用户在保存对话框中确认的路径，并按白名单校验扩展名、内容类型和大小上限。
- 保存成功提示只能在文件写入成功后显示，并包含最终路径或文件名；用户取消时显示“已取消保存”，写入失败时显示可操作错误，不得再把仅创建打印记录或后端返回 `200` 误报为“已下载”。
- 同名文件交给原生对话框执行明确的覆盖确认，禁止静默覆盖。S18、S19 由后端直接写入指定输出目录的导出流程不改为 Blob 下载，但必须回归确认其后缀/同名策略不受影响。

失败处理：任一入口仍只增加后端记录而未产生文件、取消保存仍显示成功、或任意页面绕过统一适配器重新使用裸 `anchor.click()` 时，本内部步骤不得通过。

## 12. 核心验收标准

### 12.1 功能完整性

- 第 7 章列出的所有当前入口均可从新结构到达。
- 方法条件、谱线、打印、分析质控和曲线等内部目标可直接打开正确视图。
- 不删除原路由，不改变业务 API，不修改用户数据。
- 浏览器刷新或返回页面后，当前顶层组和内部活动项显示正确。
- 当前方法从无到有时依赖项即时解锁；当前方法被清除时即时重新灰置。
- 色散摄谱/曲线、报告与数据文件选择入口不会被当前方法门禁错误禁用。
- 全时导出只在当前方法存在且启用全时模式时可执行。

### 12.2 分组正确性

- 日常分析链只包含样号准备、摄谱、谱图、分析、质控和曲线。
- 设备调试、汞灯和旧版迁移只出现在工具。
- 用户、审计、软件设置和维护只出现在系统管理。
- 全时处理、重算、矩阵导出和报告只出现在数据处理。
- 帮助与关于不混入系统管理或工具。

### 12.3 交互

- 默认状态只显示八个顶层入口。
- 悬停可临时展开，点击可固定展开；只允许一个组同时打开。
- 鼠标从组移动到右侧子菜单不会因交互间隙立即关闭。
- `Esc`、外部点击和再次点击可以可靠关闭。
- 键盘和触控可完成与鼠标等价的导航。
- 活动组、活动子项和焦点状态清晰且不只依赖颜色。

### 12.4 权限

- 系统管理员可看到全部正式入口。
- 方法管理员主要看到方法、分析条件及获授权的工具入口。
- 分析员可进入采集、谱图、分析、数据处理和报告，但不能看到未授权管理入口。
- 只读审计员只看到获授权的查看、报告和审计入口。
- 直接调用 URL/API 时仍由后端返回正确的 401/403。
- 有权限但缺少当前方法的入口是可见灰置；无权限入口不可见，二者不得呈现为同一状态。

### 12.5 构建与视觉

- TypeScript 类型检查和 Vite 生产构建通过。
- 清单生成和架构测试通过。
- 在 `1920×1080`、`2560×1440`、`3840×2160` 及当前 Tauri 桌面窗口完成浏览器验收；`1280×720` 只做最低窗口安全回归。
- 明暗主题、紧凑/舒适密度均无文字遮挡、子菜单裁切和不可达子项。
- 至少保存默认收起、悬停展开、点击固定和权限过滤四类关键截图。

### 12.6 桌面下载与导出可靠性

- 当前基线：S17“图像打印 PDF/文本打印 PDF”已改用 Tauri 原生保存命令；其余运行消息日志、方法参数 PDF、谱图 CSV/PDF 和样品队列 SAM 仍使用 Blob 临时链接，属于待统一项。
- S17 图像/文本 PDF、方法参数 PDF、谱图 CSV/PDF、运行消息 TXT 和样品队列 SAM 在 Tauri 中均能弹出原生保存对话框并实际落盘。
- 保存后的文件存在、大小大于零；可获得服务端 SHA-256 的文件必须与响应哈希一致，PDF 还须验证 `%PDF-` 文件头并可正常打开。
- 用户取消保存不创建文件且不显示成功；非法文件名、路径穿越、未允许扩展名、空内容和超过大小限制均被桌面壳拒绝。
- 浏览器开发模式仍能完成相同入口的标准下载，不因 Tauri API 不存在而报错。
- 同名保存必须出现明确覆盖确认或产生非覆盖文件；不得静默覆盖。
- S18、S19 的后端目录导出分别回归 TXT/CSV/Excel/PDF 与同名后缀策略，确认统一前端下载改造没有改变既有输出目录行为。

## 13. 风险与处理

| 风险 | 处理规则 |
| --- | --- |
| 悬停菜单频繁误开 | 使用约 250 ms 打开延迟和约 200 ms 离开容错；点击固定优先于悬停状态 |
| 分析测试子项过多 | 右侧子菜单内使用不可点击的小标题分段，并由面板内部滚动，不增加第三层折叠菜单 |
| 同一页面承载多个业务入口 | 使用稳定 `view/section` 定向，不复制页面、不创建伪路由 |
| 权限过滤后出现空组 | 在渲染顶层组前先过滤子项，空组完全隐藏 |
| 动态模块破坏分组 | 清单校验分组键；未知测试扩展进入“系统管理/扩展” |
| 自动转角和汞灯尚无真实硬件 | 保留可进入的软件模拟闭环，并显示“需真实设备”，不错误禁用或宣称现场通过 |
| 菜单内容过长 | 右侧子菜单设置独立最大高度并由面板内部滚动，避免推动底部状态区；窄窗口改用点击展开 |
| 重构意外改变业务状态 | 仅改变导航状态和内部视图选择；表单草稿、采集任务及分析运行状态不得因菜单开合被重置 |
| 把页面局部选中方法误当成当前运行方法 | 只信任 `/api/v1/methods/current`；草稿或普通列表选中项不得解锁采集与分析入口 |
| 合并入口内部包含不同前置条件 | 入口保持可达，在页面内只灰置对应按钮；例如报告导出可进入而重新计算仍要求当前方法 |
| WebView2 忽略 Blob 临时链接下载 | Tauri 模式统一走原生保存命令；只有浏览器开发模式使用标准下载回退 |
| 后端生成成功但前端误报已下载 | 把“生成/审计记录”和“用户文件已落盘”作为两个状态，仅在原生写入成功后提示保存成功 |
| 通用保存命令扩大本机文件写入权限 | 路径必须由原生对话框返回，扩展名、内容类型、大小和文件名均使用白名单校验，不接受任意目录参数 |

## 14. 完成定义

只有同时满足以下条件，S22 才可标记为 `passed`：

1. 八个顶层入口及全部组内映射按本计划实现。
2. 所有现有正式功能可达，且无不相关功能混组。
3. 悬停、点击固定、键盘、触控、关闭和活动状态验收通过。
4. 四类角色权限视图正确，后端权限没有被导航逻辑替代或放宽。
5. 模块导航元数据、生成清单和架构测试通过。
6. 前端生产构建和代表性浏览器主流程通过。
7. `docs/acceptance-reports/S22/验收报告.md` 记录计划快照、实际变更、命令、截图、风险和最终状态。
8. 当前方法、全时模式、页面数据文件和真实硬件四类运行前置条件按第 8.6 节实现并完成状态转换验收。
9. 第 12.6 节列出的全部前端下载入口统一通过桌面原生保存和浏览器回退验收，不再存在“接口成功但文件未落盘”的假成功。

本文件已获用户明确授权并按 S22 实施；实际变更、命令、截图、残余风险和结论见 `docs/acceptance-reports/S22/验收报告.md`。S22 获明确确认前不得进入未获批准的新步骤或其他 UI 重构。
