# 地质局项目 AI 执行指南

本文件是 Codex 和其他 AI 进入项目后必须遵守的根级指令，只保留长期有效的执行规则。完整功能范围、架构细节、S00–S21 路线和验收标准以根目录 `PLAN.md` 为准，不在这里重复整份计划。

## 每次任务的入口

- 用户当前明确要求优先；不要把研究、诊断或解释请求擅自扩大为代码修改。
- 开始 `app/` 的实现、修复、重构或验收前，必须先读取 `PLAN.md` 的第 1、3、5 章、第 6 章当前步骤，以及与任务有关的第 7–10 章；再读取 `docs/acceptance-reports/README.md` 和当前步骤报告，确认实际进度与未关闭项。
- 只实现当前获准步骤的最小闭环，不提前创建后续模块空壳。当前步骤完成并获得明确确认前，不得进入下一步骤。
- 强制保持项目主目录整洁：不得在根目录随意新建文件或一级目录。创建任何文件前先确定正式归属；临时内容不得落在主目录，按本文件的目录整洁规则处理。
- 只做研究或格式核对时，按本文件的最短阅读路径取证，不要求通读整份计划。
- 新事实若与 `PLAN.md` 冲突，先按事实来源优先级记录冲突并更新受影响范围，不得为了符合计划而改写事实。

## 项目定位与目录职责

重构目标是以旧版 **SpecDirect 2.0.2 自动转角光谱仪分析软件** 为事实对象，在 `app/` 中构建独立的 **FastAPI + React/TypeScript + SQLite + Tauri 2** Windows 桌面应用。旧 Delphi 工程和历史资料只用于研究、迁移与兼容验证。

- `app/`：新版正式实现，包含后端、前端、Tauri、迁移工具和测试。
- `Spec Source/`：旧 Delphi/VCL 源码、协议、构建与运行资料，是旧业务逻辑的主要源码依据。
- `Spec2.02/`：旧版实际运行目录、配置、方法库和现场数据，用于复现行为与兼容验证。
- `SpecFile/`：从旧资料提取的分类索引与副本；先读其 `README.md` 和 `文件来源清单.csv`，结论仍须回溯原件。
- `Spec2.02功能研究/`：旧功能、源码和截图的整理成果，适合快速定位，不替代原始证据。
- `docs/`：跨目录研究、基线、设计和分步骤验收报告。
- `UI测试/`：只提供前端视觉样式参考，不是开发目标，也不是任何功能、业务、接口、数据或架构依据。

准确目录名是带空格的 `Spec Source`。已经删除的 `SpecSource` 不得重新创建。

## 事实来源与最短阅读路径

发生冲突时按以下顺序判断：

1. 实际旧版运行行为和原始数据。
2. `Spec Source/` 中的 Delphi 源码与窗体定义。
3. `Spec Source/Doc/` 及其他原始协议和格式资料。
4. `Spec2.02功能研究/` 的整理结论。

`SpecFile/` 分类副本不构成新的独立证据；`UI测试/` 完全不在事实来源序列中。

- 研究旧功能：先读 `Spec2.02功能研究/01_功能树_源码整理版.md`，再到 `Spec Source/Source/` 核对 `.pas/.dfm`。
- 研究 API 或业务规则：先读 `Spec2.02功能研究/`，再用 `Spec Source/` 和 `Spec2.02/` 交叉验证。
- 解析旧文件：先读 `SpecFile/README.md` 和来源清单，回到原件，再搜索 `Spec Source/Source/Common/` 的读写实现。
- 核对旧界面：查看 `Spec2.02功能研究/screenshots/` 和 `Spec2.02/spec2.02截图/`。新版视觉以 `docs/ui-test-homepage-reference.png` 为项目级参考；即使查看 `UI测试/`，也不得从中推断功能或架构。
- 排查运行配置：读取 `Spec2.02/DIRECT.CFG`、`DIRECT.OPT` 并核对旧源码；不得参考 `UI测试/backend/` 的演示适配代码。

## 正式实现约束

- 新版产品代码只放在 `app/`，不得在 `Spec Source/`、`Spec2.02/`、`SpecFile/` 或 `UI测试/` 中实现新版功能。
- 后端采用构建期模块化单体；领域层不依赖 FastAPI、SQLite、串口或 Tauri。跨模块调用只使用公开应用服务、API 或版本化事件，不读取其他模块内部表。
- 新模块通过统一清单注册 API、前端导航、权限、审计、事件及可选设备/算法/导入导出能力；禁止在多处添加分支开关，也不得通过修改共享内核绕过模块契约。
- React 只通过版本化 API 访问业务数据；Tauri 本机文件、进程、串口和打印能力按最小权限声明。
- 旧格式采用只读迁移：保留原件、大小、时间和 SHA-256，不生成或回写旧 Access 文件；解析和迁移先在副本或 staging 中验证。
- 转角、汞灯等缺失协议不得猜测命令或用占位字节实现。模拟器可以支持开发，但不能替代 S14、S15 和正式发布的真实硬件验收。

## 测试、验收与步骤门禁

- 测试采用风险导向的最小集合：变更代码通过相应构建/静态检查；核心主流程至少一条可重复验证；最重要的边界或失败路径至少验证一条。
- 旧格式、算法、权限、迁移和设备安全发生变化时，补充受影响的黄金样本、契约、完整性或安全收尾测试。不要为同一规则重复铺设多层测试，也不设置统一覆盖率门槛。
- 性能、SQLite 完整性、截图和硬件记录只在与当前步骤核心风险直接相关时提供。选定的核心测试必须全部通过，不得用跳过或放宽断言掩盖失败。
- 步骤报告统一写入 `docs/acceptance-reports/Sxx/验收报告.md`，保持简短：执行时的计划 SHA-256、目标与实际变更、核心验收清单、命令与结果摘要、未解决问题/外部依赖、最终状态。
- `artifacts/` 仅在需要保存关键截图、硬件记录、失败诊断或较长输出时创建；不复制无关日志和构建产物，不把验收证据散放到 `app/`。
- 核心功能缺陷、核心测试失败或关键证据不足会阻断确认。只缺协议、真实硬件或签名证书等外部输入时才可标记 `deferred_external`；不影响当前闭环和发布安全的次要问题可登记风险后继续。
- 报告中的计划 SHA-256 是执行快照；后续调整计划不会自动推翻既有验收结论。每一步请求明确确认后才能继续下一步。

## 项目主目录与 `app/` 目录整洁（强制）

- 项目主目录只保留项目级入口/治理文件，以及职责已明确的 `app/`、`docs/`、`Spec Source/`、`Spec2.02/`、`Spec2.02功能研究/`、`SpecFile/`、`UI测试/`。除非用户明确批准并先说明用途与生命周期，否则禁止新增任何根级文件或一级目录。
- 项目主目录禁止散放 `tmp/`、`output/`、覆盖率文件、日志、截图、数据库副本、安装包、导出样例、命令输出或临时说明。正式跨目录成果进入 `docs/`；临时内容优先使用系统临时目录，需要保留在工作区时进入 `app/.local/`。
- `app/` 根目录只保留工程入口、必要配置/锁文件及正式源码目录；可复用脚本进入 `app/tools/`，正式测试进入 `app/tests/`。
- 临时内容优先使用操作系统临时目录；确需在工程内落盘时统一进入 `app/.local/`，可按 `tmp/`、`logs/`、`test-runs/`、`diagnostics/`、`runtime/` 分区，禁止散落到源码、测试或工具目录。
- `app/.local/` 必须排除在版本控制、源码清单、验收归档和安装包之外。安装后的正式运行数据必须使用 Windows 用户数据目录。
- `node_modules/`、`dist/`、`target/`、虚拟环境及工具缓存可使用工具约定位置，但必须忽略、不得进入安装包，也不得创建重复副本。
- 每步结束检查项目根目录和 `app/`。发现未声明内容时先确认归属再归位或清理；来源不明、可能属于用户或删除后难以恢复的文件不得直接删除。

## 修改边界

- 未经明确要求，不修改 `Spec Source/`、`Spec2.02/` 和 `SpecFile/` 的历史数据，也不修改 `UI测试/`。
- 不把旧 Delphi 的技术实现直接照搬为新架构要求；先提炼业务规则和兼容口径。
- 修改旧格式解析时保留原始文件，使用副本和自动化测试验证；不得格式化、覆盖、修复或批量重命名历史数据。
- `SpecFile/` 分类结构若经明确要求发生变化，必须同步更新其 `README.md` 和 `文件来源清单.csv`。
- 保留用户现有改动并避免无关修改；对来源或用途不明确的文件先调查，不擅自移动、覆盖或删除。
