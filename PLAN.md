# SpecDirect 2.0.2 完整重构实施计划

## 1. 项目目标、约束和已锁定决策

- 在独立的 `app/` 目录完整重构 SpecDirect，而不是改造 Delphi 工程或 `UI测试/`。
- 固定架构：FastAPI + React/TypeScript + SQLite + Tauri 2；正式目标为 Windows 10/11 x64。
- 扩展目标为构建期模块化单体：新增业务模块、设备适配器、算法、导入器、导出器或报告模板时不改动既有业务模块；不包含第三方代码运行时热插拔，若未来需要插件市场须另立安全、签名和兼容方案。
- 兼容策略已确定为只读迁移：完整导入已确认的旧版正式格式 `.MTD/.cdt/.cmt/.edt/.wdt/.pdt/.dat/.sam`、`DIRECT.CFG/.OPT`，保留原件和哈希，不生成或回写旧 Access 文件。
- 新版必须同时覆盖旧版全部功能及新增要求：独立基线谱线、转角排序与关键波段优先、异常闭环、汞灯调试、PDF/预览、角色权限和审计。
- 前端只参考指定截图的侧栏、配色、卡片密度和控件风格；不得复制 `UI测试/` 的业务、接口、数据或代码。
- 每个步骤只交付一个最小闭环。完成后只运行与本步风险直接相关的核心测试和构建，并形成简短验收结论；截图、性能、数据库或设备证据仅在它们属于本步核心验收时提供。未获明确确认不得进入下一步。
- 发布完成标准：追踪矩阵全部关闭、旧数据迁移通过、算法黄金样本通过、干净 Windows 虚拟机安装/升级通过、真实设备硬件在环测试通过。真实设备后期接入期间，模拟结果不能替代最终设备验收。

尚缺但处理规则已确定的外部输入：

- 仓库缺少 README 所称的“1单片机控制指令.doc”，尤其没有转角控制和汞灯切换协议；不得猜测命令。先实现模拟协议，后期通过设备文档或通信抓取补齐，未补齐则相关功能阻断正式发布。
- 生产发布需要 Windows 代码签名证书；证书未提供时只允许内部测试包。
- 自动更新默认关闭，首版采用签名离线安装/升级包；以后有正式更新端点时再启用签名更新。

## 2. 原始资料与事实来源

事实冲突按“旧版实际行为和数据 → Delphi 源码/DFM → 协议资料 → 研究文档”处理。

- 总入口：[AGENTS.md](AGENTS.md)、[AI 执行提示词](docs/项目重构Plan提示词与UI参考_AI执行版.md)。
- 功能基线：[功能树](Spec2.02功能研究/01_功能树_源码整理版.md)、[截图覆盖清单](Spec2.02功能研究/02_截图清单_功能覆盖版.md)、[综合功能说明](Spec2.02功能研究/03_Spec2.02功能说明与截图文档.md)。
- 业务和算法依据：[Main 模块说明](<Spec Source/Source/Main/README.md>)、[Common 核心说明](<Spec Source/Source/Common/README.md>)及对应 `.pas/.dfm`。
- 运行证据：`Spec2.02/`、`Spec Source/Bin/`、`Spec Source/Res/` 中的配置、方法库、样品、结果、日志和 `.acq` 模拟帧。
- 格式资料索引：[SpecFile 资料索引](SpecFile/README.md)和[文件来源清单](SpecFile/文件来源清单.csv)；其中 77 个分类副本均须回溯 `Spec Source/` 原路径，不重复计为独立证据。
- CCD 顺序和数据帧：每轮 3 组×2 CCD×2048 点，帧头 1 字节，总长 24,579 字节；依据[送数顺序](<Spec Source/Doc/2送数顺序.doc>)。
- Access 表及 BLOB 布局：`.cdt/.cmt` 的 `LAYOUT/CCD_BAND`、`DIRECT.MTD` 方法库和 `.sam` 文本结构；依据[数据存储格式](<Spec Source/Doc/3数据存储格式.doc>)。
- 已只读核实的真实基线：`DIRECT.MTD` 含 3 个方法、20 条谱线、5 条色散曲线和空 `USER` 表；仓库有 6 个恰为 24,579 字节的 `.acq` 帧及最大约 460 MB 的 `.cmt` 样本。
- 视觉来源仅为 [UI 首页参考图](docs/ui-test-homepage-reference.png)；其显示内容不进入需求追踪。

## 3. 目标架构、数据模型和公共接口

### 工程与模块边界

新工程统一放在 `app/`：

- `backend/`：领域模型、应用服务、SQLite 仓储、算法、旧格式适配、设备适配、FastAPI。
- `frontend/`：React/TypeScript、路由、表单、谱图组件、报告预览；只通过 API 访问业务数据。
- `src-tauri/`：窗口、单实例、文件对话框、FastAPI sidecar 生命周期、安装升级。
- `tools/legacy-mdb-reader/`：独立的 .NET 8 win-x86 只读 Jet/OLEDB 迁移工具，由 FastAPI 编排，不承载业务规则。
- `tests/`：黄金样本、契约、端到端、打包、模拟器和硬件在环测试。

领域边界固定为：身份审计、方法与版本、样品队列、采集任务、设备、谱图、分析计算、报告、迁移和维护。领域层不依赖 FastAPI、SQLite、串口或 Tauri。

### 工程与工作区目录整洁约束

- 项目根目录只保留项目级入口/治理文件（如 `AGENTS.md`、`PLAN.md`、README、许可和版本控制配置）及职责已明确的一级目录：`app/`、`docs/`、`Spec Source/`、`Spec2.02/`、`Spec2.02功能研究/`、`SpecFile/`、`UI测试/`。新增一级目录前必须先在 `AGENTS.md` 或本计划中说明用途和生命周期。
- 根目录不得散放 `tmp/`、`output/`、覆盖率文件、日志、截图、数据库副本、安装包、导出样例、命令输出或临时说明。跨目录的正式研究、设计、验收和发布说明进入 `docs/` 的对应子目录；与新版开发有关且确需保留在工作区的临时内容统一进入 `app/.local/`，其他一次性内容使用操作系统临时目录。
- `app/` 根目录只保留工程入口、必要配置/锁文件和上述正式目录，不放临时脚本、调试日志、截图、测试数据库、导出样例、命令输出或重复备份；可复用脚本进入 `tools/`，正式测试进入 `tests/`，正式验收证据进入根级 `docs/acceptance-reports/`。
- 临时内容优先使用操作系统临时目录；确需在工程内落盘时统一放入 `app/.local/`，可按 `tmp/`、`logs/`、`test-runs/`、`diagnostics/` 和 `runtime/` 分区，禁止散落到 `backend/`、`frontend/`、`src-tauri/`、`tests/` 或 `tools/`。该目录只存可删除、可再生且不属于源码的内容。
- `app/.local/` 必须被版本控制、源码清单、验收归档和安装包排除；成功任务应清理本次临时内容，失败时只保留定位问题所需的最近一次诊断。正式程序运行数据在开发环境可放 `app/.local/runtime/`，安装后必须使用 Windows 用户数据目录。
- `node_modules/`、`dist/`、`target/`、虚拟环境及工具缓存属于标准依赖或构建目录，可保持工具约定位置，但须忽略且不得进入正式安装包；不得在多个位置创建同类依赖副本。每步结束对项目根目录和 `app/` 各执行一次简单整洁检查，发现未声明文件或散落临时内容时先确认归属，再归位或清理；来源不明或可能属于用户的文件不得直接删除。

### 模块扩展契约

- 采用模块化单体而非运行时插件：后端模块统一包含 `domain/application/repository/api`，前端对应 `features/<module>`，并自带迁移、权限、审计、事件和测试；共享内核只保留稳定值对象、错误模型和契约，不放业务流程。
- 每个模块提供静态 `ModuleManifest`，声明模块键、版本、API 前缀、前端路由/导航、权限、审计动作、事件及可选的导入器、导出器、算法、报告或设备能力；构建时生成类型化注册表和 `/api/v1/capabilities`，禁止在多处维护分支开关或依赖隐式反射发现。
- 数据表、迁移和后台任务均有唯一模块所有者；跨模块调用只经公开应用服务、API 或版本化事件，禁止读取其他模块内部表。迁移使用模块前缀、全局顺序和升级/失败恢复测试，已发布迁移不可改写。
- REST、OpenAPI 和事件载荷遵守向后兼容规则：新增字段默认可选，删除/改义先弃用并跨一个发布周期；消费者契约测试阻止破坏性变化。Tauri 文件、进程、串口和打印能力按模块清单最小授权，未声明能力默认拒绝。
- 提供模块模板、生成命令和架构测试，自动检查目录、依赖方向、表所有权、路由/导航唯一性、权限/审计覆盖、事件版本和 Tauri 能力；新增模块不得通过修改共享内核绕过契约。

### SQLite 设计

- SQLite 启用 WAL、外键和版本化迁移；所有写操作通过单写入队列和短事务完成。
- 核心实体：`users/roles/permissions/audit_events`、`methods/method_revisions`、`ccd_layouts/dispersion_calibrations/spectral_lines/standard_points`、`sample_queues/items`、`acquisition_tasks/samples/spectrum_bands`、`analysis_runs/line_results/sample_results/qc_decisions/curve_snapshots`、`import_batches/artifacts/settings/device_profiles`。
- 已参与采集或分析的方法版本不可原地修改；编辑生成新版本。采集、分析和报告永久引用确切方法版本。
- 燃烧帧和暗帧按 little-endian `uint16`、均值按 `float32`，以分谱带压缩 BLOB 存入 SQLite，同时记录形状、数据类型、端序和 SHA-256。
- 每次导入记录源文件路径、大小、时间、哈希、编码、解析器版本和实体映射；同一哈希重复导入必须幂等。

### API 与事件

- REST 路由：`/api/v1/auth`、`capabilities`、`methods`、`calibrations`、`sample-queues`、`imports`、`spectra`、`acquisitions`、`analyses`、`reports`、`devices`、`settings`、`backups`、`audit-events`。
- `/ws/events` 只推送采集、分析、导入和导出的状态及曲线更新；开始、暂停、继续、停止等命令仍使用具备幂等键的 REST。
- OpenAPI 生成前端类型并做契约差异检查；所有错误使用统一的 `code/message/field/details/correlation_id`。
- 主要公开枚举：
  - `LineType = baseline | analysis | internal_standard | positioning`
  - `InternalStandardMode = none | background | line`
  - `PeakMode = max_single_point | gaussian`
  - `FitMode = linear | quadratic | cubic | spline`
  - `CoordinateMode = normal | logarithmic`
  - `StorageMode = averaged | full_interval`
  - `CalculationProfile = legacy_2_0_2 | modern_v1`
- 旧方法默认 `legacy_2_0_2`，复现旧高斯峰高、平滑和边界逻辑；新方法默认 `modern_v1`，高斯模式同时保存中心、峰高、Sigma 和峰面积，并以峰面积参与定量，满足新增要求。报告必须显示所用计算档案。

### 身份、UI 与桌面边界

- 首次启动强制创建管理员，无默认密码；密码使用 Argon2id。内置角色为系统管理员、方法管理员、分析员、只读审计员。
- 系统管理员管理用户、设备、系统设置和维护；方法管理员管理方法/谱线/色散；分析员执行采集、质控、分析和报告；只读审计员可查看并导出既有结果和审计记录。
- 所有增删改、导入、采集控制、质控剔除、曲线调整、导出和权限操作均写入不可编辑审计事件。
- UI 使用中文侧栏、浅色工作区、紧凑卡片和高密度表单；谱图由独立 `SpectrumPlot` 适配层承载缩放、平移、十字线、锁线和多样品叠加。
- FastAPI 仅监听随机 `127.0.0.1` 端口。Tauri 经匿名管道传递一次性进程密钥，完成健康握手后加载前端；浏览器端不能直接访问文件系统或启动任意进程。

## 4. 完整功能清单与追踪关系

| 功能组 | 覆盖内容 | 实施步骤 |
|---|---|---|
| F0 运行基础 | 启动、单实例、导航、当前方法/工作类型、动作状态、消息、日志、错误反馈 | S01、S03、S21 |
| F1 方法生命周期 | 新建、打开、另存、重命名、启停、删除、当前方法 | S03 |
| F2 方法条件 | 激发、CCD 布局、参考/实际波长及线宽、重复与质控、按转角保存区间、色散选择 | S03、S12 |
| F3 分析谱线 | 增删改与排序、四类谱线、检测范围、三种内标、峰值、背景、拟合、单位/含量/超差、标准样品 | S04 |
| F4 方法输出 | 页面/打印机设置、方法参数与谱线打印 | S05 |
| F5 样品和旧数据 | 样号预录、队列、前后命名、已确认旧格式只读迁移 | S06–S09 |
| F6 谱图交互 | 单/多样品、CCD/谱带导航、参考校正、定位与位置保存恢复、缩放/滚动、十字线、优先显示、细节和打印 | S10、S12 |
| F7 采集 | 色散、蒸发、均值/全时样品、预热、暂停/继续/停止、收尾 | S11–S14 |
| F8 新设备要求 | 关键波段标记、短波到长波、关键波段优先、电弧/峰位异常闭环 | S04、S14 |
| F9 调试 | 实时 CCD、通信诊断、不保存样品的独立激发、光路对中、汞灯校准 | S11、S14、S15 |
| F10 分析计算 | 参考线、定位、寻峰、背景、内标、单/多样品、慢进人工干预 | S16 |
| F11 质控与曲线 | 均值/极差/标准差/RSD/ID、剔除、四类拟合、对数坐标、曲线调整、视图切换、合并保存和曲线打印 | S17 |
| F12 后处理 | 全时区间独立导出、蒸发文件转换、方法/时间校验重算、强度/结果矩阵 | S18 |
| F13 报告 | 编号、预览、打印、文本、CSV、Excel、PDF、常规/交换排列、批量选择和同名处理 | S19 |
| F14 工具和帮助 | 数据/默认目录、显示/打印/时间选项、设备参数、备份、数据库空间整理、日志清理、帮助、关于 | S01、S11、S20 |
| F15 权限与发布 | 登录、角色、权限、审计、Tauri 安装升级 | S02、S21 |

任何新增、删除或变更的功能都必须先更新此追踪关系及来源证据，不能以 UI 实现替代功能证据。

《功能树》旧版节点映射：0.1–0.2→S01/S03/S21，1.1–1.3→S03/S05/S06，2.1–2.3→S03–S05，3.1–3.5→S07/S10–S18，4.1–4.4→S18–S19，5.1–5.6→S01/S03/S06/S11/S18/S20，6.1–6.2→S01/S20。

新软件要求映射：1.1.1→S03/S13，1.1.2–1.1.3→S04，1.1.4→S04/S16，1.1.5–1.1.6→S04/S17，1.1.7→S05；1.2.1→S04/S14，1.2.2→S12/S13/S16，1.2.3–1.2.4→S10/S12/S16，1.2.5→S07/S13，1.2.6→S16，1.2.7–1.2.8→S17，1.2.9→S11/S14/S15；1.3.1→S19，1.4.1→S02。

功能研究中“源码确认但缺少合格运行截图”的节点仍是必交付项：采集完整收尾（S13–S14）、全时独立导出（S18）、慢进结果写回（S16）、有效设备/汞灯调试（S11、S14–S15）、帮助主题（S20）及 PDF/预览（S19）。缺图只表示证据待补，不得据此删除功能。

## 5. 分阶段开发路线

- 阶段 0，S00：锁定事实、兼容口径和黄金样本。
- 阶段 1，S01–S02：建立可安装、可登录、可审计的桌面基础。
- 阶段 2，S03–S06：完成方法、谱线、打印和旧方法迁移。
- 阶段 3，S07–S10：完成样品队列、旧数据迁移和谱图查看。
- 阶段 4，S11–S15：完成模拟器、设备参数、三类采集、真实设备、转角异常与汞灯。
- 阶段 5，S16–S18：完成分析、慢进、质控、曲线、全时处理和重算。
- 阶段 6，S19–S21：完成报告、维护、帮助和正式发布。

下列每步的“变化”即涉及模块和产物；未点名的层默认无变化，不得提前搭空壳。每步只需记录范围与追踪项、实际变更、核心验收结果、执行命令及退出码、未解决风险和最终状态；数据库、API、事件、前端或 Tauri 只记录实际发生的变化，不逐层填写“无变化”。

测试采用风险导向的最小集合，不设统一代码覆盖率门槛，也不追求所有排列组合：变更代码须通过对应构建/静态检查；核心主流程至少有一条自动化或可重复验证；最重要的边界或失败路径至少验证一条。旧格式、算法、权限、数据迁移和设备安全发生变化时，分别补充受影响的黄金样本、契约、完整性或安全收尾测试。前端原则上每步只保留一条关键端到端冒烟流程，已在底层验证的同一规则不重复铺设多层测试。选定的核心测试必须全部通过，不得以跳过或放宽断言掩盖失败。

覆盖率只作为发现明显测试空白的参考，不要求每步归档；性能只验证本计划明确给出阈值的步骤；SQLite 完整性只用于迁移、导入、备份恢复等数据风险步骤；截图只用于关键 UI 状态；真实设备证据只在 S14、S15 和 S21 发布门禁中强制要求。

步骤默认按编号执行并逐步确认。只有协议、真实硬件、签名证书等外部输入缺失时，才可在核心可自动验证范围通过后标记为 `deferred_external`；经明确确认可继续不依赖该输入的后续步骤，但相关追踪项和发布门禁保持未关闭。核心功能缺陷、核心测试失败或关键证据不足不得延期绕过；不影响本步闭环和发布安全的次要问题可登记风险后进入后续步骤。

正式确认点共 22 个（S00–S21）。每步内部可以继续拆分开发任务和测试用例，但不另设外部确认门槛；合并不得取消原有验收项。

### 验收报告与证据归档

- 所有步骤统一归档到 `docs/acceptance-reports/Sxx/验收报告.md`，步骤完成后、请求确认前创建。`artifacts/` 为可选目录，仅保存报告正文不便承载的关键截图、硬件记录、失败诊断或较长机器输出，不为凑齐目录而复制日志和构建产物，也不把验收证据散放到 `app/`。
- 报告保持简短，只包含：基本信息（步骤、日期、执行时的计划 SHA-256）、目标与实际变更、核心验收清单、测试命令与结果摘要、未解决问题/外部依赖、最终状态 `passed | failed | deferred_external`。一项证据可同时支持多个验收点，不要求按技术层重复描述。
- 最低证据是可重复的命令及退出码，以及必要时 1–3 个关键文件、截图或结果链接。覆盖率、性能、SQLite 完整性和全量原始日志仅在与本步核心风险直接相关时记录；外部现场证据保留位置和 SHA-256，所有日志、通信和截图须脱敏。
- 核心验收项无结论、核心测试失败或关键证据不可复核时不得请求确认。`deferred_external` 只需简要列出已完成范围、缺失输入、受阻功能和发布影响，经明确确认后可继续独立步骤。
- 报告中的计划 SHA-256 表示执行时所依据的计划快照，后续修改计划不会自动推翻既有验收结论。报告确认后原则上不改写；确需更正时直接在报告末尾追加带日期的更正记录。`docs/acceptance-reports/README.md` 只维护步骤状态和报告链接，S21 发布清单引用全部步骤报告。

## 6. 原子实施步骤、验证标准和失败处理

下列“验收”描述需要成立的核心业务结果，不表示每个短语都要单独编写测试或生成一份证据。优先用少量代表性样例同时覆盖主流程、关键边界和数据安全；只有历史兼容、权限边界、设备安全等高风险项需要更细的专项验证。

### S00–S02：基线与桌面基础

#### S00 — 事实与黄金样本基线

- 目标/产物：建立逐功能证据账、旧文件清单与 SHA-256、字段/二进制协议说明、算法输入输出黄金样本；覆盖所有追踪 ID。
- 不含：新工程骨架和产品代码。数据库、API、UI、Tauri 均无变化。
- 验收：所有受控资料均有哈希；每个功能至少一个有效来源；每种已确认旧格式至少一个可读样本；未识别扩展须分类为别名、非产品资料或待核验项，不得静默忽略；原目录哈希和时间不变。
- 失败：冲突按事实优先级形成待核验条目；依赖该结论的步骤禁止开始。

#### S01 — 桌面壳、工作台与软件设置

- 变化：创建四区工程、`app/.local/` 临时内容约定、模块模板、清单生成器和架构测试，以“关于/诊断”作为首个真实模块验证注册链；SQLite 建元数据、应用设置和运行事件表；提供 `/health`、`/about`、`/api/v1/capabilities`、设置和日志 API；React 实现应用壳、六个业务域导航、工作台、消息面板、设置和关于页，消息支持分类、过滤、清空、保存、复制和全选；软件设置覆盖数据/方法/样品默认目录及日志、显示、打印和时间选项；Tauri 实现单实例、sidecar 启停、异常页和安全文件/目录对话框。
- 不含：登录、方法、设备和业务数据。
- 验收：冷启动 ≤8 秒，重复启动只聚焦现有窗口，退出无残留进程；关于/诊断模块仅凭清单注册 API、导航和能力且无反向依赖，重复键/路由/权限或越界依赖被架构测试拒绝；导航与功能树 F0–F15 均有可达入口或明确未启用状态；设置持久化、消息操作/状态反馈、事件顺序、脱敏、日志轮转及各端构建全部通过；简单目录检查确认项目根目录和 `app/` 无未声明文件、临时内容不散落且 `app/.local/` 不进入正式产物。
- 失败：不保留不可启动的安装包；恢复上个设置快照，归档 sidecar/stdout、关联 ID 和错误证据后停在 S01。

#### S02 — 本地账户、角色、权限和审计

- 变化：新增身份/权限/审计表、登录与管理 API、登录和用户管理 UI；Tauri 只在内存保存会话。
- 不含：业务功能授权之外的域逻辑、域账号或联网认证。
- 验收：权限矩阵所有允许/拒绝用例通过；未登录为 401、越权为 403；所有权限变更 100% 留痕；数据库无明文密码和默认账户。
- 失败：迁移回滚到测试副本，禁止以关闭鉴权绕过。

### S03–S06：方法、条件与旧方法迁移

#### S03 — 方法生命周期、条件与 CCD 布局

- 变化：建立方法和不可变版本；实现新建、打开、复制、重命名、启停、软删除和当前方法，并同步标题、工作类型和动作可用状态；加入 CCD 选择/布局、色散引用、参考波长/实际参考波长/线宽、激发与空白周期、重复次数、标准样品、校准/质控/异常阈值，以及按转角配置曝光区间平均或全区间保存的 API/UI。
- 不含：谱线编辑、色散曲线拟合和旧方法导入。
- 验收：名称及 GB18030 字节边界、当前方法切换和引用版本不可改写通过；参考线与 CCD/色散匹配正确；预激发、周期、帧数、暗帧、重复次数、RSD/ID、异常阈值及每个转角的平均/全时选择、区间包含和最少两帧规则全部通过；无效草稿不能发布。
- 失败：整笔方法事务回滚，不产生半成品版本；草稿保留逐字段错误，但不影响当前有效版本。

#### S04 — 分析谱线完整闭环

- 变化：实现按元素/波长新增及谱线选择、编辑、删除、启停和排序；支持基线/分析/内标/定位四类谱线、唯一参考基线、可检测范围、关键波段标记/优先级、背景/定位引用、三种内标、峰值、拟合、坐标、单位、含量/浓度、超差参数和标准点。
- 不含：实际曲线拟合和样品分析。
- 验收：新增谱线时即时显示可检测 CCD/转角或明确越界原因；关键标记和优先级随方法版本保存；160–800 nm、CCD 安全边界、±0.01 nm 重复、最多 300 条、背景/定位引用、单位/精度/超差、3–9 奇数高斯点及至少 4 个不重复标准点等规则全部通过。
- 失败：无效引用不能提交；循环引用或越界必须返回稳定错误码。

#### S05 — 方法参数预览、PDF 和打印

- 变化：生成方法条件/谱线/标准点 HTML 预览和 PDF，保存纸张、方向、边距、布局及系统打印机选择/参数；支持 Windows 虚拟打印机自动验证。
- 不含：分析报告和批量结果导出。
- 验收：页面和打印机设置重启后保持；预览、PDF 与实际打印字段和页数一致，分页无截断，PDF 文本可提取，虚拟打印输出成功。
- 失败：不更新打印默认值，保留渲染输入和错误 PDF。

#### S06 — 旧方法库和配置只读迁移

- 变化：实现 win-x86 Access 读取工具、FastAPI staging/校验/提交流程及迁移报告 UI；导入 `DIRECT.MTD/CFG/OPT`。
- 不含：旧样品和结果文件。
- 验收：样例库导入 3 个方法、20 条谱线、5 条色散曲线；字段/BLOB/方法引用逐项一致；源文件哈希和时间不变；重复导入不重复建数据。
- 失败：按源文件原子回滚；缺少 32 位 Jet 时给出诊断但不影响正常启动。

### S07–S10：样品、旧数据与谱图

#### S07 — 样品队列与前后命名

- 变化：新增队列/队列项，支持录入、插入、删除、清空、预览、重复次数、`.sam` 导入导出及采集后重命名。
- 不含：实际采集。
- 验收：仓库普通样例及“800 条源记录、展开 960 个谱带”样例往返一致；旧标准名规范化、空样和重复展开正确；重命名不改变谱图哈希且写审计。
- 失败：整次导入回滚，并生成行号、原文和原因清单。

#### S08 — Access 谱图数据迁移

- 变化：统一只读导入 `.cdt/.cmt/.edt/.wdt` 的布局、样品/谱带信息、均值、燃烧/暗帧、坏帧索引、激发条件、名称和时间，并生成迁移报告。
- 不含：谱图查看、分析、蒸发转换和色散拟合。
- 验收：各格式记录数、数组尺寸、帧序、端序、CCD 映射、首尾/抽样 ADC、平均值和哈希符合黄金值；坏帧只标记不替换；约 460 MB 样例导入 ≤10 分钟、峰值内存 <1.5 GB。
- 失败：单文件全量回滚，分别报告损坏记录和布局不一致；不得修改、修复或补齐源文件。

#### S09 — `.pdt/.dat` 结果迁移

- 变化：实现头、样品、重复次数、谱线元数据、区间和矩阵解析；未匹配方法可作为带警告的只读孤立结果导入。
- 不含：重算和报告编辑。
- 验收：所有仓库样例的行列数、方法 ID、时间、样品、Peak/Back/结果值正确；二次导入幂等。
- 失败：未知头或截断文件整体拒绝，不猜测剩余字段。

#### S10 — 谱图查看与交互

- 变化：提供谱带查询 API；实现单/多样品、上下记录/CCD、参考线校正、框选/适配/100%/400%/横纵缩放/还原、滚动、十字线、按转角/CCD 的点强度、曲线优先显示、谱线定位/锁定、原始帧细节和图像打印。
- 不含：在线采集和分析控制。
- 验收：1/8 样品、1/6 CCD、转角和曝光区间组合测试通过；参考校正及全部视图操作可逆，打印与当前可见范围一致；6×2048 点首屏 <500 ms，交互保持 ≥30 FPS，坐标与后端换算容差 ≤0.5 点。
- 失败：降级显示错误边界，不返回伪造或抽样后的分析数据。

### S11–S15：设备与采集

#### S11 — 模拟器、设备参数与实时调试

- 变化：定义 `DeviceAdapter/DeviceEvent/AcquisitionState`；按 3 组×2 CCD×2048 点解析并可控回放 24,579 字节 ACQ 帧及故障；设备档案覆盖串口/波特率、CCD 选择/布局、保护时间和屏幕分辨率/宽度换算；提供连接诊断和不生成样品记录的连续调试/独立激发 API/UI，实时曲线支持选择、CCD 切换、峰值/位置、十字线、滚动、适配、100%/400% 和横纵缩放。
- 不含：正式采样入库、真实串口/电弧/转角命令和汞灯校准；模拟器不作为硬件在环证据。
- 验收：6 份 ACQ 样本的帧长、帧头、CCD 顺序、镜像映射、首尾点和哈希符合黄金值；全部状态/故障可确定重放，10,000 帧内存有界；设备参数边界和屏幕换算正确，连续调试 30 分钟无乱序或泄漏，全部曲线交互可用且停止后无样品/谱图记录，权限和审计通过。
- 失败：拒绝不完整帧并保留偏移、原始头和关联 ID；无效档案整笔回滚，连接失败不覆盖最后有效配置，失败不得写入伪造成功记录。

#### S12 — 色散采集、校准与方法绑定

- 变化：建立色散采集任务、帧和状态；实现条件录入、预激发、燃烧、暗帧、暂停/继续/停止、曲线和收尾；支持已知谱线增删、单条/全部定位、向短波/长波移动及实测位置保存/恢复，据此拟合像素—波长映射，保存系数、残差、有效波段和不可变校准版本并绑定方法修订。
- 不含：修改原始帧、真实转角命令和样品定量分析。
- 验收：状态转换、帧数、暗帧、CCD 映射、时序和幂等命令符合配置；谱线增删与位置保存/恢复往返一致；黄金探针的波长—点位误差满足既有 CCD 容差，越界、点数不足、重复谱线或残差超限不能发布，已引用校准不能原地改写。
- 失败：任务明确失败或取消并保留已收帧；校准保留草稿和逐点残差，不替换当前有效版本，发布事务整体回滚。

#### S13 — 蒸发与样品采集闭环

- 变化：建立蒸发全帧任务并连接 S07 样品队列；实现蒸发/激发条件、实时曲线、逐帧区间标记、峰值/积分曲线分析，以及预录/临时样号、平均/全时保存、重复/试样/预热采集、倒计时、进度与采集消息、暂停/继续/停止、完成收尾和采集后命名。
- 不含：旧 `.edt` 转换、含量计算、标准曲线、报告和真实转角异常处理。
- 验收：燃烧/暗帧、时间轴、CCD 顺序、区间统计与输入逐点一致；空样、普通样、标准样、重复样和后命名组合通过，平均值符合 `float32_le` 黄金值，全时区间完整，暂停/继续不重不漏，重命名不改变 BLOB 哈希。
- 失败：损坏帧只标记不替换；单样失败不污染下一队列项，未完成重复不得冒充完整样品，不生成已发布的蒸发或样品结果。

#### S14 — 真实设备、自动转角与异常闭环

- 变化：依据已确认文档或通信抓取实现串口、CCD、电弧和转角适配器；按短波到长波及显式关键波段优先生成转角计划，以基线强度/位置和定位峰偏移驱动提示、自动校正、有限重采、停止或人工接管，并加入超时、互斥、保护停机、通信追踪和硬件在环入口。
- 不含：汞灯校准和任何猜测协议命令；关键优先级不改变方法版本记录的采集范围。
- 验收：真实适配器通过 S11 契约；模拟器穷举排序、优先级、重复波段、异常恢复、重试耗尽和人工接管；真实设备完成连接、无样品独立激发、转角、采集控制、断线及安全收尾并保存命令、响应、帧哈希和现场结果。硬件不可用时标记 `deferred_external` 并保持发布阻塞。
- 失败：立即停止发命令并进入安全状态；校正或重采超限后停止当前样品，隔离未确认数据并保留原始帧和决策原因，不用 ACQ 回放替代在环结论。

#### S15 — 汞灯调试与光学校准

- 变化：建立汞灯谱线和校准会话，支持选线、实时谱图、峰位偏差、校准建议、应用前后比较及审计；设备切换通过独立适配器能力暴露。
- 不含：在缺少汞灯控制协议时模拟真实开关动作；汞灯结果不替代色散方法校准。
- 验收：模拟谱线的选峰、偏移和校正计算符合黄金值；真实协议和硬件到位后完成开关、稳定、采集、校正、关闭全流程并保存证据，否则保持发布阻塞。
- 失败：任何切换或稳定性异常均回到安全关闭状态，不应用校准；保留应用前版本以支持回退。

### S16–S18：分析与后处理

#### S16 — 定量分析与慢进干预

- 变化：实现参考线校正、定位、扫描、最大单点/高斯、背景、三种内标和单/多样品结果矩阵及批量浏览，按 `legacy_2_0_2/modern_v1` 分离算法；支持逐谱线暂停、峰位周边查看、授权定位调整、接受/放弃、从检查点继续和结果写回，并审计调整前后值和理由。
- 不含：重复性质控、标准曲线编辑、报告排版、修改原始谱图和批量无监督改峰。
- 验收：S00 算法黄金样本逐项通过，单/多样品及批量顺序一致，旧档案复现峰高和边界，新档案保存中心/峰高/Sigma/面积并以面积定量；逐谱线暂停→人工调整→继续→最终结果写回及超时/取消/失败路径全部通过，补旧 EXE 输出或独立交叉验证后才能正式关闭。
- 失败：任何谱线失败均返回稳定错误码和中间量，不以零代替；未确认调整不写结果，会话仅从完整检查点恢复或整次重算，输入和版本保持可重放。

#### S17 — 重复性质控、标准曲线与结果合并

- 变化：计算重复值均值、极差、标准差、RSD/ID 和有效次数，支持预警、接受、剔除、恢复和重算；实现直线/二次/三次/样条、普通/对数坐标、标准点启停、单个/全部原值恢复、手工调整并保存调整后标准强度、不可变曲线快照、曲线图/标准点表/样品结果表切换、谱线导航、结果合并保存及曲线图像/文本预览打印。
- 不含：改变原始强度、跨批次自动合并、采集新标准样品和报告模板设计。
- 验收：零值、单值、阈值边界、全部剔除及异常值的各质控量确定；四类拟合、两种坐标、最少点数、重复 X、非正对数和病态输入通过黄金测试；视图/谱线切换不丢状态，调整强度、合并结果、图像/文本打印均可重放且审计完整，历史分析永久引用确切快照。
- 失败：有效重复不足或拟合不可用时结果不可发布，不自动放宽阈值；保留上一有效曲线，禁止覆盖已被引用快照。

#### S18 — 全时处理、结果重算与矩阵导出

- 变化：选择全时曝光区间、查看逐帧谱带并独立导出区间明细/平均；浏览旧 `.edt` 记录并支持选择/反选、取样区间和目标 CCD 布局后转换为新版样品；校验时间/方法后按确切方法/算法/曲线版本重算且保留原结果，并支持批量选择/全选/反选、输出目录和同名处理后导出原始/处理后强度及结果矩阵为文本、CSV、Excel。
- 不含：生成或回写旧 Access 文件、修改源 `.pdt/.dat`、报告版式和打印。
- 验收：用实际 `.cmt` 完成全时区间文件导出并核对行列/数值；区间边界、选择/反选、跨段、目标 CCD 布局和批量转换通过，帧序、平均值及抽样 ADC 与源一致且源哈希/时间不变；方法/时间不一致有明确阻断或警告，孤立结果、部分谱线和批量重算通过且幂等；导出选择、行列、精度、编码、排列和同名策略正确。
- 失败：每个源文件或重算批次原子回滚并保留诊断，不做隐式重采样；导出使用临时文件，完成前不覆盖目标。

### S19–S21：交付与发布

#### S19 — 报告预览、打印与多格式导出

- 变化：建立带报告编号的版本化模型和模板；支持常规/交换排列、筛选、批量选择/全选/反选、“选择文件→设置格式/排列→预览确认”的三阶段流程、输出目录/文件名/同名处理，以及文本、CSV、Excel、PDF、保存和 Windows 打印，并显示方法、计算档案和质控状态。
- 不含：回写旧 `.dat`、模板脚本执行和联网分发。
- 验收：同一报告模型在预览、PDF、Excel 和打印中的编号、字段和顺序一致；批量选择及常规/交换排列正确，同名策略不静默覆盖；分页无裁切，PDF 文本可提取，中文字体正常，虚拟打印机页数一致，导出权限和审计通过。
- 失败：不发布半成品文件；保留渲染输入、错误日志和关联 ID，原报告及默认打印设置不变。

#### S20 — 备份维护、帮助与关于

- 变化：实现 SQLite 在线备份、校验、恢复演练、保留策略、WAL 检查点、`optimize`/受控空间回收、日志轮转和临时数据清理，显示备份路径和操作结果并提供权限化维护 API/UI；用新架构离线帮助替代旧 `DIRECT.CHM`，覆盖导航、目录/索引/检索、核心流程、错误码、兼容和设备安全，关于页显示名称、版本、版权/许可、构建和诊断信息。
- 不含：Access 压缩、自动删除历史分析结果、无确认远程备份、复制旧帮助版权内容、在线遥测或把 `UI测试/` 描述当作产品说明。
- 验收：活动读写期间备份可用，恢复后 `integrity_check`、外键、实体数和抽样 BLOB 哈希一致；空间回收前后业务数据/哈希不变，空间不足、损坏、权限和保留策略路径通过；所有菜单和稳定错误码有可达主题，目录/索引/搜索无断链，关于信息与安装包元数据和 `/about` 一致。
- 失败：恢复仅在隔离副本验证成功后切换，失败保留当前数据库且不执行部分清理；缺少帮助主题阻断发布追踪项关闭，但不影响应用启动。

#### S21 — Tauri 安装、升级与正式发布

- 变化：完成 Windows x64 Tauri 包、FastAPI sidecar、前端静态资源、单实例、随机本地端口、一次性进程密钥、数据库迁移、离线安装/升级/卸载和发布清单。
- 不含：无签名正式发布和没有正式端点时启用自动更新。
- 验收：干净 Windows 10/11 x64 虚拟机完成安装、冷启动、升级、保留数据、卸载和无残留进程；使用模块模板增加一个仅在测试构建启用的示例模块，仅新增模块目录和清单即可接入 SQLite 迁移、API、前端导航、权限、审计、版本化事件及可选 Tauri 能力，消费者/架构/安装升级测试全部通过且正式包不含该模块；安装包清单确认不含 `app/.local/`、测试输出、缓存、调试文件和非运行必需的构建中间物；S00–S21 验收报告、证据链接和状态索引完整，追踪矩阵、旧数据、算法、报告、权限、审计及真实设备在环门禁全部关闭。无证书时只产出明确标记的内部测试包。
- 失败：不发布不可回滚或未签名的正式安装包；归档构建、迁移、sidecar 和安装日志，停在 S21 等待缺失门禁。

## 7. 数据迁移与旧格式兼容策略

- 旧资料始终只读：导入前记录路径、大小、UTC 时间、SHA-256、schema/编码和解析器版本；在 staging 校验后以单事务提交。
- 格式分工固定为：S06 处理 `.MTD/CFG/OPT`，S07 处理 `.sam`，S08 处理 `.cdt/.cmt/.edt/.wdt`，S09 处理 `.pdt/.dat`，S18 处理面向新版数据的蒸发区间转换。
- 32 位 Jet 仅存在于隔离的 .NET 8 win-x86 读取工具；兼容 8/10 字符样名和 16/50 点标准 BLOB，未知头、长度、端序或布局整体拒绝。
- 同一源哈希重复导入必须幂等；每次生成包含映射、警告、拒绝项、抽样值、哈希和回滚状态的报告。新格式不宣称可被旧版读取。

## 8. 设备模拟、真实接入与替代验证

- 领域与应用层只依赖 `DeviceAdapter`；ACQ 回放、故障模拟及真实串口/CCD、电弧、转角和汞灯分别实现适配器，协议不进入业务规则。
- S11 模拟场景固定随机种子、虚拟时间和输入哈希，用于可重放地验证状态机、算法、UI 和故障恢复。
- 真实适配器只能依据正式协议或通信抓取实现；缺少的转角/汞灯命令保持不可用，禁止用猜测字节占位。
- 硬件在环证据必须包含设备/固件、配置、通信、帧哈希、状态线和安全收尾。无硬件时可继续独立功能，但 S14–S15 及 S21 的发布门禁保持未关闭。

## 9. Tauri 桌面集成与发布策略

- Tauri 管理 FastAPI sidecar 的启动、握手、有限重启和退出清理；FastAPI 只监听随机 `127.0.0.1` 端口，一次性密钥经匿名管道传递且不落盘。
- 前端只访问版本化 API 和 `/ws/events`；文件、打印和受控路径能力由 Tauri 封装。数据、日志、备份和安装资源分目录存放。
- 升级前备份并在副本验证数据库迁移，失败恢复原版本和数据。首版离线发布、自动更新关闭；无代码签名证书时仅生成内部测试包。
- 构建产物记录各组件版本和 SHA-256，发布清单须能追溯安装包、测试、源版本及设备门禁。

## 10. 风险、依赖和待决策事项

- 转角和汞灯协议缺失、真实硬件在环缺失、旧 EXE 算法第二判据缺失及代码签名证书缺失仍是明确门禁，不能以模拟、截图或人工声明替代。
- `Spec Source/Bin` 中的 `.qct/.qct_` 与 `.edt/.wdt` 具有相同 Jet 表结构，但不在旧源码 `TSdFileType/FileExts` 及已确认流程中；现阶段仅作为待核验遗留资料保留，不宣称为独立兼容格式。若取得旧版运行证据，须先更新 S00 证据账、黄金样本和 S08 范围。
- 旧格式按源 schema 和真实长度分派；新增变体前先更新 S00 证据账与黄金样本。
- `legacy_2_0_2` 与 `modern_v1` 长期并存，报告和重算必须显示计算档案，禁止静默覆盖旧版复现结果。
- 当前根目录不是 Git 仓库；每一步在验收报告中记录实际变更文件、计划快照和核心测试命令即可。仅对历史输入、迁移产物、安装包和设备现场证据保留 SHA-256。
- 任何新事实与本计划冲突时，按事实来源优先级更新追踪矩阵、风险和受影响步骤，未确认前停止依赖步骤。

## 11. 第一个开发步骤与确认门禁

- S00 已通过机器验证但仍需明确确认；确认前不创建 `app/` 或开始 S01。
- S00 验收报告归档于 `docs/acceptance-reports/S00/验收报告.md`；缺少该报告或报告与机器验证不一致时不得确认 S00。
- 确认后只执行 S01 的桌面壳、工作台、软件设置与“关于”闭环，不同时进入登录、方法或设备模块。
- S01 完成后把核心构建/启动测试、主要风险和结论写入 `docs/acceptance-reports/S01/验收报告.md`；仅把必要的较长输出放入 `artifacts/`，然后再次等待确认。
