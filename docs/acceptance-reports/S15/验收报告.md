# S15 验收报告

- 计划执行快照 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`deferred_external`

## 目标与实际变更

完成汞线选线、合成实时谱、峰位偏移计算、校正建议、前后对比、应用/回滚和审计的可验证闭环，并将真实汞灯控制隔离为独立设备能力：

- 新增 schema v15 的 NIST 汞线、校准会话、选线、不可变原始帧、不可变调试轨迹、不可变光学调整版本、活动版本指针和消息表。
- 合成汞谱适配器明确标注 `physical_lamp_command=false`，不冒充真实开关；串口适配器在协议缺失时进入 `deferred_external`，无命令类型轨迹或命令字节。
- 对选定汞线执行稳定帧采集、限定窗口峰值定位、偏移中位数、校正建议及前后 RMS/最大残差计算；候选版本只有满足校正上限和校正后容差才可应用。
- 光学调整版本独立于 S12 色散校准和方法版本；应用只移动活动指针，校准前版本保留并可回滚。
- 新增 `mercury-calibration` 模块清单、三级权限、版本化 API 和 React 操作页，展示选线、状态、实时谱、峰位表、建议、版本哈希和安全轨迹。

汞线波长与相对强度采用 [NIST Strong Lines of Mercury](https://physics.nist.gov/PhysRefData/Handbook/Tables/mercurytable2.htm) 的 Hg I 数据，并限制在默认 CCD 布局覆盖范围内。

## 核心验收清单

- [x] 253.6517、296.7280、302.1498、312.5668、313.1548 nm 汞线按来源保存；会话至少选择两条且选线核心字段不可变。
- [x] 黄金模拟以 4 条线完成 2 帧稳定和 1 帧测量；定位偏移、中位数校正、前后 RMS 与最大残差计算一致，校正后残差在 1 点容差内。
- [x] 应用候选光学调整版本后活动指针切换；回滚恢复校准前版本；色散校准版本数和方法版本数不变。
- [x] 启动失败、稳定失败和采集失败均进入 `safe_off`，不生成/应用候选版本，保留失败、关闭和审计证据。
- [x] 原始汞谱帧、光学调整版本和调试轨迹不可修改或删除；并发控制拒绝重叠控制操作。
- [x] 真实串口协议缺失时进入 `deferred_external`，`commands_sent=false`，且轨迹表不允许 `command` 类型。

## 命令与结果摘要

- `python -m py_compile app/backend/app/main.py app/backend/app/schemas.py app/backend/app/auth.py app/backend/app/modules/manifest.py app/backend/app/modules/mercury_calibration.py`：通过。
- `python -m pytest tests/test_s15_mercury_calibration.py -q --basetemp=.local/test-runs/s15-pytest-run1 -o cache_dir=.local/test-runs/s15-pytest-cache1`（工作目录 `app`）：`7 passed`。
- `python -m pytest -q --basetemp=.local/test-runs/s15-full-run2 -o cache_dir=.local/test-runs/s15-full-cache2`（工作目录 `app`）：`88 passed`，仅有 Starlette/httpx 弃用警告。
- `node app/frontend/node_modules/typescript/bin/tsc -p app/frontend/tsconfig.json`：通过。
- `node node_modules/vite/bin/vite.js build --configLoader runner`（工作目录 `app/frontend`）：成功，转换 1595 modules 并生成生产包。
- `python tools/generate_manifest.py`（工作目录 `app`）：生成 14 个模块，架构清单契约通过。

## 未解决问题与外部依赖

旧源码和现有资料没有可确认的汞灯开关协议、稳定判据或现场抓包，当前也没有真实汞灯与 CCD 硬件记录。因此真实设备验收仍为 `deferred_external`，发布门禁继续阻断。获得协议文档、抓包和现场硬件后，必须补做开启、稳定、采集、校准应用、关闭及证据保存；异常时还须验证回到真实安全关闭状态。合成汞谱测试不能替代该验收，汞灯结果也不能替代 S12 色散方法校准。

## 2026-08-12 更正复验

- v15 现在通过连续迁移链单独执行和登记，并受原子失败回滚测试覆盖。
- 汞灯执行权限定为系统管理员；分析员、方法管理员和操作员只能按各自职责访问允许的读写能力，不能执行真实或合成汞灯控制。内置角色陈旧授权会在启动时撤销并审计。
- `test_s15_mercury_calibration.py` 已纳入 S11-S16 定向 `57 passed` 和后端全量 `98 passed` 回归；前端类型检查和 Vite 生产构建通过。

结论：S15 软件闭环通过，状态继续为 `deferred_external`。真实汞灯协议、稳定判据、现场抓包与硬件安全关闭证据仍是未满足的发布门禁。

## 2026-08-12 长信息完整性更正

- 会话名、轨迹名称和安全状态增加完整悬停文本；候选快照与通信 SHA-256 可查看并复制完整值。
- 调试轨迹不再只渲染最后 6 条，全部记录进入可滚动列表；状态继续为 `deferred_external`。
