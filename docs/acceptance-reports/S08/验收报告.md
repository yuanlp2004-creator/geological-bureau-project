# S08 验收报告

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`passed`

## 目标与实际变更

实现 `.cdt/.cmt/.edt/.wdt` 的只读 Access 读取、暂存、结构校验、迁移报告和单文件原子提交。保留源文件路径、大小、修改时间、SHA-256、读取器信息和解析器版本；布局 BLOB 按旧源码校验小端 `Single/Byte`，均值使用小端 `Single`，燃烧/暗场帧使用小端 `Word`，CCD 映射与数组维度全部校验。`ErrIndex` 仅转换为坏帧元数据，不替换原始 ADC。提交按源 SHA-256 幂等，源变化或解析失败不写入正式谱带。

新增后端 `spectrum-migration` 模块、权限、API、schema v9 暂存/正式表和审计事件；前端增加旧谱迁移报告页。

## 核心验收清单

- [x] 四种扩展名均完成合成黄金样本暂存与提交，记录数、布局维度、端序、CCD 映射、首尾/采样值和 SHA-256 可复核。
- [x] `.cmt/.edt/.wdt` 燃烧/暗场 BLOB 长度按激发条件和 CCD 维度校验；`.cdt` 均值 BLOB 按 `CCD × 点数 × 4` 校验。
- [x] 坏帧只记录 `phase/index/legacy_value`，提交后的原始 ADC BLOB 与输入一致，无替换。
- [x] 损坏 CCD 映射在写库前拒绝；同一源 SHA-256 再次导入返回 `already_committed`。
- [x] SQLite 单事务提交与源文件前后指纹校验；服务重启后 schema v9、完整性检查和前端代理均正常。

## 命令与结果摘要

- `python -m pytest -q tests/test_s08_spectrum_migration.py`：`6 passed`
- `python -m pytest -q -o addopts='' --basetemp=app/.local/full-test-tmp -o cache_dir=app/.local/full-test-cache`：`44 passed, 1 warning`
- `npm run build`：TypeScript 与 Vite 构建通过
- 运行态：`GET http://127.0.0.1:8787/health` 返回 `ok/schema_version=9`；`/api/v1/diagnostics` 的 `sqlite_integrity=ok`；`http://127.0.0.1:5173/health` 代理返回 `ok`。受限执行环境下使用原数据库的工作副本 `app/.local/runtime/geospectrum.sqlite3` 启动验证，原 `%LOCALAPPDATA%` 文件未修改。

## 未解决问题/外部依赖

- 当前机器未安装 32 位 Microsoft Jet 4.0 时，真实旧 Access 文件导入会按设计返回 `legacy_reader_unavailable`（HTTP 503）；不影响其他功能，真实文件验收需具备 Jet 读取环境。
- 尚未实现谱图查看、分析、蒸发换算或色散拟合，这些属于后续步骤。

## 2026-08-09 更正复验

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 将 x86 Jet 读取改为 JSONL 元数据加分块 BLOB 文件，Python 端按记录流式校验并写入 staging；源文件 SHA-256 也改为分块计算，正式提交使用单条 `INSERT ... SELECT`，不再把全部谱图 BLOB 同时载入内存。
- 修正真实 CMT/EDT/WDT 的激发条件来源：`BurnCount`、`DarkCount` 等字段取自 `LAYOUT`；转角只在旧文件确有角度字段时记录，不再推断或伪造。
- 四格式与损坏分块回归：`8 passed`。真实 `.CDT/.EDT/.WDT` 各解析 120 条；460,529,664 字节真实 CMT 解析 960 条，暂存 19.819 秒、提交 6.234 秒，x86 Jet/Python 联合峰值工作集 133.47 MiB，满足 10 分钟和 1.5 GB 门槛。
- 本机已验证 32 位 Jet 4.0 可用；上文“当前机器未安装”是旧快照，本次结果取代该项。全量测试：`57 passed, 1 warning`。
- 最终状态：`passed`，无未关闭项。

## 2026-08-12 长信息完整性更正

- 旧谱迁移的源文件名、完整 SHA-256、迁移指纹和首条记录摘要均可完整查看，长名称改为换行展示或悬停全文。
- 前端类型检查、生产构建及真实浏览器复验通过；S08 继续为 `passed`。

## 2026-08-10 能力清单标题修正

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 修正 `spectrum-migration` 模块标题的历史乱码，将能力清单标题恢复为“旧谱数据迁移”，并重新生成 `app/manifest.generated.json`。
- 增加生成清单中文标题回归断言；架构与 API 定向测试：`8 passed, 2 warnings`，Python 编译检查通过。
- 最终状态：`passed`，无未关闭项。
