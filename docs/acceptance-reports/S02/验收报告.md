# S02 验收报告

## 基本信息

| 项目 | 内容 |
|---|---|
| 步骤 / 日期 | S02 / 2026-08-06（Asia/Shanghai） |
| 计划 SHA-256 | `6875AE6DF7D3CAB921A117A8E7A3359A228E2F1953CEF92DDC3347212AB36E5F` |
| 最终状态 | `passed` |

## 目标与实际变化

完成本地账户、Argon2id 密码哈希、四类内置角色、权限依赖、内存会话、用户/角色管理 API 与 UI、审计记录。

## 核心验收

- [x] 允许/拒绝权限矩阵通过；未登录返回 401，越权返回 403。
- [x] bootstrap 只允许一次，创建 `system_administrator`、`method_administrator`、`analyst`、`read_only_auditor`。
- [x] 用户和角色权限变更均写入审计；停用账户不能登录。
- [x] 数据库仅保存 `$argon2id$` 哈希，无明文密码和默认账户。
- [x] 会话只保存在进程内存，服务重启后令牌失效。

## 测试摘要

- `python -m pytest -q --basetemp=app/test-tmp`：`15 passed`。
- `python -m compileall -q backend tests`：通过。
- `npm run build`：通过。
- 运行验证：`/health` 为 `ok`、schema `2`；`/auth/status` 为 `bootstrapped=false`；浏览器首屏为首次管理员初始化页。

## 未解决问题 / 外部依赖

无 S02 阻塞项。运行环境需要 `argon2-cffi>=23.1,<26`；Tauri 安装包与后续业务域不在本步骤范围。

## 2026-08-09 更正复验

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 补齐设置与运行消息域的 `settings.read/write`、`runtime-events.read/write` 权限；设置读取/修改/重置、日志读取/写入/清空及 WebSocket 事件订阅均执行 401/403 鉴权。
- 设置更新/重置、运行事件创建/清空与谱图查看、导出、打印均登记版本化审计动作；设置审计仅保存脱敏前后摘要和变更分组。
- WebSocket 现在校验内存会话和 `runtime-events.read`，未登录关闭码为 4401，越权关闭码为 4403。
- 定向权限、审计和 WebSocket 回归：`12 passed`；全量 `python -m pytest app/tests -q`：`57 passed, 1 warning`。
- 最终状态：`passed`，无未关闭项。
