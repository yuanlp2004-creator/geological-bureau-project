# S18 验收报告

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 目标：全时曝光区间查看与导出、旧 `.edt` 选择转换、精确版本重算、文本/CSV/Excel 矩阵导出。
- 实际变更：新增 v18 后处理表和 `postprocessing` 模块；保存源哈希、测量时间、区间、目标 CCD 布局、方法/算法/曲线快照引用；EDT 转换产出只读源之外的新版全时样品；重算和导出批次幂等；导出采用临时文件、fsync 和原子替换，并提供同名后缀策略。
- 前端：新增“全时后处理”页面，支持 EDT 全选/反选、区间查看、目标布局转换、版本重算和三类矩阵导出。

## 核心验收

- 区间边界：服务按 1-based 帧号校验，逐帧 ADC 与均值按源顺序计算；跨段区间不做隐式重采样。
- 转换完整性：重复请求按输入指纹返回同一批次；源 `.edt` 哈希、测量时间和原始 BLOB 不变；新版样品保存完整区间帧、均值和 SHA-256。
- 版本阻断：结果矩阵目标方法与重算方法不一致时生成 `blocked` 批次并保留诊断，不覆盖旧结果。
- 导出安全：文本/CSV 使用 UTF-8 BOM，Excel 使用确定性的 SpreadsheetML `.xls`；目标文件完成前不覆盖，默认同名生成后缀文件。

## 命令与结果

- `python -m py_compile app/backend/app/db.py app/backend/app/modules/postprocessing.py app/backend/app/main.py app/backend/app/schemas.py app/tests/test_s18_postprocessing.py`：通过。
- `node app/frontend/node_modules/typescript/bin/tsc --noEmit`：通过。
- `python tools/generate_manifest.py`：通过，生成 16 个模块。
- 独立 SQLite 夹具：区间读取、CSV 原子导出、EDT 转换和幂等重复请求通过。
- 完整 pytest：当前工作区 Python 运行时未安装 `pytest`，未能执行；需在开发环境安装 `app/backend/requirements.txt` 后复跑。

## 未解决问题/外部依赖

- 实际 32 位 Jet/真实 `.cmt` 全量运行仍依赖 S08 既有外部组件；本步骤使用已提交谱带的确定性夹具验证后处理契约。
- Excel 导出使用 Excel 2003 SpreadsheetML `.xls`，不依赖第三方工作簿库；后续如需现代 `.xlsx` 可在不改变矩阵契约的前提下增加格式适配。

最终状态：`passed`

## 2026-08-15 S21 全量复验更正

S21 首次全量回归发现 EDT 转换夹具把 cycle-major 多 CCD 数据的期望顺序写错，并暴露非首位 CCD 仍按位置映射的真实缺陷。已改为按源 `ccd_indices` 映射并补充非首位 CCD 回归；S18 定向测试 5 项和最终全量测试 117 项全部通过，状态继续为 `passed`。

## 2026-08-16 阻断修复复验

- 后处理列表与区间接口同时支持已提交的 `.edt` 和 `.cmt`；前端从 API 枚举已发布方法版本、来源记录和可发布曲线快照，不再要求手工输入来源 ID。
- 精确重算不再复制旧结果：旧 PDT 按 Delphi 的峰值/背景、背景内标、谱线内标和 `1e-5` 下限规则恢复定量信号，再严格应用用户选择的不可变曲线快照；任一来源失败时整批回滚。采集样品来源也使用所选曲线计算最终值。
- 真实原件 `Spec2.02/DATA/测试15-5_20170509_[2-16].cmt`（50,323,456 bytes，SHA-256 `959F3F1822ADBAD3BE960112236DF6376DA2B870348C3BFD8C96D5DDBB94149F`）只读暂存并提交 `120` 条记录；后处理读取 `15` 帧、每 CCD `2048` 点，首帧 SHA-256 `FE7F423659EF5E9F27220C6181CAAE5BBB9EA217FD7E939E336443439AC5802A`，均值 SHA-256 `E9F88B33EC5410445138A1CA7ECC95BDBB773C54120E61C5A82934E246960336`，执行前后原件元数据与哈希一致。
- 最终全量回归 `124 passed`，前端生产构建通过。

更正结论：真实 CMT 证据和精确重算阻断均已关闭，状态继续为 `passed`。

## 2026-08-18 桌面 UI 布局更正复验

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`。
- 修复 S18 页面未接入项目样式体系的问题；改为项目原生页面边距、双栏工作区、卡片、字段和按钮，并新增专用响应式样式。
- 旧记录表、重算源、曲线快照和运行历史均使用独立限高滚动区；旧记录支持搜索、全选/反选、固定表头、选中反馈和完整值提示，补回目标 CCD 布局输入；导出卡片独立置于工作区底部。
- 业务 API、后处理算法、旧文件只读规则和导出契约未变。

复验结果：

- `npm.cmd run frontend:build`：通过，TypeScript 与 Vite 生产构建成功。
- `python -m pytest -q tests/test_s18_postprocessing.py`（项目绑定 Python，工作目录 `app/`）：`7 passed`。
- Tauri 开发版以 1440×920 窗口和隔离的 200 条源记录数据完成桌面人工复验：双栏排版、表格内部滚动、固定表头、记录选择反馈、曝光区间字段及底部导出卡片显示正常；隔离验收库已清理。

更正结论：S18 桌面 UI 阻断已关闭，状态继续为 `passed`。
