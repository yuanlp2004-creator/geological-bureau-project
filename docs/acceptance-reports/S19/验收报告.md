# S19 验收报告

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 日期：2026-08-14
- 目标：报告编号与版本化模型、模板、常规/交换排列、筛选和批量选择，统一预览确认，以及文本、CSV、Excel、PDF、保存和打印输出。
- 实际变更：新增 v19 SQLite 迁移（`report_templates`、`reports`、`report_exports`）；新增 `reports` 后端模块和 `/api/v1/reports` 路由；统一模型驱动 HTML 预览、显式确认、UTF-8 文本/CSV、SpreadsheetML Excel、ReportLab PDF/打印；文件采用临时文件、fsync、原子替换和 suffix/error/overwrite 同名策略；所有创建、预览、确认和导出写入审计；新增 React 报告三阶段页面。

## 核心验收

- 同一模型一致性：报告编号、版本、字段、排列、方法版本、计算档案和质控状态保存于模型；预览、文本、CSV、Excel、PDF 和打印均从同一模型渲染。
- 批量与排列：支持多个已完成分析运行、全选/反选、常规/交换排列和样品/元素筛选；筛选为空时拒绝建立报告。
- 输出安全：TXT/CSV 使用 UTF-8 BOM；Excel 使用确定性的 SpreadsheetML；PDF 记录页数并使用中文 CID 字体；目标完成前不覆盖，默认同名追加后缀；打印输出返回 PDF 内容和 SHA-256。
- 发布门禁：新报告初始为 `draft`，必须完成预览后的显式 `confirm` 才能导出或打印，未确认报告不会生成半成品文件。
- 权限与审计：`reports.read/write/export` 按角色注册；报告创建、预览、导出分别记录不可编辑审计事件。

## 命令与结果

- `python -m py_compile app/backend/app/db.py app/backend/app/modules/reports.py app/backend/app/main.py app/backend/app/schemas.py app/tests/test_s19_reports.py`：通过（bundled Python）。
- `npm --prefix app/frontend run build`：通过，Vite 生产构建完成。
- `python app/tools/generate_manifest.py`：通过，生成 17 个模块清单。
- 独立 SQLite 夹具（v19 初始化、报告创建、统一预览、draft 导出阻断、显式确认、TXT/CSV/Excel/PDF 导出、页数、SHA-256、原子文件、同名后缀和审计事件）：通过。
- 完整 `pytest` / FastAPI `TestClient` 契约：未执行；当前 bundled Python 未安装 `pytest` 和 `fastapi`（`No module named pytest` / `No module named fastapi`），需在安装 `app/backend/requirements.txt` 的开发环境复跑。

## 未解决问题/外部依赖

- Windows 实际打印机和虚拟打印机页数仍需在带 Windows 打印栈的环境复验；本步打印端点已生成与 PDF 相同的确定性内容并记录页数。
- Excel 输出保持项目既有的 SpreadsheetML `.xls` 兼容策略；如后续需要 `.xlsx`，可增加格式适配而不改变报告模型。

最终状态：`passed`

## 2026-08-16 阻断修复复验

- 报告模型改为读取 S17 最新不可变结果合并快照中的最终 `value`、曲线快照、计算口径、质控状态和单位；缺少最终合并结果时以稳定错误码阻断，不再把 S16 `quantitative_signal` 当作报告结果。
- PDF 固定每页最多 `20` 行并重新计算列宽；`45` 行回归样本生成 `3` 页且末行文本存在。
- 打印 API 先枚举 Windows 打印机并要求明确选择；虚拟 PDF 打印保留输入和完成文件，系统打印通过 Windows 打印分派，成功与失败均记录报告导出和审计，不再让前端把 PDF 当 JSON 解析。
- TXT、CSV、Excel、PDF 与虚拟打印一致性测试通过；最终全量回归 `124 passed`，前端生产构建通过。

更正结论：最终结果口径、分页和实际打印链路阻断已关闭，状态继续为 `passed`。

## 2026-08-19 桌面预览阻断修复复验

- React 报告预览不再依赖 `window.open`；改为应用内模态预览和 `iframe srcDoc`，避免 Tauri WebView2 弹窗策略拦截或静默忽略。
- `npm.cmd run build` 通过；独立目标目录完成 Tauri release 与 NSIS 构建，新 EXE SHA-256 为 `4169F481CC575C991D9B162B86C81ECEE4C95BCEC28F8ECAB5C131C539D3EA76`。
- 关闭旧单实例进程后，以原验收数据目录启动新 EXE；人工选择 `S19-MANUAL-001 v1` 并打开应用内预览，报告编号、QA-1、Fe、254.0、分析值、单位、曲线快照、`modern_v1`、质控状态和模型 SHA-256 均正常显示。

更正结论：桌面预览阻断已关闭，状态继续为 `passed`。

## 2026-08-19 交换排列字段顺序修复复验

- 修正 `exchange` 只调整记录排序、未调整输出字段顺序的问题；新建交换排列报告统一使用“报告编号、元素、波长、样品名称、分析结果、单位、曲线快照、计算档案、质控状态”，因此 S17 数据显示为 `Fe / 254.0 / QA-1`，常规排列保持原顺序。
- 表头改为按不可变报告模型的 `columns` 动态生成，HTML 预览、TXT、CSV、Excel、PDF 和打印继续共用同一模型；既有报告版本不回写，需新建交换排列版本复验。
- `test_s19_reports.py` 增加交换排列模型、表头和 CSV 首行顺序断言；S19 针对性回归 `3 passed`，Python 编译检查和前端生产构建通过，独立 Tauri release/NSIS 构建通过。

更正结论：交换排列字段顺序阻断已关闭，待登录后使用 S17 运行新建报告版本完成桌面人工复验。
