# S21 验收报告

- 执行日期：2026-08-15（Asia/Shanghai）
- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 初次执行状态：`failed`

## 目标与实际变更

完成 Tauri 随机回环端口、一次性进程密钥、前端运行时握手、窗口关闭 sidecar 回收、升级前在线备份/副本迁移/原子切换/失败回滚、内部测试包元数据、发布清单生成器和安全构建链。新增仅在测试构建发现的 manifest-only 示例模块，接入 SQLite 迁移、API、动态导航、系统管理员权限、审计、版本化事件和可选 Tauri 能力；正式模块清单仍为 18 个模块，不含示例模块。

## 核心验收

- [x] React/Vite 生产构建通过；Rust/Tauri `cargo check --locked --offline` 通过。
- [x] 数据库 v19→v20 升级先在副本验证，数据保留；损坏数据库拒绝升级且原文件字节不变。
- [x] 测试模块只含目录与 `manifest.json`；迁移和模块/Tauri 能力契约 fixture 通过，前端动态导航通过 TypeScript 构建，正式清单排除该模块。
- [ ] 测试模块 API、权限拒绝、审计和版本化事件的 FastAPI TestClient 回归已编写，但当前运行时缺依赖而未执行。
- [x] 发布命令强制先重建和验证 sidecar；旧 sidecar、缺少构建元数据或 schema 非 v20 时拒绝生成安装包/发布清单。
- [ ] Windows x64 新 sidecar 与 NSIS 内部测试包：未生成。当前 bundled Python 缺少 FastAPI/PyInstaller，网络受限无法安装；现有旧 sidecar实测 schema v1，已被拒绝。
- [ ] 干净 Windows 10/11 安装、升级保留数据、卸载、单实例聚焦和无残留进程：缺少虚拟机安装包证据。
- [ ] 正式发布门禁：代码签名证书、S14 转角和 S15 汞灯真实硬件证据仍缺失。

## 命令与结果摘要

- Python `py_compile`：通过。
- `npm.cmd run build`（前端）：通过，1604 modules；仅有既有 chunk size 警告。
- `cargo check --locked --offline`：通过。
- 标准库升级 fixture：通过，v19→v20、实体保留、损坏输入不替换当前库。
- 测试模块 fixture：通过，迁移表、权限和能力契约成立；正式清单生成 18 modules 且不含 `s21-example`。
- `app/tools/verify_sidecar.py`：失败并正确阻断，现有 sidecar `schema_version=1`。
- `app/tools/build_sidecar.py`：失败，当前运行时 `No module named PyInstaller`；pip 因网络策略无法取得依赖。
- 完整 pytest/FastAPI TestClient：未执行，当前 bundled Python 不含 FastAPI/pytest。
- `git diff --check`：通过。

## 未解决问题/外部依赖

发布门禁详见 [GeoSpectrum 0.1.0 发布门禁](../../releases/0.1.0/发布门禁.md)。本次没有生成或保留不可用安装包，也没有用旧 sidecar 伪装 S21 产物。补齐构建依赖并通过 sidecar/NSIS 后仍只能作为未签名内部测试包；正式发布还需干净虚拟机、签名证书和 S14/S15 真实硬件证据。

## 2026-08-15 补齐复验

- 更正后的最终状态：`deferred_external`。
- 使用隔离 Python 3.11 环境补齐 requirements；同时把 PDF 验收测试实际使用的 `pdfplumber`、`pypdf` 纳入依赖清单。S21 定向测试通过，全量回归 `117 passed`；仅有 FastAPI TestClient 上游弃用提示。
- 新 sidecar 构建、随机端口/进程密钥健康检查和 schema v20 校验通过；修正 PyInstaller 单文件进程树回收，独立验证及桌面关闭后均无残留进程。
- 数据目录改为 `%LOCALAPPDATA%\cn.geospectrum.desktop`。旧 `%LOCALAPPDATA%\GeoSpectrum` 数据只复制已知运行数据，经 SQLite 完整性、外键、schema 和逐表实体数核对后原子启用，旧目录和数据库保持不变。
- `npm.cmd run release:internal` 整链通过；生成内嵌 WebView2 离线安装器的 Windows x64 NSIS 包及发布清单。安装包 `GeoSpectrum_0.1.0_x64-setup.exe` 为 242591972 bytes，SHA-256 `D0D6DB7AAC128F3E7D888E7AC5528C9DCF051D9316DA2AE3BF673584ED0510F4`，Authenticode 状态 `NotSigned`。
- 本机 Windows 11 Home x64（build 26200）完成 0.0.9 测试夹具→0.1.0 升级、v19→v20 数据保留、完整性检查、冷启动（3.644 秒；最终离线包复测 2.748 秒）、第二实例聚焦、主窗口关闭、卸载、用户数据保留和无残留进程；正式清单为 18 个模块且不含 `s21-example`，NSIS 输入只含主程序、sidecar 和 WebView2 离线运行库。
- 本机不是干净 Windows 10/11 虚拟机，且未发现代码签名证书、真实转角设备/协议或真实汞灯设备/协议。因此只能保留未签名内部测试包；正式发布门禁继续阻断。

## 2026-08-16 阻断修复复验

- 一次性进程密钥不再写入子进程环境变量：Tauri 通过子进程标准输入匿名管道发送一行 32 位随机十六进制密钥，sidecar 校验后才加载应用；密钥写入或 sidecar 启动失败会直接阻止桌面壳继续启动。独立 sidecar 健康检查通过，schema 为 v20。
- 旧数据迁移与升级不再对源库执行 `wal_checkpoint(TRUNCATE)`。迁移先无锁复制稳定的 DB+WAL 原始快照，再只在副本上使用 SQLite backup API；带活动 WAL 的回归确认源 DB、WAL、SHM 三个文件哈希均不变且未提交数据完整进入目标库。
- 发布清单生成器读取 `docs/acceptance-reports/README.md`，S00–S20 软件门禁未关闭时拒绝生成内部测试清单；S01、S14、S15 只允许既定的 `deferred_external` 例外。
- `cargo check --locked --offline`、前端生产构建和最终全量回归 `124 passed`。`npm run release:internal` 整链通过；新 sidecar 为 24,598,289 bytes，SHA-256 `151FC66912BD07057AEA6F461CB082919889363709B4BD0F154ED51649A85D18`。
- 新 release 桌面程序实测启动后保持运行，匿名管道启动的两级 PyInstaller sidecar 进程保持存活；结束桌面进程树后无 sidecar 残留。
- 新 NSIS 包 `GeoSpectrum_0.1.0_x64-setup.exe` 为 242,604,685 bytes，SHA-256 `B782605FD8EFB243AC4C23F7878958FD201E93E623068373D1BC2E98A04B2F6C`，内嵌 WebView2 离线安装器，Authenticode 状态仍为 `NotSigned`；发布清单的软件验收门禁为 `passed`。

本次修正关闭了进程密钥、源库写入和软件发布门禁阻断。干净 Windows 10/11 虚拟机、代码签名证书及 S14/S15 真实硬件证据仍属外部依赖，因此 S21 最终状态继续为 `deferred_external`，安装包仅限内部测试，不可作为正式发布包。

## 2026-08-16 Windows 桌面登录阻断修复复验

- 根因确认：Windows Tauri 2 生产 WebView 的来源是 `http://tauri.localhost`，后端 CORS 白名单此前只有开发来源和 `tauri://localhost`，导致桌面端登录请求在预检阶段以 `Disallowed CORS origin` 被拒绝；本地账户、启用状态和 Argon2id 密码验证本身均正常。
- 后端已显式放行 `http://tauri.localhost`，并新增边界回归：Windows Tauri 来源访问登录预检返回 200 且回显该来源，未知来源仍返回 400 且不返回允许来源头。
- 定向认证/发布回归 `7 passed`；最终全量回归 `125 passed`，仅有既有 FastAPI TestClient 上游弃用提示。`npm.cmd run release:internal` 整链重新通过，前端生产构建为 1604 modules。
- 新 release 桌面程序使用现有正式用户数据目录启动；真实桌面登录后进入工作台，数据库新增一条 `login` 审计记录（ID 1674，`2026-08-16T09:51:21.573+00:00`）。运行中的主程序与 sidecar 均保持响应。
- 新 sidecar 为 24,597,834 bytes，SHA-256 `2D8D6719BFD5315ED271AFC4A298189C731ACC9AE23B5B5A8C867BA722701DF5`；新 NSIS 内部测试包为 242,604,316 bytes，SHA-256 `4A087DF920D7B5F08192A7E02EE327399D26E84DDD318ED0FCD0929C59844003`。

Windows 桌面登录的软件阻断已关闭。S21 状态仍为 `deferred_external`：干净 Windows 10/11 虚拟机、代码签名证书及 S14/S15 真实硬件证据尚未补齐，当前安装包仍只可用于内部测试。

## 2026-08-18 sidecar 运行资源阻断修复复验

- 本次计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`。
- 复现旧 sidecar `2D8D6719BFD5315ED271AFC4A298189C731ACC9AE23B5B5A8C867BA722701DF5`：`/health` 为 `ok` 且 schema v20，但模拟调试返回 `acq_sample_missing`。旧健康检查不足以证明冻结后端具备实际采集能力。
- 三个 ACQ 模拟样本和 32 位 Jet PowerShell 读取脚本已迁入 `app/backend/app/resources/`；清单逐项固定相对路径、字节数、原件路径和 SHA-256，源码运行与 PyInstaller onefile 统一通过 `importlib.resources` 读取，不再依赖安装包外的 `Spec Source/` 或 `app/tools/`。
- sidecar 构建现在先核验资源原件与 app 副本，再显式嵌入资源目录；构建元数据新增资源清单 schema、清单 SHA-256 和四项资源明细。发布清单在 sidecar 资源元数据缺失、过期或不一致时拒绝继续。
- 冻结业务验证不再只检查健康状态：实际完成临时管理员建立/登录、模拟器配置连接、`280-288.acq` 调试启动/推进/停止和旧迁移读取器诊断。返回 5 路 CCD，帧序号推进至 1，源 SHA-256 为 `EF130DE0BDE91CDB084333002207493380AD4B776FC2CE1C6960D19E0E164C0C`，诊断包含 `windows-powershell-x86`，结束后 sidecar 残留进程为 0。
- 最终构建只逐项嵌入资源清单和四个登记文件，不把资源目录中的缓存或未登记内容纳入产物。新 sidecar 为 36,759,065 bytes，SHA-256 `451818C00BADA9D82613F7F4CA53FBCE8DC145A9B4FD24109429B504BF6C710C`；相关资源、设备、迁移、采集、Tauri 和 S21 回归合计 `26 passed`，仅有既有 FastAPI TestClient 上游弃用提示。
- 执行发布清单生成时，新 sidecar 资源校验已通过，随后按预期被 `S17: in_progress` 软件门禁阻断。本次未重建 NSIS；现有安装包仍是旧 sidecar 产物，不得用来判定本修复通过，也不得正式发布。

本次关闭了 sidecar 缺少运行资源且健康检查无法发现的构建阻断。S17 人工数据复验完成前不得重建安装包；S21 仍保留干净 Windows 10/11 虚拟机、代码签名证书及 S14/S15 真实硬件等外部依赖，状态继续为 `deferred_external`。

## 2026-08-18 S17 门禁关闭后内部包重建

- 用户明确确认 S17 及人工验收已通过，验收索引恢复为 `passed`；发布清单读取的 S00-S20 软件门禁通过，仅保留 S01/S14/S15 既定 `deferred_external`。
- 完整执行 `npm.cmd run release:internal` 成功：重新冻结 sidecar、完成业务级 sidecar 验证、重新构建 1605 modules 的前端、编译 Tauri release 桌面壳、生成内嵌 WebView2 离线运行库的 NSIS，并生成内部发布清单。
- 最终 sidecar 为 36,758,645 bytes，SHA-256 `1D522E732366F2D079AAD1C0713A9ACC46A2560338B8F2313BDA5E97E2C5B149`；`src-tauri/binaries` 构建源与 `target/release/geospectrum-backend.exe` 完全一致。冻结验证返回 5 路 CCD、源 ACQ SHA-256 匹配、旧读取器诊断可见且残留进程为 0。
- Tauri 桌面壳 `geospectrum.exe` 为 11,729,408 bytes，SHA-256 `035A4DA58024A5A2FF2C9BE5579BB04CFF872245701AE2FA3CA9C5F726FED55F`。
- 新 NSIS `GeoSpectrum_0.1.0_x64-setup.exe` 为 254,996,400 bytes，SHA-256 `68CD6D73EB2FD26496B3F94C4D26D29CDDFD96BCA02883A0D43116C43582FBA5`；Authenticode 为 `NotSigned`，只可内部测试。发布清单 `software_acceptance_gate` 为 `passed`，S17 为 `passed`。
- 发布后资源/S21 定向回归 `12 passed`；仅有既有 FastAPI TestClient 上游弃用提示。`git diff --check` 通过，临时 Tauri 配置已清理，GeoSpectrum 进程残留为 0。

本次内部测试构建已包含新的 app 自有运行资源 sidecar。S21 状态仍为 `deferred_external`：未签名包不可正式发布，干净 Windows 10/11 虚拟机安装/升级/卸载以及 S14/S15 真实硬件证据仍需外部验收。
