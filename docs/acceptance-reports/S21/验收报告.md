# S21 验收报告

- 执行日期：2026-08-15（Asia/Shanghai）
- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 初次执行状态：`failed`

## 目标与实际变更

完成 Tauri 随机回环端口、一次性进程密钥、前端运行时握手、窗口关闭 sidecar 回收、升级前在线备份/副本迁移/原子切换/失败回滚、内部测试包元数据、发布清单生成器和安全构建链。新增仅在测试构建发现的 manifest-only 示例模块，接入 SQLite 迁移、API、动态导航、系统管理员权限、审计、版本化事件和可选 Tauri 能力；正式模块清单仍为 18 个模块，不含示例模块。

## 核心验收

- [x] React/Vite 生产构建通过；Rust/Tauri `cargo check --locked --offline` 通过。
- [x] 数据库 v19→v20 升级先在副本验证，数据保留；损坏数据库拒绝升级且原文件字节不变。
- [x] 测试模块只含目录与 `manifest.json`；迁移和模块/Tauri 能力契约 fixture 通过，前端动态导航通过 TypeScript 构建，正式清单排除该模块。
- [ ] 测试模块 API、权限拒绝、审计和版本化事件的 FastAPI TestClient 回归已编写，但当前运行时缺依赖而未执行。
- [x] 发布命令强制先重建和验证 sidecar；旧 sidecar、缺少构建元数据或 schema 非 v20 时拒绝生成安装包/发布清单。
- [ ] Windows x64 新 sidecar 与 NSIS 内部测试包：未生成。当前 bundled Python 缺少 FastAPI/PyInstaller，网络受限无法安装；现有旧 sidecar实测 schema v1，已被拒绝。
- [ ] 干净 Windows 10/11 安装、升级保留数据、卸载、单实例聚焦和无残留进程：缺少虚拟机安装包证据。
- [ ] 正式发布门禁：代码签名证书、S14 转角和 S15 汞灯真实硬件证据仍缺失。

## 命令与结果摘要

- Python `py_compile`：通过。
- `npm.cmd run build`（前端）：通过，1604 modules；仅有既有 chunk size 警告。
- `cargo check --locked --offline`：通过。
- 标准库升级 fixture：通过，v19→v20、实体保留、损坏输入不替换当前库。
- 测试模块 fixture：通过，迁移表、权限和能力契约成立；正式清单生成 18 modules 且不含 `s21-example`。
- `app/tools/verify_sidecar.py`：失败并正确阻断，现有 sidecar `schema_version=1`。
- `app/tools/build_sidecar.py`：失败，当前运行时 `No module named PyInstaller`；pip 因网络策略无法取得依赖。
- 完整 pytest/FastAPI TestClient：未执行，当前 bundled Python 不含 FastAPI/pytest。
- `git diff --check`：通过。

## 未解决问题/外部依赖

发布门禁详见 [GeoSpectrum 0.1.0 发布门禁](../../releases/0.1.0/发布门禁.md)。本次没有生成或保留不可用安装包，也没有用旧 sidecar 伪装 S21 产物。补齐构建依赖并通过 sidecar/NSIS 后仍只能作为未签名内部测试包；正式发布还需干净虚拟机、签名证书和 S14/S15 真实硬件证据。

## 2026-08-15 补齐复验

- 更正后的最终状态：`deferred_external`。
- 使用隔离 Python 3.11 环境补齐 requirements；同时把 PDF 验收测试实际使用的 `pdfplumber`、`pypdf` 纳入依赖清单。S21 定向测试通过，全量回归 `117 passed`；仅有 FastAPI TestClient 上游弃用提示。
- 新 sidecar 构建、随机端口/进程密钥健康检查和 schema v20 校验通过；修正 PyInstaller 单文件进程树回收，独立验证及桌面关闭后均无残留进程。
- 数据目录改为 `%LOCALAPPDATA%\cn.geospectrum.desktop`。旧 `%LOCALAPPDATA%\GeoSpectrum` 数据只复制已知运行数据，经 SQLite 完整性、外键、schema 和逐表实体数核对后原子启用，旧目录和数据库保持不变。
- `npm.cmd run release:internal` 整链通过；生成内嵌 WebView2 离线安装器的 Windows x64 NSIS 包及发布清单。安装包 `GeoSpectrum_0.1.0_x64-setup.exe` 为 242591972 bytes，SHA-256 `D0D6DB7AAC128F3E7D888E7AC5528C9DCF051D9316DA2AE3BF673584ED0510F4`，Authenticode 状态 `NotSigned`。
- 本机 Windows 11 Home x64（build 26200）完成 0.0.9 测试夹具→0.1.0 升级、v19→v20 数据保留、完整性检查、冷启动（3.644 秒；最终离线包复测 2.748 秒）、第二实例聚焦、主窗口关闭、卸载、用户数据保留和无残留进程；正式清单为 18 个模块且不含 `s21-example`，NSIS 输入只含主程序、sidecar 和 WebView2 离线运行库。
- 本机不是干净 Windows 10/11 虚拟机，且未发现代码签名证书、真实转角设备/协议或真实汞灯设备/协议。因此只能保留未签名内部测试包；正式发布门禁继续阻断。

## 2026-08-16 阻断修复复验

- 一次性进程密钥不再写入子进程环境变量：Tauri 通过子进程标准输入匿名管道发送一行 32 位随机十六进制密钥，sidecar 校验后才加载应用；密钥写入或 sidecar 启动失败会直接阻止桌面壳继续启动。独立 sidecar 健康检查通过，schema 为 v20。
- 旧数据迁移与升级不再对源库执行 `wal_checkpoint(TRUNCATE)`。迁移先无锁复制稳定的 DB+WAL 原始快照，再只在副本上使用 SQLite backup API；带活动 WAL 的回归确认源 DB、WAL、SHM 三个文件哈希均不变且未提交数据完整进入目标库。
- 发布清单生成器读取 `docs/acceptance-reports/README.md`，S00–S20 软件门禁未关闭时拒绝生成内部测试清单；S01、S14、S15 只允许既定的 `deferred_external` 例外。
- `cargo check --locked --offline`、前端生产构建和最终全量回归 `124 passed`。`npm run release:internal` 整链通过；新 sidecar 为 24,598,289 bytes，SHA-256 `151FC66912BD07057AEA6F461CB082919889363709B4BD0F154ED51649A85D18`。
- 新 release 桌面程序实测启动后保持运行，匿名管道启动的两级 PyInstaller sidecar 进程保持存活；结束桌面进程树后无 sidecar 残留。
- 新 NSIS 包 `GeoSpectrum_0.1.0_x64-setup.exe` 为 242,604,685 bytes，SHA-256 `B782605FD8EFB243AC4C23F7878958FD201E93E623068373D1BC2E98A04B2F6C`，内嵌 WebView2 离线安装器，Authenticode 状态仍为 `NotSigned`；发布清单的软件验收门禁为 `passed`。

本次修正关闭了进程密钥、源库写入和软件发布门禁阻断。干净 Windows 10/11 虚拟机、代码签名证书及 S14/S15 真实硬件证据仍属外部依赖，因此 S21 最终状态继续为 `deferred_external`，安装包仅限内部测试，不可作为正式发布包。

## 2026-08-16 Windows 桌面登录阻断修复复验

- 根因确认：Windows Tauri 2 生产 WebView 的来源是 `http://tauri.localhost`，后端 CORS 白名单此前只有开发来源和 `tauri://localhost`，导致桌面端登录请求在预检阶段以 `Disallowed CORS origin` 被拒绝；本地账户、启用状态和 Argon2id 密码验证本身均正常。
- 后端已显式放行 `http://tauri.localhost`，并新增边界回归：Windows Tauri 来源访问登录预检返回 200 且回显该来源，未知来源仍返回 400 且不返回允许来源头。
- 定向认证/发布回归 `7 passed`；最终全量回归 `125 passed`，仅有既有 FastAPI TestClient 上游弃用提示。`npm.cmd run release:internal` 整链重新通过，前端生产构建为 1604 modules。
- 新 release 桌面程序使用现有正式用户数据目录启动；真实桌面登录后进入工作台，数据库新增一条 `login` 审计记录（ID 1674，`2026-08-16T09:51:21.573+00:00`）。运行中的主程序与 sidecar 均保持响应。
- 新 sidecar 为 24,597,834 bytes，SHA-256 `2D8D6719BFD5315ED271AFC4A298189C731ACC9AE23B5B5A8C867BA722701DF5`；新 NSIS 内部测试包为 242,604,316 bytes，SHA-256 `4A087DF920D7B5F08192A7E02EE327399D26E84DDD318ED0FCD0929C59844003`。

Windows 桌面登录的软件阻断已关闭。S21 状态仍为 `deferred_external`：干净 Windows 10/11 虚拟机、代码签名证书及 S14/S15 真实硬件证据尚未补齐，当前安装包仍只可用于内部测试。

## 2026-08-18 sidecar 运行资源阻断修复复验

- 本次计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`。
- 复现旧 sidecar `2D8D6719BFD5315ED271AFC4A298189C731ACC9AE23B5B5A8C867BA722701DF5`：`/health` 为 `ok` 且 schema v20，但模拟调试返回 `acq_sample_missing`。旧健康检查不足以证明冻结后端具备实际采集能力。
- 三个 ACQ 模拟样本和 32 位 Jet PowerShell 读取脚本已迁入 `app/backend/app/resources/`；清单逐项固定相对路径、字节数、原件路径和 SHA-256，源码运行与 PyInstaller onefile 统一通过 `importlib.resources` 读取，不再依赖安装包外的 `Spec Source/` 或 `app/tools/`。
- sidecar 构建现在先核验资源原件与 app 副本，再显式嵌入资源目录；构建元数据新增资源清单 schema、清单 SHA-256 和四项资源明细。发布清单在 sidecar 资源元数据缺失、过期或不一致时拒绝继续。
- 冻结业务验证不再只检查健康状态：实际完成临时管理员建立/登录、模拟器配置连接、`280-288.acq` 调试启动/推进/停止和旧迁移读取器诊断。返回 5 路 CCD，帧序号推进至 1，源 SHA-256 为 `EF130DE0BDE91CDB084333002207493380AD4B776FC2CE1C6960D19E0E164C0C`，诊断包含 `windows-powershell-x86`，结束后 sidecar 残留进程为 0。
- 最终构建只逐项嵌入资源清单和四个登记文件，不把资源目录中的缓存或未登记内容纳入产物。新 sidecar 为 36,759,065 bytes，SHA-256 `451818C00BADA9D82613F7F4CA53FBCE8DC145A9B4FD24109429B504BF6C710C`；相关资源、设备、迁移、采集、Tauri 和 S21 回归合计 `26 passed`，仅有既有 FastAPI TestClient 上游弃用提示。
- 执行发布清单生成时，新 sidecar 资源校验已通过，随后按预期被 `S17: in_progress` 软件门禁阻断。本次未重建 NSIS；现有安装包仍是旧 sidecar 产物，不得用来判定本修复通过，也不得正式发布。

本次关闭了 sidecar 缺少运行资源且健康检查无法发现的构建阻断。S17 人工数据复验完成前不得重建安装包；S21 仍保留干净 Windows 10/11 虚拟机、代码签名证书及 S14/S15 真实硬件等外部依赖，状态继续为 `deferred_external`。

## 2026-08-18 S17 门禁关闭后内部包重建

- 用户明确确认 S17 及人工验收已通过，验收索引恢复为 `passed`；发布清单读取的 S00-S20 软件门禁通过，仅保留 S01/S14/S15 既定 `deferred_external`。
- 完整执行 `npm.cmd run release:internal` 成功：重新冻结 sidecar、完成业务级 sidecar 验证、重新构建 1605 modules 的前端、编译 Tauri release 桌面壳、生成内嵌 WebView2 离线运行库的 NSIS，并生成内部发布清单。
- 最终 sidecar 为 36,758,645 bytes，SHA-256 `1D522E732366F2D079AAD1C0713A9ACC46A2560338B8F2313BDA5E97E2C5B149`；`src-tauri/binaries` 构建源与 `target/release/geospectrum-backend.exe` 完全一致。冻结验证返回 5 路 CCD、源 ACQ SHA-256 匹配、旧读取器诊断可见且残留进程为 0。
- Tauri 桌面壳 `geospectrum.exe` 为 11,729,408 bytes，SHA-256 `035A4DA58024A5A2FF2C9BE5579BB04CFF872245701AE2FA3CA9C5F726FED55F`。
- 新 NSIS `GeoSpectrum_0.1.0_x64-setup.exe` 为 254,996,400 bytes，SHA-256 `68CD6D73EB2FD26496B3F94C4D26D29CDDFD96BCA02883A0D43116C43582FBA5`；Authenticode 为 `NotSigned`，只可内部测试。发布清单 `software_acceptance_gate` 为 `passed`，S17 为 `passed`。
- 发布后资源/S21 定向回归 `12 passed`；仅有既有 FastAPI TestClient 上游弃用提示。`git diff --check` 通过，临时 Tauri 配置已清理，GeoSpectrum 进程残留为 0。

本次内部测试构建已包含新的 app 自有运行资源 sidecar。S21 状态仍为 `deferred_external`：未签名包不可正式发布，干净 Windows 10/11 虚拟机安装/升级/卸载以及 S14/S15 真实硬件证据仍需外部验收。


## 2026-09-13 后端包目录简化

- 用户授权：将 `app/backend/app/` 全部合并到 `app/backend/`；属于 S01/S21 工程维护。
- 计划 SHA-256：`BDD60187F47E5098887AC569849D151B83EF6BA5F7167CC896E0E0224D2DA1A1`。
- 实际变更：移动 Python 代码、`modules/`、`resources/`，将版本定义合并进 `backend/__init__.py`；更新 `app/package.json`、测试导入、工具脚本、PyInstaller 资源目的路径、发布版本读取、README、项目阅读指南和代码导读。当前入口为 `backend.main:app`。
- 目录深度修正：`modules/legacy_migration.py` 从 `app/tools/` 定位读取器；`modules/legacy_sources.py` 从项目目录查找旧数据。新增两个隔离目录测试，覆盖非项目工作目录和本机读取器定位。资源哈希/源文件一致性测试通过。
- 首轮全量回归 `140 passed, 2 failed`：两个既有测试仍要求 28 个导航入口和未包含目录选择器的 Tauri 命令列表。按 2026-09-13 S22 的 26 入口契约及已存在的 `select_legacy_directory` 注册更新精确断言，未放宽权限或命令边界。
- 最终验证（工作目录 `app/`）：`python -m pytest -q` 为 `142 passed in 42.56s`；`python -m compileall -q backend tools tests` 退出码 0。测试使用隔离 `SPECTRUM_DATA_DIR`。实际 Uvicorn 新入口启动返回 health=ok、schema_version=20。
- `python tools/build_sidecar.py` 和 `python tools/verify_sidecar.py` 均退出码 0。冻结后端健康、模拟采集 5 CCD、帧推进、ACQ 哈希、PowerShell 读取资源均通过；验证器强制终止测试进程树后无残留，此项不等同 GUI 自然退出验收。
- 已更新根目录 `geospectrum-backend.exe`，与 `app/src-tauri/binaries/geospectrum-backend-x86_64-pc-windows-msvc.exe` 哈希一致：`7658DC7DADBD7EF39EBB79106BA36FB353AE52ADD61D19375879176A36FF5A4C`。前端主程序无需源码修改；本次未重建 NSIS 安装包，也未进行 GUI 人工复验。
- 历史验收正文及已发布清单保留当时路径；当前代码与开发文档旧包引用扫描无匹配。根目录及 `app/` 无本次新增散落文件；构建日志在 `app/.local/logs/`，可恢复源码与旧根目录后端备份在系统临时目录 `geospectrum-flatten-hw37p1k9`。
- 本次目录调整状态：`passed`。S21 总体仍为 `deferred_external`，真实硬件、签名和干净 Windows 验收门禁不变。


## 2026-09-13 代码结构重构 R0/R1

- 执行依据：用户“以你的计划开始重构”；范围为可恢复基线和第一批运行依赖/系统 API 拆分。计划 SHA-256：`3334D06B62C4B39D500D6245C7D6A538F5537A2A76F0C8EA8943A274683D3142`；专项计划见 [结构重构计划](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。
- R0：`app/.local/refactor-r1/baseline/` 保存 242 个原文件、哈希、Git 状态及完整 OpenAPI。基线 `python -m pytest -q` 为 `142 passed in 44.97s`，`npm.cmd run frontend:build` 退出码 0（已有 >500 kB bundle 提示，非本次变化）。项目仍有大量未跟踪文件，未提交或清理用户文件。
- 变更映射：main 中的服务装配、共享事件集合及发布逻辑 → `backend/runtime.py`；认证/权限依赖 → `backend/api/dependencies.py`；进程密钥/CORS/请求校验 → `backend/api/middleware.py`；健康、关于/诊断、能力、设置、运行消息接口 → `backend/api/system.py`，通过 `api/__init__.py` 显式注册。新增 `create_app()`，运行对象在 `app.state.runtime`。main 由 2388 行减为 2002 行。
- 其余业务路由保留在 main，依赖参数改为从当前请求获取 runtime，未搬移业务规则。`auth.py` 已无对 main 的反向导入。`sidecar_entry.py` 在升级准备后创建携带进程密钥的独立 runtime，不再读写 main 全局变量。
- 测试迁移：API 测试按用例创建独立应用，固定服务类从其所属模块导入；不再替换 main 的数据库、认证对象或模块缓存。新增 `tests/test_runtime_isolation.py`，覆盖同时存在的两个应用、会话/数据库/设备缓存/事件集合隔离、HTTP/WS 密钥、鉴权拒绝、API 不反向依赖入口。
- 核心验证（工作目录 app）：全量 `python -m pytest -q` 为 `145 passed in 45.64s`；最后仅调整三个测试文件的局部变量名后，`python -m pytest -q tests/test_auth.py tests/test_methods.py tests/test_spectral_lines.py` 为 `18 passed in 11.04s`。`python -m compileall -q backend tests` 退出码 0。
- 契约核对：与基线完整 OpenAPI 相等；203 条 HTTP/WS 路由的名称、权限和声明一致，无重复。已有业务模块、资源、db.py、schemas.py 和前端 src 文件哈希保持一致。测试均使用隔离数据目录。
- 真实子进程启动 `python -m uvicorn backend.main:app --host 127.0.0.1 --port <随机空闲端口>`：health=ok、schema_version=20，验证后停止进程。
- `python tools/build_sidecar.py`、`python tools/verify_sidecar.py` 均退出码 0；冻结健康、5 路 CCD 模拟采集、帧推进、ACQ 哈希和 PowerShell 读取器资源通过。验证器强制停止测试进程树后无残留，不等同 GUI 自然退出的人工验收。
- 新构建只更新 `app/src-tauri/binaries/`：后端 SHA-256 `CCB6F9001C43A4A1B45AEC1C540438CB671EFAE08DB575B45B2AF17243984E8D`。本批未替换根目录 EXE、未重建 NSIS、未进行 GUI 人工复验；完整桌面交付属于 R7。
- 当前代码导读、README、项目阅读指南及计划进度已同步。一次性迁移脚本、备份和日志归入 `app/.local/refactor-r1/`；未增加根级文件或一级目录。
- 本批结果：`passed`。按专项计划停在 R1；R2 身份与方法 API 尚未开始。S21 总状态仍为 `deferred_external`，既有真实硬件、签名和干净 Windows 门禁不变。


## 2026-09-13 代码结构重构 R2

- 用户授权：“继续r2”。计划 SHA-256：`7248D6856FA7041C02303B5F26044612A28D051B5391D4C9CAEAD20F01AB7805`；范围为身份与方法 API、账户事务和对应模型，详见 [专项计划第 11 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。
- 备份：`app/.local/refactor-r2/baseline/` 保存 233 个源码/配置/文档/资源文件及哈希、Git 状态和 R1 完整 OpenAPI；旧 schemas.py 移至本次临时目录，未删除用户文件。
- 代码映射：12 个身份接口 → `backend/api/auth.py`；30 个方法/谱线/方法打印接口及其错误转换 → `backend/api/methods.py`；统一 `api.register_api()`。main 从 2002 行减为 1576 行。
- 账户用户/角色查询、四组写事务及审计 → `AuthService`。服务抛出 `AuthError`，API 映射回原状态码和 detail。事务体只替换依赖对象、actor 参数和框架异常名称，未调整 SQL、校验顺序或会话策略；归一化 AST 比较一致。
- 模型映射：原 schemas.py → schemas 包；6 个身份模型和 14 个方法模型分别到 `schemas/auth.py`、`schemas/methods.py`，三个方法业务模块同步直接导入所属模型。其余模型暂留 `schemas/__init__.py`，兼容旧导入；此过渡状态在 R3/R6 继续整理，不宣称 schemas 已全部拆分。
- 契约：完整 OpenAPI 相等；203 条路由名称、权限和声明保持一致，无重复；全部 84 个模型定义 AST 一致，所有旧模型均有唯一归属。
- 新增 `tests/test_auth_transactions.py`：4 组审计写入失败后的用户/角色/关联/权限整笔回滚；1 组 API 重名、重复角色、未知角色、自禁用、缺失对象及内置角色拒绝矩阵，检查原状态/detail 与零写入。测试准备使用登录后的真实用户 ID，未修改 bootstrap 返回结构。
- 最终验证（工作目录 app，隔离 SPECTRUM_DATA_DIR）：`python -m pytest -q` 为 `150 passed in 47.51s`，退出码 0；包含原权限、方法保存/非法输入、谱线、打印及全部其他业务回归。`python -m compileall -q backend tests` 退出码 0。
- `python tools/build_sidecar.py`、`python tools/verify_sidecar.py` 退出码均 0。冻结后端 health=ok、schema=20，登录后模拟连接/5 CCD/帧推进/ACQ 哈希/PowerShell 读取资源通过，验证器强制停止进程树后无残留；此结果不替代 GUI 自然退出验收。
- 新冻结后端仅在 `app/src-tauri/binaries/`，SHA-256 `0506ACDBD41D53FC1D9E80F95F7CAA4AF0839A4B4B89A85B88F55DDC462B8A27`。根目录 EXE 和 NSIS 未更新，整体桌面交付仍为 R7。
- README、代码导读、项目阅读指南与计划进度已同步。一次性脚本、基线和日志在 `app/.local/refactor-r2/`；根目录与 app 无本次新增散落文件。
- 本批：`passed`。停在 R2；下一批 R3 尚未开始。S21 仍为 `deferred_external`，真实硬件、签名和干净 Windows 等既有门禁不变。


## 2026-09-13 代码结构重构 R3

- 用户授权：“继续r3”。计划 SHA-256：`3635B7A8164A09260DF809112FF73711DED3F4B376AFE2D53364FA2813C6A3AD`；范围与映射见 [专项计划第 12 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。R3 已完成，R4 尚未开始。
- 基线与可恢复副本：`app/.local/refactor-r3/baseline/`，238 个源码/配置/文档/资源文件及哈希、Git 状态、完整 OpenAPI；一次性脚本与各组日志均在 `app/.local/refactor-r3/`。
- 分组完成：队列/三类迁移/谱图 → 对应 api 与 schemas 文件，定向 `30 passed in 13.26s`；设备/色散/采集/硬件/汞灯 → 对应文件，定向 `33 passed in 9.72s`；分析/后处理/报告/维护/WebSocket/测试扩展 → 对应文件，定向 `37 passed in 9.35s`。前组通过后才实施后组。
- main.py 从 1576 行减为 44 行，仅保留生命周期、应用工厂和标准 app 实例。全部 API 通过 `api/__init__.py` 显式注册；WebSocket 位于 `api/events.py`，受测试构建门禁控制的扩展注册位于 `api/extensions.py`。
- 其余 64 个模型已按业务迁入 schemas 各文件，全部 84 个模型定义 AST 保持一致；包入口仅保留显式兼容导出。后端内部模型导入直接指向业务文件，数据库和业务处理未重写。
- 原接口与辅助函数（除应用注册装配）按名称逐项 AST 比较一致，共享错误转换在各目的模块均核对；完整 OpenAPI 相等。业务模块除模型导入外的 AST 一致，db.py、运行资源及前端源码与基线一致。
- 增强既有扩展契约测试：同一进程先创建带测试扩展的应用，再创建普通应用，不残留扩展路由。其他权限、迁移幂等/源完整性、模拟设备安全收尾、分析黄金样本、打印/报告、WebSocket 鉴权与订阅清理沿用已有回归。
- 最终验证（工作目录 app，隔离 SPECTRUM_DATA_DIR）：`python -m pytest -q` 为 `150 passed in 46.08s`，退出码 0；`python -m compileall -q backend tests` 退出码 0。未改变前端源码，无需重复前端构建。
- `python tools/build_sidecar.py`、`python tools/verify_sidecar.py` 退出码均 0；冻结 health=ok、schema=20，5 CCD 模拟采集/帧推进/源哈希/PowerShell 读取资源通过，测试进程树强制停止后无残留。不将其视为 GUI 自然退出人工验收。
- 构建目录后端 SHA-256：`637443463534C80338F2FECDAE8D204CFF97066DE9DAC1B135476730C0F95190`。未替换根目录 EXE、未重建 NSIS；整体验证和桌面交付仍属 R7。
- 当前 README、模块导读、项目阅读指南及计划状态已同步；未新增根级文件或一级目录，临时内容均归于 app/.local。
- 本批 `passed`。S21 总体仍 `deferred_external`，硬件、签名和干净 Windows 外部门禁不变。下一批 R4 为前端请求层与本机能力拆分。


## 2026-09-13 代码结构重构 R4

- 授权：“继续r4”；计划 SHA-256：`75C4E9F0FEB637725F814C11C8688C787C64339B8C2A279327F67CD2D7D4E3BA`；范围见 [专项计划第 13 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。R4 完成，R5 尚未开始。
- 基线：`app/.local/refactor-r4/baseline/` 保存 279 个文件、哈希和 Git 状态。原 1562 行 api.ts 移至 `.local/refactor-r4/retired-api.ts`，已无同级 api.ts 与 api/ 并存。
- 实现：18 个业务请求/类型模块，62 行 `api/index.ts` 保留原 api 对象、函数和类型导出；`api/client.ts` 负责请求/ApiError，`api/events.ts` 负责事件 URL；`platform/runtime.ts` 负责握手缓存，`platform/files.ts` 负责保存和目录选择。
- TypeScript 语法打印归一化比较：181 个请求实现、95 个类型（含内部 DesktopRuntime）、7 个函数/类一致；另外核对握手缓存变量、内容类型映射、PDF 别名和 Window 声明一致。原 94 个公开类型仍由入口显式导出，页面不必更改导入。
- 代码边界：所有既有页面、样式、后端与 Rust 文件哈希与本批基线一致。仅更新了前端请求层、测试脚本/入口、两个静态契约测试的实际文件定位及开发文档。
- 验证（工作目录 app）：`npm.cmd run frontend:build` 退出码 0，1635 modules，保留已有 >500 kB bundle 提示；`npm.cmd --prefix frontend test` 为 4 passed；`python -m pytest -q tests/test_architecture.py tests/test_tauri_contract.py` 为 `13 passed in 2.10s`，退出码 0。后端实现未变，本批未重复全量后端回归或 sidecar 打包。
- 浏览器集成：新增 `tests/frontend_api_browser.mjs`，实际执行 `npm.cmd --prefix frontend run test:api` 退出码 0。通过 `PLAYWRIGHT_MODULE_PATH` 指向本机 bundled Playwright，默认无界面 Edge。自动启动隔离 Python 后端和 Vite，使用临时数据目录，结束后清理自己创建的进程与数据。
- 浏览器场景：真实初始化、错误密码与登录、方法创建/列表、PDF 响应及真实 PDF/UTF-8 CSV 下载、结构化 422、纯文本 HTTP 错误、浏览器网络失败不重试、204 登出、401 与页面会话恢复、事件地址以及浏览器目录选择提示通过。下载内容实际读取比对，无页面脚本异常。
- 桌面桥接场景：受控 invoke 验证握手一次、进程密钥、短暂网络失败重试、保存参数/字节、目录选择和保存取消、错误传递。`nativeDialogTested=false`：未实际操作 OS 文件对话框，也未声称真实桌面 GUI 验收；最终原生集成留待 R7。
- 结果和日志：`app/.local/refactor-r4/browser-final/result.json`、`contract-check.json`、`python-contracts.log`；脚本支持 `API_BROWSER_OUTPUT` 指定已归属的临时输出路径。根目录 EXE、安装包未更新。
- 导读、README、PLAN 和专项计划已同步，根目录/app 无新增散落文件。本批 `passed`；S21 整体仍为 `deferred_external`，既有外部门禁不变。下一批 R5 为页面/组件归位。


## 2026-09-14 代码结构重构 R5

- 授权：“继续r5”；执行计划 SHA-256：`04F850EDAFBB3D2F80FBEA5D4D8AF9D7B260675111308C83222D4F42E03DCCAB`。范围见 [专项计划第 14 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。R5 完成，R6 尚未开始。
- 可恢复基线在 `app/.local/refactor-r5/baseline/`，保存 302 个文件及哈希、Git 状态和原构建 CSS；原文件归入同批 retired-originals，迁移映射与日志均在 app/.local，未增加根级文件或一级目录。
- App.tsx 从 1725 行减为 82 行，仅保留认证状态和入口。跨页面状态归入 layout/WorkspaceApp.tsx；业务页面归入 pages/，共享组件归入 components/，布局归入 layout/。保留原行为，没有新增路由或状态管理库。
- 三组逐步实施并验证。原 App 的 41 个顶层定义语法归一化比较一致，另有 33 个既有组件/样式文件归位；16 个 CSS 由 styles/index.ts 按原加载顺序显式导入，最终编译 CSS 与基线逐字节一致。后端、Rust、前端 api/ 与 platform/ 哈希不变。
- 最终验证（工作目录 app）：`npm.cmd run frontend:build` 退出码 0，1661 modules，已有 >500 kB bundle 提示保留；`npm.cmd --prefix frontend test` 为 4 passed；隔离数据目录下 `python -m pytest -q tests/test_architecture.py tests/test_tauri_contract.py` 为 13 passed in 1.89s。后端实现未变化，本批未重复全量后端回归或 sidecar 打包。
- 新增 tests/frontend_pages_checks.mjs，复用隔离后端/Vite 和无界面 Edge；`npm.cmd --prefix frontend run test:pages` 退出码 0。真实初始化、错误密码、登录、三个方法视图、方法保存并刷新、清空数值不产生新版本通过；14 个可进入页面挂载、明暗主题与 1280/1920 宽度无窗口横向溢出通过。只读身份登录后方法导航隐藏，服务端写入返回 403。
- 缺少已发布当前方法时，谱图、队列、样品采集、硬件采集、分析 5 个入口继续禁用；未将入口门禁检查视为其完整数据/硬件业务验收。既有 PDF/CSV 实际下载、HTTP 错误、会话恢复及受控 Tauri 桥接检查同时通过；nativeDialogTested=false，原生对话框与最终桌面集成仍属 R7。
- 结果见 app/.local/refactor-r5/final-browser/result.json、contract-check.json、contracts.log。静态测试路径与当前导读、README、PLAN、专项计划已同步。根目录 EXE 和安装包未更新。
- 本批 passed；S21 总体仍为 deferred_external，既有硬件、签名和干净 Windows 门禁不变。下一批 R6 为数据库基线及历史迁移脚本整理。


## 2026-09-14 代码结构重构 R6

- 授权：“继续r6”；执行计划 SHA-256：`640B1E8D56BCBDC81327E1E55C23399DCE2ABD07089005DDB0DBD2DCD7DF78D5`。范围与映射见 [专项计划第 15 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。
- db.py 从 2020 行减至 104 行；基线、默认数据/触发器、SQL 辅助函数和 v11–v20 历史迁移归入 migrations/。注册顺序、基线 v10、当前 v20、事务边界、upgrade.py 副本切换行为保持。
- 原 13 个函数、注册表和展开后的 Database 类 AST 一致；固定时间生成的新旧空库完整 SQL 导出一致，268 个结构对象及默认数据保持。业务服务、API、模型、前端和 Rust 源码未改。
- 工作目录 app，隔离 SPECTRUM_DATA_DIR：重构前数据库/升级测试 12 passed；增加验证后 25 passed in 4.46s；最终 `python -m pytest -q` 为 163 passed in 50.56s，退出码 0。`python -m compileall -q app/backend app/tests`（根目录）退出码 0。
- 覆盖 v10–v20 合成检查点的剩余步骤顺序、重复初始化、方法与版本/历史迁移时间保留、JSON 帧转压缩 BLOB 与哈希、完整性/外键、后期失败后结构和历史整笔回滚并成功重试、暂存升级失败不替换原库。合成检查点不等同于历史安装包实库验证。
- `python tools/build_sidecar.py` 和 `python tools/verify_sidecar.py` 退出码 0；冻结后端初始化 v20、健康、5 CCD 模拟采集/帧推进/停止/读取资源通过。构建后端 SHA-256：`C4EA4918CA43F2FA4F864FB494D5BCE8554403CFADA8C4A96B06592BD18E30ED`。根目录 EXE、安装包未更新，未声称真实桌面或硬件验收。
- 可恢复基线 237 个文件在 app/.local/refactor-r6/baseline/，证据在同批 contract-check.json、database-comparison.json、pytest.log、sidecar-build.log、sidecar-verify.log。新文件均有正式归属，根目录/app 无新增散落文件。一次系统临时比较目录清理被策略拦截，副本保留；修正连接关闭后的比较与清理成功。
- README、导读、PLAN 和专项计划已同步。本批 passed，停在 R6；下一批 R7。S21 总状态仍 deferred_external，外部门禁不变。


## 2026-09-14 代码结构重构 R7 与最终交付

- 授权：“继续r7”；执行计划 SHA-256：`82823F09D6F27543C3A51A870FA835C3F94D3B00A43FDC20B2C9836C2733E5B3`。R0–R7 已完成，范围见 [专项计划第 16 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。本批 passed；S21 整体仍 deferred_external。
- 最终入口：main.py 44 行、db.py 104 行、App.tsx 82 行。页面/接口/模型/迁移归位完成，正式公共导出保留；原文件、搬移脚本和基线仅保留在 app/.local 各批目录，发布包不包含它们。无旧 backend/app、同级 api.ts 或当前导读中的失效 app 链接。
- R7 修正启动检查工具的临时目录和结果语义，发布清单增加桌面哈希与独立归档参数；README 和代码导读同步。未改业务/API/数据库/前端逻辑，未提交 Git。R7 基线备份 264 个源码/配置/文档及原根目录程序，位于 app/.local/refactor-r7/baseline/。
- 工作目录 app，隔离数据目录：`python -m pytest -q` 为 163 passed in 53.99s；前端单测 4 passed；`npm.cmd --prefix frontend run test:pages` 退出码 0；Rust `cargo test --manifest-path src-tauri/Cargo.toml` 3 passed；工具修改后 S21 定向 10 passed；compileall 通过。完整 OpenAPI 与 R0 基线一致（173 paths）。
- TypeScript/Vite 与 Tauri release 构建通过；已有 >500 kB 前端提示保留。R6 后端源码未变，沿用其冻结 EXE，本批重复冻结验证通过。真实 release 的 s24_frontend_regression.mjs 55 个状态、各页面/采集/分析/报告/备份主流程与关键失败路径通过，无页面脚本异常；明暗主题代表截图已查看。
- 原生系统对话框实际验证：方法页导出 30290 字节 PDF，2 页且含 S16 方法文本；保存取消/目录取消返回 null，目录选择正确，非法文件名拒绝。无进程密钥请求 HTTP 403。第二实例退出 0 且未新建后端；正常关闭窗口后桌面及两个后端均消失。工具 UIA 定位和短暂提示检查曾失败，改用刷新后的截图/实际原生返回值核验，未改产品逻辑。
- 第一次 NSIS 因文件占用失败；关闭测试程序后 `tauri bundle --ci` 成功，重复 bundle-type 标记提示已记录。7-Zip 解包 8 文件，含两个程序、NSIS 支持文件与离线 WebView2，无测试/缓存/.local；包内程序哈希与构建结果相同。解包后后端实启/模拟采集验证通过。此为包内容与可执行验证，未执行本机安装/卸载，不替代干净 Windows 验收。
- 根目录两 EXE 已备份后更新，空目录实启管理员初始化页、health=ok/schema=20，正常关闭无残留。安装包：`app/.local/releases/2026-09-14-refactor-r7/GeoSpectrum_0.1.0_refactor-r7_x64-setup.exe`。
- 哈希：桌面 `1E692D3675E81AC1EB7B6E8507886307BC19910CB9D0F2DB0E78A9E3EBA3CF35`；后端 `C4EA4918CA43F2FA4F864FB494D5BCE8554403CFADA8C4A96B06592BD18E30ED`；安装包 `91671877C38CA009159B074C4E5DCCB4175C349E0FFE7799854BEF3E51BBE488`。
- 正式归档：[发布清单](../../releases/0.1.0/2026-09-14-refactor-r7/internal-test-manifest.json)、[验证摘要](../../releases/0.1.0/2026-09-14-refactor-r7/verification.json)。详细日志和可恢复副本在 app/.local/refactor-r7/；项目根目录未新增散落文件。
- 未签名内部交付；真实转角/汞灯硬件、签名证书、干净 Windows 10/11 安装升级卸载仍未验收。结构重构到此结束，不自动扩展其他业务。


## 2026-09-14 analysis 模块补充重构

- 用户授权处理 analysis.py；计划 SHA-256：`596AE6054E9FE5830C963090336981E1773D0FAB8DB63BABE488EF655CDE25A0`。范围见 [专项计划第 17 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。本轮 passed，未延伸到其他模块。
- 原 1188 行 analysis.py 已替换为 analysis 包：service 343 行、algorithms 262、curves 256、repository 160、quality 139、printing 134；errors/serialization/__init__ 为小型共享定义与公开入口。39 个业务方法、12 个函数、错误与常量保持原逻辑，事务接收者显式组合；17 个公开方法签名与完整 OpenAPI 一致。数据库结构和其他后端源码哈希未变。
- 重构前分析/质控/后处理/报告 21 passed in 7.58s；重构后 `python -m pytest -q` 为 166 passed in 65.63s；最后整理导入/注释后四组定向 24 passed in 7.25s。compileall 通过。均使用隔离 SPECTRUM_DATA_DIR。新增三组审计失败回滚测试，核对完整数据库导出不变并成功重试，完整性与外键无错误。
- `build_sidecar.py`、`verify_sidecar.py` 和最终冻结分析脚本退出码 0。冻结 API 的登录/质控/拟合/发布/合并/预览/PDF/重复打印及缺失谱线拒绝通过；PDF 23204 字节，两次打印字节相等。没有重复前端/桌面 UI 测试，因本轮未改前端、Rust 或 API 契约。
- 根目录配套后端已更新，SHA-256：`5A1F52BA15372974C8ED556A26F8CDCA8763CA804D25C931A77E9D370517EE02`；旧版保存在 app/.local/refactor-analysis/baseline/。桌面壳与前端不变，R7 安装包仍为上一交付快照，本轮未重建安装包。
- 基线 164 个文件、搬移映射及详细日志位于 app/.local/refactor-analysis/；[长期验证摘要](../../releases/0.1.0/2026-09-14-analysis-refactor/verification.json)。当前代码导读/README/PLAN 已同步，无新根级文件或散落产物。S21 真实硬件、签名和干净 Windows 外部门禁保持。


## 2026-09-14 methods 与 legacy_migration 补充重构

- 授权：“优先级高的两个都改了吧”；执行计划 SHA-256：`F8F012259F32B9767D40C6CC70A5786E9C470EF4096438E95FE0855EF16DF2DC`。范围见 [专项计划第 18 节](../../规划与计划/CODE_STRUCTURE_REFACTOR_PLAN.md)。本轮 passed，未进入其他业务模块。
- methods.py 922 行拆为 methods 包，最大 service.py 300 行；legacy_migration.py 833 行拆为 legacy_migration 包，最大 service.py 301 行。32/17 个业务方法、15 个顶层函数/错误及全部常量保持，全部原服务签名与 OpenAPI 不变。只调整组合接收者与相对路径；完整写事务、校验顺序和原件只读策略不变。
- 基线 175 个文件与根目录旧后端已备份。重构前五组 22 passed in 19.63s；methods 首组 19 passed in 19.77s 后继续 legacy；两模块/资源/发现/谱图/结果迁移共 31 passed in 15.32s；最终 `python -m pytest -q` 为 169 passed in 54.41s，退出码 0。compileall 通过，浏览器 `test:pages` 通过。
- 新增创建/发布/打开审计失败的整库导出不变与成功重试检查，原迁移实体写入中途失败全量回滚保留。资源定位测试改查 reader.py 的真实路径；缺 Jet 模拟改替换 LegacyAccessReader 的候选来源，保持 API 可启动断言。没有放宽断言或用跳过隐藏失败。
- 真实 DIRECT.MTD/CFG/OPT 验证 3 方法、20 谱线、5 色散，BLOB/字段检查通过，源大小/mtime/哈希不变且幂等。冻结专项通过真实 HTTP 验证方法创建发布/打开暂停恢复、非法名称拒绝、读取器诊断、迁移暂存提交与重复导入；SQLite 完整性/外键通过。build_sidecar.py、verify_sidecar.py 与专项验证退出码 0。
- 根目录配套后端已更新，SHA-256：`E54D9F7E6D9AD16B57B3127E71DB79826E270F944EF28B5C4880D968085F2378`。桌面壳与前端未变，R7 安装包保留旧快照，本轮不重建。没有重做原生窗口/真实设备验收；既有外部门禁保留。
- 详细基线、脚本、AST 映射和日志在 app/.local/refactor-methods-legacy/；[长期验证摘要](../../releases/0.1.0/2026-09-14-methods-legacy-refactor/verification.json)。当前导读与 README 已同步，其他后端文件哈希不变，根目录无新增散落内容，未提交 Git。

## 2026-09-14 postprocessing 与 method_printing 结构补充验收

- 执行计划 SHA-256：`2ddb12a668bd13f4a0b2d87fa03c96b8fdf6d19896a1170d2c8231d79fcbb039`。
- 用户授权仅拆分这两个模块。postprocessing 684 行拆为 9 文件（最大 263）；method_printing 662 行拆为 5 文件（最大 262）。公开服务签名和原事务、算法、错误、输出顺序保持；其余 113 个后端基线文件哈希不变。
- 核心验收：`python -m pytest -q` 为 171 passed in 57.22s；compileall、完整 OpenAPI 对比通过。转换审计失败完整回滚、导出替换失败保留原文件并清理临时文件、打印调度失败保留输入/PDF且设置不变均通过。
- A4 纵向和 A3 横向各 2 页/44 字段，文档分页和 HTML 与原模块相同；固定 ReportLab PDF 元数据后字节相同。原始/处理后强度的 CSV、TXT、Excel 共 6 组编码结果相同。
- `npm --prefix frontend run test:pages`、`python tools/build_sidecar.py`、`python tools/verify_sidecar.py` 和冻结专项退出码 0。冻结 API 验证转换幂等、区间均值、CSV、重算值 18/36、虚拟 PDF 打印及 SQLite 完整性/外键；未进行本轮真实打印机或原生对话框验收。
- 根目录后端已留存基线后更新，SHA-256 `73756f64f12e85e7b63d7a203f3813a854b7a8dbabf967dd59d0e23a9cf4fff0`。桌面壳与前端未修改；R7 安装包未重建，仍为历史快照。此前签名、干净系统、真实硬件等外部门禁不变。
- 证据：`docs/releases/0.1.0/2026-09-14-postprinting-refactor/verification.json`；临时备份/测试脚本/日志在 `app/.local/refactor-postprinting/`。最终状态：passed，范围内工作完成。

## 2026-09-14 后端内部解耦 D1

- 授权：“根据后端解耦计划进行解耦”；本阶段为打印能力独立。执行前根计划 SHA-256 `B58BF0A1850D4441E6F4973773971C1D850D7010CD27B99EE31F2EC5E463F35D`，解耦计划 SHA-256 `CCED391AF1DC5964437FC041CB5591482F364EE6D2A81317BBDF31721E4031D4`。
- 原 method_printing/printers.py 归入 backend/printing/__init__.py，公开 `printers()` / `dispatch_pdf()`。方法打印 service 与 reports.py 改用公共适配，删除原无消费者的私有转发与常量重导出。消费者检索覆盖 backend/tests/tools；方法 API 的公开 printers 入口保留。其余后端源码与基线一致。
- 报告不再构造方法打印服务；文档、任务、虚拟输出和审计仍属于各自业务。仅移动与改名技术调用，SQL、提交顺序、默认设置、API 和渲染逻辑保持。
- 工作目录 app，设置隔离 SPECTRUM_DATA_DIR：修改前 `python -m pytest -q tests/test_method_printing.py tests/test_s19_reports.py` 为 8 passed；修改后增加 test_printing_boundary.py 的定向命令为 10 passed；`python -m pytest -q` 为 **173 passed in 50.69s**；`python -m compileall -q backend tests` 退出码 0。
- 新增报告系统调度异常测试：检查 PDF 字节/哈希/页数、失败导出元数据和审计、原报告与默认设置保持，并检查成功重试 queued。沿用方法失败 HTTP 502、报告失败 500 的既有区别。虚拟打印输出和已有方法失败边界通过。
- 基于修改前同一隔离数据库，比较完整 OpenAPI、A4 纵向/A3 横向方法文档 JSON/HTML/PDF、报告 HTML/PDF；固定 ReportLab 元数据后 **9 份产物逐字节一致**。测试断言公共打印层只有标准库/Windows 适配依赖，报告源码不依赖方法打印模块。
- 可复核比较脚本及结果见 [D1 证据](artifacts/internal-decoupling-D1/verification.json)。因源码未跟踪，实施前 backend/tests 和计划副本保留在 app/.local/internal-decoupling/D1/baseline；测试数据库/原始日志同属该隔离目录。根目录和 app 无新增散落文件。
- 本次为源码交付，未构建或替换 sidecar、桌面壳、安装包，未操作用户程序。HTTP 路径/模型和前端调用方式未变，未重复页面/原生窗口验收；真实系统打印未验收，系统调用成功/失败由受控替身验证。
- 最终状态 **passed**，D1 待用户确认；D2–D4 未开始，下一步为 Runtime 装配。S21 整体仍 deferred_external，签名、干净系统和真实硬件外部门禁不变。

## 2026-09-14 后端内部解耦 D2–D4 连续验收

- 最新授权：“连续完成D2-D4”，覆盖连续实施及阶段验证。执行前根计划 SHA-256 `8E02D2A9FD89A6D4C3640CF006A06697C32A4841EABE82AA376AB0DAC09469D9`，解耦计划 SHA-256 `FA35C1537BDA7A84DD433899303AD0634559E75C09EA39FC6F5EB87FCFAB5E0E`。各阶段完成后继续本次已获授权的后续阶段，最终统一归档。
- D2：Runtime 装配分析、方法打印、报告和后处理所选依赖；业务构造器无隐式回退。后处理通过 RecalculationAnalysis 契约复用同一个分析对象，create/start/step 顺序与错误传递通过。所有正式 Python 测试、样例脚本调用者一起迁移；设备缓存、会话和事件隔离保持。全量 **176 passed in 49.14s**。
- D3a：方法模块拥有公开快照读取，实现最新/指定/仅已发布等语义，返回独立数据对象；绑定原连接，不独立开事务。三个目标消费者不再读取方法表或使用私有快照辅助。打印两次查询、批量版本映射一次查询；分析运行装配增加一次固定身份读取，没有逐谱线读取版本。草稿限制、指定旧版本、软删除后已引用版本读取、同连接未提交版本可见、脱离连接的数据使用及哈希保持通过。全量 **178 passed in 49.97s**。
- D3b：acquisition_import.py 是采集模块的已有记录导入能力，拥有任务、样品、谱带、逐帧记录与哈希写入；不改原硬件采集服务。转换工作单元只调用一次 Database.write，转换与审计由原流程统一提交。第二个谱带失败、审计失败时整库导出相同；重试、幂等、外键和完整性通过。按 `[1,0]` CCD 顺序比较全部采集/转换/审计行和 BLOB，与搬移前相同。全量 **180 passed in 49.95s**。
- D4：PrintSettings 只描述内部值，不重复 HTTP 校验/默认值；应用层转换原 MethodPrintSettings。文档组织/分页脱离请求模型和 ReportLab；渲染仍负责字体、文件格式。LegacyIntensity 接收已解码矩阵，纯计算复用既有曲线算法；原格式/档案/矩阵校验顺序保持。固定 PDF 元数据后，OpenAPI、A4/A3 文档、HTML/PDF 与报告输出共 9 份产物逐字节一致；与旧实现比较 10 组净强度/内标/缺曲线/损坏矩阵/档案错误用例，以及 6 组原始/处理强度 TXT/CSV/Excel 输出，均相同。全量 **188 passed in 50.63s**。
- 重算事务边界专项：最终后处理审计失败时，已提交的分析运行仍为 completed，分析结果保留；后处理批次和其审计回滚。修复故障重试另建运行，原运行不被回写或删除。此为已有语义，未声称全流程原子提交。
- 工作目录 app，全部 Python 命令设置独立 SPECTRUM_DATA_DIR。阶段命令及结果：D2 的 `python -m pytest -q tests/test_runtime_isolation.py tests/test_s16_analysis.py tests/test_s18_postprocessing.py tests/test_method_printing.py tests/test_s19_reports.py` 为 30 passed；D3a 分析/曲线/后处理/方法打印 30 项与快照边界 2 项通过；D3b 后处理/采集 17 项与后处理/边界 15 项通过；D4 后处理/打印/报告 21 项与纯计算/后处理 20 项通过。每阶段 `python -m pytest -q`、`python -m compileall -q backend tests`、完整 OpenAPI 对比均成功。
- 最终 `npm.cmd --prefix frontend run test:pages` 退出码 0；使用 bundled Playwright 和无界面 Edge，隔离后端/Vite/数据库。登录、方法创建/保存、三个视图、PDF/UTF-8 CSV 下载、权限和页面挂载通过。Tauri 桥接为受控替身，不是原生对话框验收。第一次运行缺少 Playwright 路径，设置已有依赖路径后通过；没有新增依赖或改前端代码。新增快照测试最初的测试模块导入路径错误已修正，未跳过测试或放宽断言。
- [验证摘要与 28 个源码/测试/工具变更清单](artifacts/internal-decoupling-D2-D4/verification.json)、[旧计算与导出对比](artifacts/internal-decoupling-D2-D4/calculation-comparison.json)、[文档哈希](artifacts/internal-decoupling-D2-D4/document-comparison.json)、[浏览器结果](artifacts/internal-decoupling-D2-D4/browser.json)。逐阶段可恢复源码副本、比较脚本与日志在 app/.local/internal-decoupling/D2、D3a、D3b、D4；其余后端源码及数据库迁移未变。根目录/app 无新增散落产物。
- 本轮源码交付，**未构建或替换 sidecar、桌面 EXE、安装包**，未操作用户运行程序。历史可执行交付仍是此前快照，未执行冻结专项、真实打印机或原生窗口验收。当前源码通过不等于可执行分发已更新。
- D2、D3a、D3b、D4 最终状态均 **passed**，D1–D4 指定范围完成。剩余几何/身份私有辅助、迁移/报告跨表访问、重算清理分析任务和历史序列化依赖见 [解耦计划第 10 节](../../规划与计划/INTERNAL_DECOUPLING_PLAN.md)。S21 整体继续 deferred_external，原有外部发布门禁保持。

## 2026-09-14 后端小文件检查与合并

- 授权：“全面检查并合并一下可以合并的小文件”。执行前根计划 SHA-256 `E1B744DF328E942E7DDD3F5FC695AE20D295E9ABD1B94E647EFDCE1D3C7797AD`，解耦计划 SHA-256 `937CF622C9559C8B63271DB9AC1D85E3BECF31A32636CA20DF532339F6DF0F32`。
- 全部 127 个后端 Python 文件完成大小/定义扫描，60 个不超过 80 行的文件逐项核对引用、职责和合并方向。合并 2 个：postprocessing/codec.py 的两个函数收回 repository.py；conversion_uow.py 的两个类收回 conversion.py。原包级编解码导出改从仓储提供，不增加兼容文件；其余 58 个按明确职责保留，理由在 [逐文件清单](artifacts/small-file-consolidation/audit.json)。
- 仅修改 conversion.py、repository.py、postprocessing/__init__.py，删除上述两个已备份文件，其余 122 个后端文件哈希保持。合并后 125 个后端 Python 文件，转换/仓储分别 122/192 行；无业务、算法、SQL、默认值或事务顺序变化。
- 搬移的两个函数、两个事务类以及原有仓储/转换服务的完整 AST 与基线相同。工作目录 app，设置隔离 SPECTRUM_DATA_DIR；`python -m pytest -q tests/test_s18_postprocessing.py tests/test_method_snapshot_boundary.py tests/test_decoupled_calculation.py tests/test_runtime_isolation.py` 为 **27 passed in 4.08s**；`python -m pytest -q` 为 **188 passed in 50.21s**；`python -m compileall -q backend tests` 退出码 0。
- 完整 OpenAPI 与合并前相同。固定时钟/标识，在 `[1,0]` CCD 顺序下逐字段比较采集任务、样品、谱带、帧、转换批次和审计，全部行、BLOB 和哈希相同。既有谱带/审计失败整体回滚、成功重试、幂等、重算部分提交和应用隔离测试通过；未新增重复测试。
- [验证摘要](artifacts/small-file-consolidation/verification.json)、[AST 对比](artifacts/small-file-consolidation/ast-comparison.json)。可恢复源码副本、前后转换数据、OpenAPI 和比较脚本/日志在 app/.local/internal-decoupling/small-files/。当前 README、代码导读及计划已同步；历史报告不回写。根目录和 app 未增加散落文件。
- 状态 **passed**。本次源码整理未更新 EXE、sidecar 或安装包，也未改变前端调用链，未重复浏览器/冻结/真实设备验证。S21 既有外部发布门禁保持。
