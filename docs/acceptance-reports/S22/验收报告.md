# S22 验收报告

## 基本信息

- 步骤：S22 — 侧边栏信息架构与统一文件保存
- 日期：2026-08-20（Asia/Shanghai）
- 执行依据：`SIDEBAR_REORGANIZATION_PLAN.md`
- 执行时 `PLAN.md` SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`

## 目标与实际变更

- 将 18 个模块的 28 个导航入口写入 schema v2 模块清单，前端按权限构建工作台、光谱方法、分析条件、分析测试、数据处理、工具、系统管理、帮助八组。
- 实现悬停延迟、点击固定、外部点击/Esc 关闭、方向键焦点、活动项、真实硬件徽标，以及无当前方法和暂停/恢复的即时灰置/解锁。
- 方法、分析和后处理页支持入口级内部视图直达与页内切换高亮同步；子菜单改为在顶层组项下方展开，长菜单在导航区内部滚动，底部运行状态保持可见。
- 运行消息、方法 PDF、谱图 CSV/PDF、样品队列 SAM 等前端生成文件统一使用 `saveFile()`；Tauri 命令只接受叶子文件名并白名单校验 PDF/CSV/TXT/LOG/SAM、内容类型、文件头和 64 MiB 上限，浏览器保留标准下载回退。

## 核心验收

- [x] 八组默认收起；悬停、固定、互斥、外部点击、Esc、上下方向键及焦点返回通过。
- [x] 系统管理员、方法管理员、分析员、只读审计员四类视图通过；无权限入口和空组隐藏，后端 401/403 契约未放宽。
- [x] 无当前方法时入口可见灰置；设为当前后即时解锁；暂停后即时灰置并显示原因，恢复后重新解锁；页面级文件选择入口保持可达。
- [x] 方法打印设置/谱线、分析质控、后处理重算等入口直达正确视图，页内切换同步活动菜单。
- [x] 子菜单不再向右展开，统一在对应顶层组项下方展开；长菜单内部滚动且不遮挡主工作区。
- [x] `1920×1080`、`2560×1440` 和 `3840×2160` 无横向溢出；2K/4K 使用扩展工作区留白，明暗主题与舒适/紧凑密度通过，控制台无 warning/error；`1280×720` 仅保留为最低窗口安全回归，长菜单底部仍在视口内。
- [x] 浏览器实际触发运行消息 LOG 下载并提示文件名；Tauri 对话框绑定与取消分支存在，Rust 验证非法路径/类型/内容并在系统临时目录核对允许内容精确落盘。

关键截图：

- [默认收起 1920×1080](artifacts/05-default-collapsed-1920x1080.png)
- [默认收起 2560×1440](artifacts/07-default-collapsed-2560x1440.png)
- [默认收起 3840×2160](artifacts/08-default-collapsed-3840x2160.png)
- [只读审计员权限过滤 1920×1080](artifacts/09-permission-filter-read-only-auditor-1920x1080.png)

`artifacts/` 同时保留悬停、点击固定和 `1920×1080` 状态截图。

## 命令与结果

- `npm.cmd run frontend:build`：退出码 0，TypeScript 与 Vite 生产构建通过；仅有既存的 500 kB chunk 提示。
- `npm.cmd run tauri:build`：退出码 0，已重新生成包含下方展开导航的 Windows x64 内部安装包。
- `python -m pytest -p no:cacheprovider --basetemp <system-temp>`：退出码 0，`131 passed`。
- S22 最终定向回归：退出码 0，架构/API/方法/Tauri 契约 `18 passed`；保存契约复验 `13 passed`。
- `cargo test --manifest-path src-tauri/Cargo.toml`：退出码 0，`3 passed`，覆盖 loopback 限制、导出输入拒绝和精确字节写盘。
- `python tools/generate_manifest.py`：退出码 0，生成 18 模块、28 导航入口；`git diff --check` 退出码 0。
- 浏览器验收：四类角色、1080p/2K/4K 三档目标分辨率及 720p 最低窗口安全回归、两种主题/密度、内部视图、运行条件转换、浏览器 LOG 下载和控制台检查通过。

## 未解决问题与外部依赖

- S14、S15 的真实转角/汞灯协议和硬件在环证据仍缺失；菜单只显示“需真实设备”，不宣称现场通过。
- S21 的代码签名证书和干净 Windows 10/11 安装升级复验仍缺失；S22 不改变其 `deferred_external` 状态。
- Tauri 原生对话框未保存独立 GUI 截图；对话框绑定、取消返回、输入白名单及实际写盘由 Rust/契约测试覆盖，保留一次人工桌面复验的低风险项。
- 浏览器验收使用系统临时目录中的隔离 SQLite 数据库和 `s22_*` 临时账号；未修改正式运行数据库或用户账号。

## 最终状态

`passed`。S22 软件闭环及核心证据完成，等待用户明确确认；不得据此解除 S14、S15、S21 外部门禁。

## 2026-08-20 菜单内嵌视觉修订

用户复核截图后确认，子菜单改为直接插入对应顶层组项下方的深色内嵌列表：移除白色浮层卡片、重复组标题和“已固定”标签，取消子菜单独立高度裁切，导航区统一滚动并隐藏滚动条端按钮，条目说明允许自然换行。前端构建通过，架构/API 定向回归 `12 passed`，Tauri Windows x64 内部包已重新生成并更新当前用户安装及主目录启动文件；主目录版本使用临时数据库启动 8 秒未退出。浏览器自动化本轮未取得本地标签权限，未新增截图；未改变权限、API、数据库或业务流程。

随后复核发现导航区与底部间隔同时使用伸展布局，导致侧栏剩余高度被分出大段空白。现已将底部间隔改为固定 `12px`，导航区独占功能导航到服务状态之间的可用高度，底部服务卡片、当前方法和版本信息仍固定可见。前端构建、架构/API `12 passed`、Tauri 打包和主目录启动回归再次通过。

## 2026-08-21 右侧展开与中心对齐修订

用户进一步确认子菜单应在顶层菜单右侧展开。现已将 `.navigation-panel` 改为组项内的绝对定位面板：`left: calc(100% + 14px)`，`top: 50%`，并以 `translateY(-50%)` 使面板垂直中心与左侧正在展开的顶层按钮中心重合。面板使用侧栏同色背景、独立边界和阴影，长菜单改为面板内部滚动；导航区取消横向裁切，底部服务状态不再被展开内容推动。

本节修订覆盖前一节“下方内嵌”视觉状态，业务导航和文件保存行为保持不变。

本次修订执行时 `PLAN.md` SHA-256 为 `B249F1C82BBE114D5CE3D535FF5AEEC4123541662F607A9859F46C308D9EBACA`，补充计划 SHA-256 为 `39858AC75A5590F78C091FC7F5144021EC468325BEB36297CEDA229F594F7B4A`。

- `npm.cmd run frontend:build`：退出码 0。
- `python -m pytest -p no:cacheprovider tests/test_architecture.py tests/test_api.py --basetemp <system-temp>`：退出码 0，`12 passed`。
- `npm.cmd run tauri:build`：退出码 0，重新生成并安装 Windows x64 内部 NSIS 包。
- 主目录 `GeoSpectrum.exe` 与 `geospectrum-backend.exe` 已从当前安装目录同步；根目录程序使用临时 `SPECTRUM_DATA_DIR` 启动 8 秒保持响应，随后已清理进程。
- 浏览器自动化桥接仍不可用，本次未新增截图；未改变权限、API、数据库或业务流程。

## 2026-08-21 BF-01 导航与可访问性更正

- BF-01 计划 SHA-256：`92483587B355806B3421FFADD956373077E31F712919E3AA5AE394DC82DBEC65`。
- 系统管理员经模块清单获得 `about.read`，导航契约恢复 28/28；无权限角色不显示“关于与诊断”，直接访问同样受后端 403 保护。
- 软件设置的透明文件输入已从 Tab 和可访问性树移除，具名目录按钮仍保留；桌面树实测只出现目录编辑框和“选择目录”按钮，没有孤立文件输入。
- 前端构建、全量 `134 passed`、Tauri release/NSIS 构建和独立桌面错误/正确登录、管理员导航、设置键盘复验通过。
- S22 状态继续为 `passed`；S14、S15、S21 外部门禁不变。
