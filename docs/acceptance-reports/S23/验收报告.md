# S23 三页 UI 视觉精修验收报告

- 日期：2026-09-05。
- 授权：用户明确要求先完成首页、方法页、谱图页。
- 依据：`PLAN.md` 第 6、11 章及 `docs/规划与计划/UI_VISUAL_REFINEMENT_PLAN.md` 本次授权范围。
- 执行及交付计划 SHA-256：`3956E508F0E627C418F0BD0D0D8A85A26E68137408CDEA67C1DD8A389473AF89`（执行中将本次授权补入路线和第 6 章；不改变既有步骤结论）。

## 目标与实际变更

- 首页：当前方法、运行消息作为主栏；诊断统计移入右栏；缩减装饰性标题，增加辅助文字可读性。
- 方法页：统一表单、版本状态、列表、页签、数值编辑与错误样式；放大原有 7～11px 小字，方法列表元数据允许换行。窄窗口下谱线列表与编辑区、打印设置与预览按可用宽度重排，宽表局部滚动。
- 谱图页：统一工具分组、记录选中态、图例和元数据；用与 SVG viewBox 一致的比例消除固定高度形成的留白及指针映射偏差；暗色曲线和图例同步提高亮度；补充原始帧和方向控制的可访问名称，燃烧/暗帧显示中文。
- 新增 `app/frontend/src/visual-refinement.css`，以 `.refined-page` 限定三个页面，集中明暗主题、密度和控件变量；更新 `App.tsx` 与 `main.tsx` 接入。三页使用 Windows 本地字体，不依赖在线字体完成显示；既有全局字体导入未在本次范围内重构。
- 新增 `app/tests/s23_ui_regression.mjs`，复用既有 S16/S10 隔离夹具，覆盖浏览器与实际桌面。未变更业务 API、数据模型、算法、设备控制或报告/PDF 模板；未推广独立分析等其他业务页面。

## 核心验收

- [x] TypeScript/Vite 生产构建通过，既有数值输入 4 项通过。
- [x] 浏览器完成 27 个布局状态：1920×1080、1280×720、2560×1440、3840×2160 下三页浅色舒适/暗色紧凑，以及窄窗口方法谱线、打印外围、原始谱图；无全窗口横向溢出，无 pageerror。
- [x] 方法非法采集帧数保持空值、焦点返回无效字段、零 PATCH 写入。夹具中两个参考波长先在编辑态规整至界面步长精度，避免其他无效字段替代目标边界。
- [x] 谱图第三个样本点的指针坐标与实际 SVG 点位误差小于 0.1 viewBox 单位；锁线、横向缩放、拖动平移、框选、适配及浏览器可见范围 CSV 下载通过。S10 的输出及边界定向测试 6 项通过。
- [x] 复用 BF02 浏览器回归：实际分析完成、错误反馈、非法谱线零写入、合法谱线保存、方法快捷入口和备份目录读取全部通过。
- [x] 构建目录和根目录最终 EXE 各完成一次实际 WebView2 回归，均 9 个布局状态及上述方法/谱图交互通过。机器显示器为 3840×2160，实际 `devicePixelRatio=2`，桌面内容视口为 1440×920；这是真实当前 200% 缩放，不冒充其他 DPI 组合。
- [x] 代表性配色比值：浅色正文/白底 12.17:1、辅助文字/浅底 5.47:1、控件边界/白底 3.15:1；暗色对应 12.72:1、8.50:1、4.09:1。其余已有导航等颜色未宣称全站逐项认证。
- [x] 根目录 EXE 与构建 EXE 哈希一致；后端复用本次未修改且已验证的同哈希 sidecar；安装包归入 `app/.local/releases/`。关闭测试原生窗口后，桌面/sidecar 进程为 0，9235/9236 调试监听均关闭。

关键视觉证据：[首页](artifacts/after-workspace.png)、[方法页](artifacts/after-methods.png)、[谱图页](artifacts/after-spectrum.png)。目录同时保留方法谱线、初始截图、200% 实际桌面截图及布局结果 JSON；早期谱图截图含展开菜单，不作为精确像素差分基准。

## 命令与结果摘要

| 命令/检查 | 结果 |
|---|---|
| `npm.cmd run frontend:build` | 退出 0；随后 Tauri 的 beforeBuildCommand 再次构建通过 |
| `npm.cmd --prefix frontend test` | 退出 0，4 passed |
| `.local/build-venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_s10_spectrum_viewer.py --basetemp .local/tmp/s23-pytest` | 退出 0，6 passed |
| `node tests/s23_ui_regression.mjs`，`QA_ISOLATED=1` | 浏览器、构建 EXE、根目录 EXE 三次最终运行均退出 0 |
| `node tests/bf02_browser_regression.mjs` | 退出 0，F02/F03/F04/F05 回归通过 |
| `npm.cmd run tauri:build` | sidecar 业务/启动/退出验证、前端构建、Rust release 编译通过；首轮 NSIS 因测试进程占用文件退出 1 |
| `npm.cmd exec -- tauri bundle --ci` | 释放测试窗口和进程后最终退出 0，生成 NSIS |
| `.local/build-venv/Scripts/python.exe tools/build_release_manifest.py` | 退出 0，更新内部发布清单 |

复验准备：以 `tests/seed_bf02_browser.py <新隔离目录>` 初始化数据库，再调用 `tests/test_s10_spectrum_viewer.py` 的 `seed_database(<同一数据库路径>)` 加入谱图夹具。后端设置 `SPECTRUM_DATA_DIR`，浏览器前端用 `VITE_API_TARGET` 指向隔离服务。脚本通过 `PLAYWRIGHT_MODULE` 选择本机已有 Playwright，通过 `QA_URL` 或 `QA_CDP` 选择测试目标。日志、下载与临时数据仅进入 `.local/`，不使用正式账号或正式数据库。

## 桌面产物

| 产物 | 字节 | SHA-256 |
|---|---:|---|
| 根目录 `GeoSpectrum.exe` | 11756032 | `784761DDE0375329741032C4E4555BF633546F47A31E7BE67ABD8377EB74211E` |
| 根目录 `geospectrum-backend.exe`（未修改） | 24840959 | `978590C3E95381CD8C789FE86B9DF58A241EA22AB664B5D9F75A4416B2628592` |
| `app/.local/releases/GeoSpectrum_0.1.0_s23_x64-setup.exe` | 289465805 | `10810461A36B93B25DB3B80344601E8AF76C8F84372D8FD518A0020E016E4B77` |

旧根目录 EXE 保留于 `app/.local/ui-s23/previous/GeoSpectrum.exe`，其 SHA-256 为 `B0438D2D41B132A4B8239E8642A02E327B0A8873F798F04B3424485DA8DE35B4`，可配合未变化的后端回退。原用户窗口在退出前已确认停留于空谱图查看页；未编辑其业务数据。

## 未解决问题与实际限制

- F-06 固定“待连接/构建中”保持独立登记，未通过视觉隐藏或伪造正常状态处理。
- 既有谱图首次默认选择结果矩阵时，默认 `mode=mean` 可使纵轴仍显示强度，而主动重新选中结果记录后转为结果模式；此状态初始化问题未在本次纯视觉范围修改，应在后续功能修复中处理。谱图数据显示值及算法未因本次样式变更改变。
- 未逐一执行实际 Windows 125%/150% 显示切换，也未本轮重做全部角色矩阵、现场打印及真实硬件测试；当前 200% 桌面和浏览器代表尺寸证据覆盖本次三页核心风险。
- 保留既有 Vite 大 chunk、pytest 配置及 TestClient 依赖警告；重复 bundle 提示已补写 NSIS 标记的 EXE 不再含原始占位变量，最终安装包已生成，本项目未配置自动更新，不能据此声称自动更新验收通过。
- 安装包仍为 `NotSigned` 内部测试包；S14/S15/S21 的真实硬件、签名、干净 Windows 安装升级门禁未解除。发布清单工具的历史门禁集合不代表本次 S23，S23 以本报告为准。
- 已停止隔离 Vite/后端及测试桌面进程，关闭测试端口。收尾时针对明确列出的 `app/.local/tmp/s23-*` 测试目录批量清理被自动审批以“blocked by policy”拒绝，未继续递归删除；这些可再生测试数据、临时脚本及 WebView 缓存保留在已忽略目录中，不进入安装包。项目根目录及 app 根目录无本轮新增散落内容。

## 最终状态

`passed`：三页实现、核心验收及桌面交付完成，等待用户确认；不自动进入其他页面推广。

## 2026-09-05 方法页单页布局修订

- 授权：用户指出多层左侧列表把主体挤到右下角，要求先单独修改这一页看效果。本次只调整方法管理页面，属于 S23 修订。
- 执行计划 SHA-256：`D68ABEB0F4244993A4D480C078DE95DBF7DDCC3C92B42EE07329C0760031C6C8`。上文首版产物和计划摘要保留为历史记录，当前产物以下表为准。
- 实现：方法列表改为顶部原生选择器；合并页面标题、方法版本与原有操作；状态和版本摘要可展开查看。分析谱线只保留 240–280 px 的页内清单，参数区与清单顶部对齐，并可收起/展开清单。选择编辑方法仍不自动切换运行方法。
- 变更文件：`app/frontend/src/App.tsx`、`visual-refinement.css`；新增 `app/tests/s23_method_layout_regression.mjs`，同步 BF02 回归中的方法选择器。
- 验证：前端 TypeScript/Vite 构建通过；数值输入单测 `4 passed`；隔离浏览器和根目录新版 Tauri 均通过单页回归（方法创建、切换、草稿保存后读取、非法数值零写入、清单收起/展开、页面无 JS 错误）。浏览器 1060/1280/1920 宽、明暗主题及舒适/紧凑组合无全窗口横向溢出；清单与编辑区始终并排顶部对齐。1920 宽时参数区宽 1322 px，1060 宽时仍有 549 px；当前桌面正常窗口参数区宽 827 px。
- 命令：`npm.cmd --prefix app/frontend run build`、`npm.cmd --prefix app/frontend test`、`node app/tests/s23_method_layout_regression.mjs`（`QA_ISOLATED=1`，第二次以 `QA_CDP` 指向隔离桌面）、`npm.cmd run tauri:build`、`python app/tools/build_release_manifest.py` 均通过。回归脚本调试中修正了 PATCH 请求匹配、异步列表等待和数值输入焦点校验；最终浏览器与桌面全流程通过。
- 证据：`artifacts/method-layout/` 保存代表截图及浏览器、桌面结果。测试只使用 `app/.local/tmp/s23-method-layout-data/`，未修改用户方法数据。隔离测试服务已停止，程序已按正常数据入口重新启动。临时内容保留在已忽略的 `app/.local/`；项目根目录及 app 根目录无新增散落文件。

| 当前产物 | SHA-256 |
|---|---|
| 根目录 `GeoSpectrum.exe` | `C2B77FE962A32B67645CD067DB249C3036D95F7927D39EE8C6AAA57ECC570135` |
| 根目录 `geospectrum-backend.exe`（未修改） | `978590C3E95381CD8C789FE86B9DF58A241EA22AB664B5D9F75A4416B2628592` |
| `app/.local/releases/GeoSpectrum_0.1.0_s23_method-layout_x64-setup.exe` | `DAC1EFEC9BBAB13073806DE4758196E4F61274496D725626068EF1F7415C70FE` |

上一版程序备份为 `app/.local/ui-s23-method-layout/previous/GeoSpectrum.exe`，SHA-256 `784761DDE0375329741032C4E4555BF633546F47A31E7BE67ABD8377EB74211E`。保留既有 Vite 大 chunk 提示及外部门禁，本轮未执行全业务回归或真实硬件验收。

本次修订状态：`passed`，已交付桌面效果，等待用户视觉确认。
