# S07 验收报告

| 项目 | 结果 |
|---|---|
| 步骤 / 日期 | S07 / 2026-08-07（Asia/Shanghai） |
| 计划 SHA-256 | `73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11` |
| 状态 | passed |

## 目标与实际变更

S07 交付样品队列与前后命名闭环，不包含实际采集。新增 `sample-queues` 模块、SQLite `sample_queues/sample_queue_items` 表和 schema version 8；提供录入、插入、删除、清空、预览、重复展开、`.sam` 导入/导出、采集后重命名和审计 API；前端启用样品队列页面。旧 `.sam` 仍只读解析，导入以源 SHA-256 幂等，失败不写入半成品。

解析口径依据 `Spec Source/Source/Common/uMethod.pas` 的 `TSampList.LoadFromFile/SaveToFile` 与 `Spec Source/Source/Main/mmAnalyze/FmSampInput.pas` 的旧标准名规范化逻辑：制表符分隔、重复次数 0 表示空样、`S0..SF` 映射为 `S0..S15`，名称按 GB18030 10 字节上限校验。

## 核心验收清单

- [x] 普通样品队列创建、列表和 API 鉴权。
- [x] 800 条源记录展开为 960 个谱带，导出再导入记录数、展开数和 SHA-256 一致。
- [x] 旧标准名规范化、空样和重复展开正确。
- [x] 同一源 SHA-256 重复导入幂等，不创建第二个队列。
- [x] 非法行导入返回行号/原文/原因，并保持已有数据不变。
- [x] 采集后重命名保留谱图哈希并写入审计；删除/清空为独立审计动作。
- [x] React/TypeScript 前端构建通过。

## 命令与结果摘要

- `python -m pytest -q` → **38 passed**, 1 个 Starlette 弃用警告。
- `npm run frontend:build` → TypeScript 与 Vite 构建通过。
- `python tools/generate_manifest.py` → 生成 6 个模块清单，包含 `sample-queues`。

## 未解决问题 / 外部依赖

无 S07 阻断项。真实采集和谱图内容哈希由 S13/S14 负责；S07 仅保留可选 `spectrum_hash`，不伪造采集数据。

## 最终状态

`passed`

## 2026-08-21 BF-01 队列参数显示更正

- BF-01 计划 SHA-256：`92483587B355806B3421FFADD956373077E31F712919E3AA5AE394DC82DBEC65`。
- 样品采集页选择队列项后，重复次数显示队列权威值并标注“来自队列”，请求负载不再发送误导性的本地默认值；切回临时样品后恢复本地可编辑值。
- 回归覆盖队列重复次数 1、3、10 的创建参数与任务详情一致，包含在全量 `134 passed` 中。
- S07 状态继续为 `passed`。
