# S20 验收报告

计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`

## 目标与实际变更

完成 SQLite 在线备份、完整性/外键/实体计数/BLOB 抽样校验、隔离恢复演练、保留清理、WAL checkpoint、优化、受控空间回收、日志轮换/临时清理、维护权限与审计记录；新增离线帮助目录/搜索/主题详情，About 展示版本、许可证、构建和诊断信息。

## 核心验收

- [x] v20 迁移创建维护表，帮助主题覆盖导航、核心流程、错误码、兼容性和设备安全。
- [x] SQLite backup API 在活动数据库上生成副本；副本 integrity_check、外键、实体计数和 BLOB 抽样哈希通过。
- [x] 备份校验与恢复演练在隔离副本执行，失败不会替换当前数据库；备份记录和维护操作追加写入并受稳定错误码保护。
- [x] WAL checkpoint、优化、VACUUM 回收前后实体计数与 BLOB 哈希保持不变；日志/临时文件清理限制在正式运行目录。
- [x] 维护 API 受 `maintenance.read/write`、`help.read` 权限保护；模块清单和内置角色同步。
- [x] 前端新增备份维护页、帮助页，About 展示后端构建元数据；帮助主题关联路由均为已注册页面。

## 命令与结果摘要

- `...python.exe -m py_compile ...`：通过。
- `npm run build`（`app/frontend`）：通过，Vite 产物生成；仅有既有 chunk size 警告。
- `...python.exe app/tools/generate_manifest.py`：通过，生成 18 个模块。
- SQLite 临时 fixture：通过，备份、校验、恢复演练、VACUUM 回收和帮助查询闭环通过。
- `git diff --check`：通过。
- 完整 `pytest` 未执行：当前 bundled Python 未安装 `pytest`/FastAPI TestClient；已保留 `app/tests/test_s20_maintenance.py` 供项目依赖环境执行。

## 未解决问题/外部依赖

真实转角/汞灯协议和硬件仍按 S14/S15 标记为外部依赖；S20 不执行真实硬件切换。正式安装包签名和法律许可证文本待发布阶段确认。

最终状态：`passed`

## 2026-08-16 阻断修复复验

- 离线帮助扩展为 `19` 个主题，关联路由覆盖当前 `21` 个菜单/系统页面；新增稳定错误码解析入口，已知业务前缀映射到对应主题，未知但格式有效的错误码映射到通用错误主题。
- 维护页新增可配置保留天数的“清理过期日志”和“清理临时文件”操作；回归验证只删除正式日志轮转文件与数据库运行目录 `tmp` 下的过期文件，近期文件保持不变并记录维护操作。
- 备份、校验、隔离恢复、回收、帮助覆盖、错误码解析和文件清理测试通过；最终全量回归 `124 passed`，前端生产构建通过。

更正结论：帮助覆盖和维护前端入口阻断已关闭，状态继续为 `passed`。

## 2026-08-19 维护页 UI 修复

- 修复维护页缺少通用布局样式导致的标题贴边、左右卡片等高大面积空白、操作按钮拥挤和空备份列表过高问题。
- 新增紧凑标题操作区和数据库健康标识；备份、数据库整理、文件清理按组显示，保留天数与清理按钮在窄窗口可换行。
- 备份记录的“校验/恢复演练”改为操作组，避免状态列挤压；新增最近维护记录区，显示 checkpoint、优化、回收、日志/临时文件清理等已记录操作。
- 空状态改为带说明的紧凑占位，不执行任何备份、清理或数据库写操作。

## 命令与结果摘要

- `npm.cmd run build`（`app/frontend`）：通过，Vite 生产构建完成。
- `npm.cmd run tauri:build`（独立 `app/.local/target-s19-exchange` 目标）：通过，sidecar 健康探针、前端、Windows release 和 NSIS 构建均完成。
- 视觉验证：使用原验收数据目录启动新包；未自动代填认证或执行维护动作，待登录后人工确认实际数据状态。

## 2026-08-19 帮助页 UI 修复

- 修复帮助页标题区缺少布局样式、搜索框横向撑满、目录/正文内边距缺失和 Grid 等高造成的大面积空白。
- 主题目录增加数量标识、独立滚动区域、空结果状态和清晰的当前主题选中态；搜索框增加清空操作。
- 正文增加结构化标题区和关联模块展示；补齐浅色/深色主题下的文字、边框和控件对比度。
- 保持帮助查询 API、主题正文、关联模块和权限契约不变。

## 命令与结果摘要

- `python.exe -m pytest tests/test_s20_maintenance.py -q -o addopts='' ...`：`2 passed`。
- `npm.cmd run build`（`app/frontend`）：通过，Vite 生产构建完成；仅有既有 chunk size 警告。
- `git diff --check`：通过。

## 2026-08-21 BF-01 维护输入与 About 权限更正

- BF-01 计划 SHA-256：`92483587B355806B3421FFADD956373077E31F712919E3AA5AE394DC82DBEC65`。
- 维护保留天数等数值字段迁移到统一编辑/提交校验，清空期间保持空态，不再写入隐式零。
- “关于与诊断”现在由明确的 `about.read` 权限保护；内置角色升级幂等，无权限自定义角色入口隐藏且直接 API 请求为 403。
- 相关回归包含在全量 `134 passed` 中，前端生产构建和独立桌面导航复验通过；S20 状态继续为 `passed`。
