# S12 验收报告

- 日期：2026-08-11
- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`passed`

## 目标与实际变更

完成 S12 最小闭环：新增色散采集任务、燃烧/暗帧、定位谱线、不可变校准版本和方法修订绑定模型（schema 12）；复用 S11 `DeviceAdapter` 模拟器完成预激发、燃烧、暗帧、暂停/继续/停止和失败收尾；保存每 CCD 原始点、ACQ SHA-256 与虚拟时序，不修改输入帧。实现单条/全部定位、短波/长波移动、实测位置保存/恢复、像素—波长二次拟合、逐点残差、有效波段、发布阈值和方法布局校验，并提供权限、审计 API 与前端色散工作台。

## 核心验收

- [x] `draft → pre_excitation → burn → paused/resume → dark → completed` 状态、幂等控制、燃烧/暗帧数量和虚拟时序符合配置；停止/失败保留已收帧。
- [x] 每帧按选中 CCD 保存 2048 点、24,579 字节 ACQ 哈希和帧头；燃烧/暗帧顺序正确，未生成样品或谱图记录。
- [x] 谱线增删、单条/全部定位、短波/长波移动及位置保存/恢复往返通过；同 CCD `±0.01 nm` 重复谱线被拒绝。
- [x] 黄金探针恢复 `a·λ²+b·λ+c` 系数和中间波长点位；点数不足、重复波长、奇异拟合和残差超限不能发布，草稿保留系数与逐点残差。
- [x] 已发布校准版本和方法绑定不可原地更新/删除；只有已发布、CCD 布局一致的校准才能绑定已发布方法修订，权限与审计通过。
- [x] 前端支持任务条件、连续/单帧控制、燃烧/暗帧进度、实时 CCD、谱线位置操作、拟合发布和方法绑定。

## 命令与结果摘要

- `python -m pytest app/tests/test_s12_dispersion.py -q --basetemp app/.local/s12-test-run2`：`3 passed`。
- `python -m pytest app/tests -q --basetemp app/.local/s12-full-run`：`66 passed`（仅测试运行器缓存目录权限警告）。
- `npm.cmd run build`（前端 `tsc && vite build`）：通过，Vite 转换 1,589 个模块。
- `python app/tools/generate_manifest.py`：通过，生成 11 个模块。

## 未解决问题 / 外部依赖

本步只使用确定性 ACQ 模拟器验证色散状态、帧、定位和校准，不实现或猜测真实转角命令。真实串口/转角、硬件异常闭环和现场证据仍由 S14 门禁约束；不影响 S12 自动化闭环。

## 2026-08-12 更正复验

- 色散帧点阵由 JSON 改为 `uint16_le + zlib` 压缩 BLOB；同时保存点数、dtype、端序、压缩方式、未压缩点阵 SHA-256、原始 ACQ 传输 SHA-256 和原始字节长度。
- v12 迁移会把遗留 `points_json` 原子转换为压缩 BLOB，并在提交前复核解压长度和哈希；转换失败时整个迁移回滚。色散帧更新和删除继续由数据库触发器阻断。
- 对外读取在完成长度/哈希校验后解压为点数组，API 兼容字段保留；新增迁移成功、损坏遗留数据回滚及不可变性测试。
- 公开谱线枚举将定位类型改为 `positioning`、最大单点模式改为 `max_single_point`、普通坐标改为 `normal`；旧持久化别名仅在只读兼容路径映射，新写入拒绝旧枚举。方法打印标签也同步使用新值。
- 复验命令：S11-S16 定向集合 `57 passed`，后端全量 `98 passed`，前端类型检查与生产构建通过。

结论：S12 继续为 `passed`；真实转角协议和硬件证据仍由 S14 门禁约束。

## 2026-08-12 长信息完整性更正

- 色散任务名和校准系数增加完整悬停文本，采集帧 SHA-256 改为可查看并复制完整值。
- 前端类型检查、生产构建及真实浏览器复验通过；S12 继续为 `passed`。
