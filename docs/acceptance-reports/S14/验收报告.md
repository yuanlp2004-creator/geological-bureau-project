# S14 验收报告

- 计划执行快照 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`deferred_external`

## 目标与实际变更

完成真实设备与自动转角的可验证闭环入口，并把缺失的真实协议明确隔离为外部依赖：

- 新增 schema v14 的硬件任务、不可变转角步骤、原始 CCD 帧、通信追踪、异常决定和任务消息表及完整性触发器。
- 新增 `hardware-acquisition` 模块清单、权限、审计动作、版本化 API 和 React 操作页。
- 模拟器实现短波到长波、关键波段优先、重复波段拒绝、转角/采集状态机、基线强度/位置和峰位偏移检测、自动纠正、有限重试、人工接管、暂停/恢复、停止和安全状态收尾。
- 串口适配器仅提供协议闸门：缺少已确认的转角协议或现场抓包时任务进入 `deferred_external`，不发送猜测命令字节。

## 核心验收清单

- [x] 计划排序保持波长、角度、优先级和关键波段规则；重复波长（0.01 nm）和重复角度（0.001°）被拒绝。
- [x] S11 确定性模拟器契约可连接、预激发、转角、采集 CCD 帧和安全关闭；帧保留 uint16 BLOB、长度、传输哈希与点哈希。
- [x] `baseline_low`、`baseline_high`、`baseline_shift`、`peak_shift`、`frame_fault`、`turn_timeout` 脚本覆盖异常检测；峰位偏移记录自动纠正，有限重试耗尽进入安全停止。
- [x] 人工策略进入 `manual_intervention`；质量异常帧可由人工确认，物理损坏帧不可接受；未确认帧不进入结果哈希。
- [x] 暂停/恢复、用户停止和异常安全停止均保留追踪、理由、审计和安全状态；硬件追踪与原始帧核心字段不可覆盖或删除。
- [x] 串口协议缺失时无 `command` 追踪、无命令字节写出，并明确返回 `deferred_external`。

## 命令与结果摘要

- `python -m py_compile app/backend/app/modules/hardware_acquisition.py app/backend/app/main.py app/backend/app/schemas.py app/backend/app/db.py`：通过。
- `python -m pytest -q -p no:cacheprovider --basetemp=.local/test-runs/s14-final2-20260812 tests/test_s14_hardware_acquisition.py`：`12 passed`，覆盖 6 类异常与互斥控制。
- `python -m pytest -q -p no:cacheprovider --basetemp=.local/test-runs/s14-full-final2-20260812`（工作目录 `app`）：`81 passed`，仅有 pytest 配置和 Starlette 弃用警告。
- `npm.cmd exec -- tsc --noEmit`：通过。
- `npm.cmd exec -- vite build --configLoader runner`：成功，Vite 转换 1593 modules 并生成生产包。
- `python app/tools/generate_manifest.py`：生成 13 个模块，架构清单契约通过。

## 未解决问题与外部依赖

旧源码和现有资料没有完整的转角/汞灯串口协议或可复核现场抓包，当前没有真实设备、独立激发和断开安全闭环记录。因此真实硬件验收仍为 `deferred_external`，发布门禁保持阻断；收到协议文档、抓包和现场硬件记录后，必须在本步骤补做真实适配器验收，不能用 ACQ 回放替代。

应用内浏览器的已保存本地地址访问策略阻止了 `127.0.0.1:5173` 页面检查，因此未形成 S14 浏览器截图；前端以 TypeScript 静态检查和 Vite 生产构建作为本次可自动化证据。

## 2026-08-12 更正复验

- v14 现在通过连续迁移链单独执行和登记；迁移失败会整体回滚，不能再由单个 v16 标记掩盖缺失的 v14 结构。
- 内置角色权限改为精确同步：分析员保留硬件采集执行权；方法管理员仅可读取设备资料，不可连接或执行硬件采集；操作员不可执行分析/硬件任务；系统管理员保留完整权限。启动时会撤销陈旧内置授权。
- `test_s14_hardware_acquisition.py` 已纳入 S11-S16 定向 `57 passed` 和全量 `98 passed` 回归；前端类型检查和 Vite 生产构建通过。
- 本地浏览器限制已不再复现，应用可正常加载且共享工作台控制台无警告或错误；本次仍不把浏览器或模拟器结果当作真实硬件证据。

结论：S14 软件闭环通过，状态继续为 `deferred_external`。真实转角协议、抓包、现场设备和安全断开记录仍是未满足的发布门禁。

## 2026-08-12 长信息完整性更正

- 任务、最后消息、通信名称、安全状态和异常原因增加完整悬停文本；结果及通信 SHA-256 可查看并复制完整值。
- 命令响应和异常处置不再只渲染最后 8 条，全部记录进入可滚动列表；窄窗口也不再隐藏事实字段。状态继续为 `deferred_external`。
