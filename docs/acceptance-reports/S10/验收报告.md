# S10 验收报告

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`passed`

## 目标与实际变更

实现谱图查看与交互闭环。新增只读 `SpectrumViewerService` 和 `/api/v1/spectra` 查询接口，直接读取 S08 `spectrum_bands` 与 S09 `result_matrices` 的已提交原始数据，不重算、不采样、不回写；提供 CCD/谱线选择、旧版步长/波长换算、按需原始 burn/dark 帧、结果矩阵样品展开和边界错误。前端新增谱图工作台，支持均值/峰值/背景/结果曲线优先显示、适配/100%/400%/还原、缩放滚动、十字线、点定位锁定、可逆参考线偏移和当前可见范围打印。

## 核心验收

- [x] 原始谱带支持 1/6 CCD 选择，返回完整 2048 点；S10 测试覆盖 6 CCD 首屏查询，未采样且耗时小于 500ms。
- [x] 结果矩阵支持单/多样品和 DAT/PDT 行级查询，返回完整展开列；无效记录、CCD、谱线和原始帧均返回结构化错误。
- [x] CCD 累计步长、间隔点和 `TWsCof.Step2Wave` 二次换算与旧 Delphi 源码一致，测试误差小于 0.5 点/波长读数。
- [x] 视图状态（缩放、滚动、参考线偏移、十字线和锁定）只存在于前端交互状态，可随时还原；打印使用当前 SVG 可见范围并提供打印样式。
- [x] S10 模块清单、`spectra.read`/`spectra.export` 权限、前端导航和 FastAPI 路由已注册；未授权请求返回 401。

## 命令与结果摘要

- `python -m pytest app/tests/test_s10_spectrum_viewer.py --basetemp app/.local/s10-test-tmp2 -q`：`4 passed, 1 warning`。
- `python -m pytest app/tests --basetemp app/.local/s10-full-test-final -q`：`52 passed, 1 warning`。
- `node_modules/.bin/tsc.CMD --noEmit`：通过。
- `node_modules/.bin/vite.CMD build`：通过。
- `http://127.0.0.1:8787/health`：`status=ok`、`schema_version=10`；`/about` 阶段为 S10，能力清单包含 `spectrum-viewer`。
- `http://127.0.0.1:5173/health`：代理返回 `status=ok`、`schema_version=10`。

## 未解决问题 / 外部依赖

无。S10 不依赖在线采集、算法分析或真实硬件；真实设备采集仍由后续步骤负责。

## 2026-08-09 更正复验

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 补齐 1–8 原始样品叠加与优先曲线、转角筛选、燃烧曝光区间精确均值、CCD/谱线上下导航、X/Y 缩放与复位、平移/框选、谱线定位锁定、原始帧、可逆参考校正、当前可见范围精确 CSV 导出和打印审计。
- 后端谱图详情支持 1 基曝光区间并按真实 burn 帧求均值；CSV 不采样，仅输出当前 X 范围点位，响应附 SHA-256；查看、导出和打印分别写入 `spectrum.view/export/print` 审计。
- 浏览器复验覆盖 8 曲线、CCD 6、曝光 1–2、框选缩放、定位锁定、原始帧和导出；导出点击后确认新增 `spectrum.export` 审计，浏览器控制台 0 个错误。修复未定义 CSS 色值导致的激活按钮不可见问题。
- S10 定向测试：`6 passed`；全量测试：`57 passed, 1 warning`；TypeScript `--noEmit` 与 Vite 生产构建均通过。
- 最终状态：`passed`，无未关闭项。

## 2026-08-09 谱图工作台排版修正

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 修正新增叠加、定位、曝光与原始帧控件在 1280–1440px 常用桌面宽度下挤压右侧工作台的问题：该宽度自动改为记录区置顶的紧凑网格，工具栏与控制区按内容左对齐换行，卡片补齐一致内边距。
- 修正原始帧序号输入的 JSX 括号遗漏，避免开发服务器热更新/生产编译失败。
- `tsc --noEmit`：通过；`tests/test_s10_spectrum_viewer.py`：`6 passed, 1 warning`；运行中开发服务对 `App.tsx`、`SpectrumPlot.tsx` 和样式模块的转换响应均为 HTTP 200。
- 受当前会话浏览器访问策略限制，隔离复现地址无法打开；未使用替代浏览器绕过限制。请在已打开的软件中刷新“谱图查看”页确认最终视觉效果。

## 2026-08-10 数字输入交互修正

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`。
- 将 S03–S10 已实现页面的数字输入统一接入可空编辑组件；删除全部数字时不再由 `Number('')` 强制写回 `0`，可直接输入新值。
- 数字字段属于必填业务参数：若清空后直接离开输入框，会恢复最后一个有效值，避免界面显示空值但提交旧值。
- 架构回归检查确保前端只有统一组件直接声明 `type="number"`；TypeScript 检查、Vite 生产构建通过，`tests/test_architecture.py` 为 `9 passed`。浏览器实测“重复次数”从 `50` 全选删除后保持为空。
- 修正不改变各步骤既有状态，S10 继续为 `passed`。

## 2026-08-10 谱图打印修正

- 复现确认原“打印”按钮已成功写入 `spectrum.print` 审计，但仅调用 `window.print()`；内置浏览器不显示系统打印窗口，用户侧表现为点击无反应。
- 新增 `/api/v1/spectra/{record_id}/print-pdf`：服务端按当前 X/Y 可见范围、CCD/谱线、参考校正、原始帧或曝光区间和 1–8 条叠加曲线生成 A4 横向 PDF，同时记录文件 SHA-256、曲线数、可见点数和完整打印状态审计；旧 `/print` 审计接口保持兼容。
- 前端按钮改为“打印 PDF”，点击后直接下载文件并明确提示曲线数和可见点数，不再依赖浏览器原生打印弹窗。
- 代表性正式数据生成 1 页、1 条曲线、120 个可见点的 PDF；以 140 DPI 渲染检查，中文标题、坐标轴、图例和曲线无空白、裁切或重叠。
- `test_s10_spectrum_viewer.py` 与 `test_architecture.py` 合计 `15 passed`；TypeScript 检查和 Vite 生产构建通过。本地后端已重启，新接口未登录返回 `401` 而非 `404`，路由已生效。
- 最终状态：`passed`，无新增阻塞项。

## 2026-08-12 长信息完整性更正

- 谱图记录名称和布局摘要增加完整悬停文本；多样品长列表改为可展开全文；来源 SHA-256 可查看并复制完整 64 位值。
- 真实浏览器验证 488 字符样品列表可展开，原先被截断的记录元数据已有完整值入口；S10 继续为 `passed`。
