# S01 命令结果

## 后端测试与编译

```powershell
& 'C:\Users\Hust\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m pytest -q -p no:cacheprovider --basetemp app/data/pytest-tmp-final app/tests
& 'C:\Users\Hust\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m compileall -q app/backend app/tests app/tools
```

- 两条命令退出码均为 `0`。
- 测试结果：`12 passed, 1 warning in 1.22s`。
- 编译结果：`compileall ok`。
- 日志专测确认容量触发 `runtime.jsonl.1`，并确认 `token/password` 值不写入日志。
- 覆盖率：后端总计 `99%`；`services.py` `99%`、`config.py` `100%`、API 入口 `main.py` `98%`、sidecar 入口 `92%`，覆盖率命令退出码 `0`；明细见 [`coverage.json`](coverage.json)。

## 模块清单与 SQLite

```powershell
& 'C:\Users\Hust\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' app/tools/generate_manifest.py
& 'C:\Users\Hust\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' app/tools/verify_s01.py --database app/data/geospectrum.sqlite3 --manifest app/manifest.generated.json --dist app/frontend/dist
```

- 两条命令退出码均为 `0`。
- 注册 `core`、`about-diagnostics` 两个模块。
- `integrity_check=ok`、`foreign_keys=1`、`journal_mode=wal`、`schema_version=1`；必需表齐全。
- 前端资源 3 个，共 `249,105` 字节，均可计算 SHA-256。

## API 烟测

TestClient 烟测退出码 `0`：`/health`、`/about`、`/api/v1/capabilities`、`/api/v1/diagnostics`、设置读取/恢复和日志 API 均可用。详细 JSON 见 [`api-smoke.json`](api-smoke.json)。

## 前端生产构建

```powershell
npm.cmd run build
```

- 工作目录：`app/frontend/`
- 退出码：`0`
- Vite `6.4.3`，TypeScript 检查通过，1,580 个模块。
- 3 个资源共 `249,105` 字节；JS gzip `71.07 KB`，CSS gzip `4.26 KB`。

## 冷启动与退出

```powershell
& 'C:\Users\Hust\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' app/tools/measure_startup.py
```

退出码 `0`；`startup_seconds=0.989`、`health_status=ok`、`schema_version=1`、`clean_exit=true`。进程返回码 `1` 是向 uvicorn 发送 SIGTERM 后的预期终止码，脚本据 `clean_exit=true` 判定无残留。

## 原生构建与浏览器验证

- Rustup 安装成功；`cargo check --locked` 和 `cargo build --locked` 退出码均为 `0`；Tauri 构建产物和 sidecar 哈希见 [`Tauri构建诊断.txt`](Tauri构建诊断.txt)。
- `tools/build_sidecar.py` 退出码 `0`；`tools/verify_sidecar.py` 退出码 `0`，健康状态 `ok`、schema `1`、启动后可终止。
- `tools/measure_tauri_startup.py` 能启动 GUI 进程并保持运行，但自动化强制终止路径返回 Windows 中断码 `3221225786`，并观察到 sidecar 子进程残留；真实窗口退出和单实例聚焦仍待桌面事件验证。
- `npm.cmd run tauri:build` 的前端和 Rust release 构建通过，但 NSIS `3.11` 下载超时，未生成安装器。
- 浏览器客户端 `browser/26.730.61639` 初始化成功；已验证工作台、禁用入口、设置持久化/恢复默认、消息筛选/选择和关于/诊断页，并生成 [`workspace.png`](workspace.png)、[`settings.png`](settings.png)、[`about-diagnostics.png`](about-diagnostics.png)。详细记录见 [`浏览器验证说明.md`](浏览器验证说明.md)。
