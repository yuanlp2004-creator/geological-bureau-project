# S01 验收报告

## 基本信息

| 项目 | 内容 |
|---|---|
| 步骤 | S01 — 桌面壳、工作台与软件设置 |
| 执行日期 | 2026-08-05（Asia/Shanghai） |
| 计划 SHA-256 | `ECA4365069372E495D9D6C258AA052FC439987A07261600F3DB0E953D8F3ECE5` |
| 版本 | GeoSpectrum `0.1.0`；FastAPI `0.141.1`；Vite `6.4.3` |
| 最终状态 | `deferred_external` |

## 目标与实际变更

完成 `app/` 四区基础工程：FastAPI/SQLite 本地服务、模块清单与诊断 API、React 工作台/设置页、Tauri 2 壳、FastAPI sidecar 打包工具，以及对应测试和证据。未实现登录、方法、设备、采集、分析、报告和旧格式迁移；旧资料目录与 `UI测试/` 未修改。

## 核心验收

| 验收项 | 结果 | 证据 |
|---|---|---|
| 模块注册、API、设置、运行消息和 SQLite 基线 | 通过 | [`sqlite-integrity.json`](artifacts/sqlite-integrity.json)、[`api-smoke.json`](artifacts/api-smoke.json) |
| 架构约束、日志轮转与脱敏 | 通过 | [`后端测试输出.txt`](artifacts/后端测试输出.txt) |
| React 构建、六域导航、设置持久化和诊断页 | 通过 | [`浏览器验证说明.md`](artifacts/浏览器验证说明.md) |
| FastAPI 冷启动、Tauri 原生构建、sidecar 健康启停 | 通过 | [`性能数据.txt`](artifacts/性能数据.txt)、[`Tauri构建诊断.txt`](artifacts/Tauri构建诊断.txt) |
| Tauri GUI 退出/单实例无残留、NSIS 安装包 | `deferred_external` | [`Tauri构建诊断.txt`](artifacts/Tauri构建诊断.txt) |

## 命令摘要

- `python -m pytest -q`：退出码 `0`，`12 passed, 1 warning`；覆盖率退出码 `0`，后端总计 `99%`。
- `python -m compileall -q app/backend app/tests app/tools`：退出码 `0`。
- `python tools/verify_s01.py`：退出码 `0`；SQLite `integrity_check=ok`、WAL、外键启用。
- `npm run frontend:build`、`cargo check/build --locked`、`python tools/verify_sidecar.py`：退出码均为 `0`。
- `python tools/measure_startup.py`：`0.989s`，健康状态 `ok`。

## 未解决问题与外部依赖

自动化强制终止 GUI 时观察到 sidecar 残留，真实窗口退出和单实例聚焦尚未复验；NSIS 工具下载超时，未生成安装包。已完成范围不受影响，但发布门禁保持阻塞。

## 结论

S01 的后端、SQLite、API、React、浏览器基础交互、原生构建和 sidecar 已完成；GUI 生命周期与安装包仍待桌面发布环境复验，因此状态为 `deferred_external`。

## 2026-08-10 设置生效链路修正

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`。
- 修正显示设置只保存、不应用的问题：主题、界面密度、侧栏运行状态和时区现在保存后立即应用，重新进入设置页仍保持。
- 日志级别现在会过滤写入文件的低级别日志；日志大小和保留天数仍沿用既有运行时配置。
- 打印纸张与打印预览继续由方法打印服务读取；默认目录明确为桌面文件操作默认位置，浏览器开发模式不能强制改变浏览器自身的下载目录。
- 验证结果：前端 TypeScript 检查、Vite 生产构建、Python 编译均通过；`app/tests/test_architecture.py app/tests/test_api.py` 共 `9 passed`；浏览器实测深色/紧凑/UTC/隐藏状态均生效，随后恢复为浅色/舒适/Asia/Shanghai/显示状态并复核持久化。
- 本次修正不改变既有外部依赖结论，最终状态仍为 `deferred_external`。

## 2026-08-12 长信息完整性更正

- 工作台运行消息的正文、类别和关联 ID 保留紧凑单行展示，同时提供完整悬停文本，长消息不再只有不可恢复的省略号。
- 前端类型检查、生产构建及真实浏览器全页面信息完整性扫描通过；S01 状态继续为 `deferred_external`。
