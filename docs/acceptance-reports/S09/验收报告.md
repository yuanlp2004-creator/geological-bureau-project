# S09 验收报告

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`passed`

## 目标与实际变更

实现 `.dat/.pdt` 只读结果迁移闭环。解析器严格校验 Delphi 小端布局、文件头、短字符串、样品/谱线数量、重复展开、曝光段、Peak/Back 或单值矩阵、截断和尾随字节；保留源路径、大小、UTC 修改时间、SHA-256、编码、解析器版本和原始矩阵 BLOB。S09 新增独立暂存/正式表、单文件原子提交、源哈希幂等、方法 ID 匹配与孤儿结果告警，并接入权限、审计、模块清单、后端 API 和前端迁移页。schema 版本为 10。

## 核心验收

- [x] 扫描仓库 `Spec Source/Bin/DATA` 中全部 7 个 `.dat/.pdt` 样本均可暂存解析，未发现未知头或布局截断。
- [x] 真实 `.dat` 样本解析为 90 个样品、3 条谱线、90 列结果矩阵。
- [x] 真实 `0x0A73 .pdt` 样本解析为 100 个样品、10 条谱线、120 个展开波段，保留逐线曝光段、波长、Back、Digits 与 Peak/Back 矩阵。
- [x] `.pdt` 的 legacy method id `24` 在无匹配方法时以 warning-only orphan 保留，不猜测方法。
- [x] 未知文件头和截断矩阵拒绝整文件，数据库不写入正式结果。
- [x] 同一源 SHA-256 再次导入返回 `already_committed=true`，不产生重复矩阵。
- [x] staging 与 commit 使用 SQLite 单事务，提交失败更新失败状态并回滚。

## 命令与结果摘要

- `python -m pytest -q -o addopts='' --basetemp=.local/s09-test3-tmp -o cache_dir=.local/s09-test3-cache tests/test_s09_result_migration.py`：`4 passed`。
- `python -m pytest -q -o addopts='' --basetemp=.local/s09-full-test5-tmp -o cache_dir=.local/s09-full-test5-cache`：`48 passed, 1 warning`。
- `node_modules/.bin/tsc.CMD --noEmit`：通过。
- `node_modules/.bin/vite.CMD build`：通过，生成前端 dist。
- FastAPI TestClient：`/health` 返回 `schema_version=10`，`/about` 返回 S09，能力清单包含 `result-migration`；结果诊断端点在未登录时按权限返回 `401`。
- 运行态：已重启 `127.0.0.1:8787` 后端，`/health` 返回 `schema_version=10`；`127.0.0.1:5173` 前端代理 `/health` 返回 `schema_version=10`。

## 未解决问题/外部依赖

无。真实 `.pdt/.dat` 样本来自旧版 Bin/DATA，未修改原件；S09 不依赖 Jet/Access 运行时。

## 2026-08-10 验收修正

- 修正暂存阶段 `atomic_commit=false` 被界面误标为“失败”的状态语义：暂存阶段显示蓝色“待提交”，提交成功显示“通过”，只有实际提交失败或回滚才显示“失败”；新暂存报告使用 `null` 明确表达尚未执行。
- 正式运行库中的 DAT、PDT 两次结果迁移均为 `committed`，报告阶段为 `committed`、`atomic_commit=true` 且无错误；本次未改动迁移原件或正式结果数据。
- `test_method_printing.py`、`test_s09_result_migration.py`、`test_architecture.py` 合计 `18 passed`；前端 TypeScript 检查和 Vite 生产构建通过。

## 2026-08-12 长信息完整性更正

- 结果迁移的源文件名、完整 SHA-256、迁移指纹和矩阵摘要均保留完整显示入口，避免长文件名与样品名被吞没。
- 前端类型检查和生产构建通过；S09 继续为 `passed`。
