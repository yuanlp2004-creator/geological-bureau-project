# S17 验收报告

- 计划执行快照 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 执行日期：2026-08-13（Asia/Shanghai）
- 状态：`passed`（2026-08-18 人工验收数据复验已确认通过）

## 目标与实际变更

完成重复测量质控、标准曲线编辑与发布、曲线绑定结果和合并保存的最小闭环：

- schema 升级到 v17，新增不可变质控决定/快照、曲线操作/修正集/拟合快照、活动曲线指针、曲线绑定结果、合并快照和打印记录；历史分析结果永久引用确切曲线快照，数据库触发器禁止覆盖或删除受保护记录。
- 重复统计实现均值、极差、样本标准差、RSD、ID 和有效次数；支持提示接受、单次重复剔除/恢复和重新统计，所有人工决定均保留操作者、理由与新快照。
- 实现直线、二次、三次和自然样条四种拟合，以及普通/对数坐标；校验最少标准点、重复 X、对数非正值和病态方程，失败时不发布且保留此前有效活动曲线。
- 标准点原始强度只读；启停、手工修正、单点恢复和全部恢复产生新的修正集。发布生成新的不可变曲线快照，并据此写入可重放的样品结果。
- 增加原始结果、重复质控、曲线图、标准点和样品结果五视图，按分析运行保留视图和谱线选择；支持结果合并保存、曲线图像/文本 HTML 预览及确定性 PDF 打印。
- 注册 S17 API、`analysis.quality`、`analysis.curve`、`analysis.print` 权限、审计动作和模块能力；分析员可执行，审计员保持只读。

## 核心验收清单

- [x] 零次、单次、阈值附近、全部剔除和离群值统计边界通过；RSD 与 ID 的零值/符号边界返回稳定结果。
- [x] 直线、二次、三次、自然样条在普通坐标和对数坐标黄金向量上通过；最少点数、重复 X、非正对数和病态方程均被拒绝。
- [x] 质控提示接受、剔除、恢复和重算产生不可变快照并记录理由；标准点启停、修正、单点/全部恢复均可重放和审计。
- [x] 不可用拟合不能发布；拟合失败不改变先前活动曲线；数据库禁止更新或删除已引用曲线快照。
- [x] 样品结果永久携带曲线快照 ID；合并保存保留来源谱线、结果和 SHA-256，重复读取保持一致。
- [x] 图像/文本预览字段一致，图像/文本 PDF 重放字节与 SHA-256 一致，打印输入与结果均有审计记录。
- [x] 五视图、谱线导航、曲线图、标准点输入和样品合并历史在本地浏览器中可用；浏览器验收发现并修复了恢复原始值后输入缓存未同步的问题。
- [x] API 鉴权、内置角色权限、模块清单和 schema 0→17 / 16→17 顺序迁移通过。

## 命令与结果摘要

- `python -m pytest -q`（工作目录 `app`）：`103 passed`；仅有 Starlette/httpx 弃用警告。
- `python -m pytest -q tests/test_s17_quality_curves.py tests/test_s16_analysis.py tests/test_api.py tests/test_auth.py tests/test_schema_migrations.py`：`18 passed`，覆盖 S17 黄金/失败边界、S16 回归、API、权限和迁移。
- `npm run build`（工作目录 `app/frontend`）：TypeScript 与 Vite 生产构建成功，转换 1600 modules。
- `python tools/generate_manifest.py`（工作目录 `app`）：生成 15 个模块，S17 权限、审计与能力清单同步。
- 本地同源 FastAPI + 生产前端浏览器验收：登录、五视图、重复值剔除/恢复、曲线图、文本预览、标准强度保存/恢复和样品合并历史通过；临时服务及标签页已关闭。

## 未解决问题与外部依赖

- 没有阻断 S17 的核心缺陷或外部依赖。S17 不采集新的标准样品，不修改原始强度，不进行跨批次自动合并，也不提供报告模板。
- 现有 Starlette/httpx 弃用警告不影响本步结果，后续依赖升级时处理。
- S18 的全时区间转换、重算和矩阵导出未提前实现；需获得明确确认后才能进入下一步。

结论：S17 最小闭环通过，状态为 `passed`；等待用户确认后再进入 S18。

## 2026-08-16 阻断修复复验

- 修正标准样品映射：`S1` 现在精确对应方法标准点 `S1`，不再按数值产生一位偏移；标准样品只按大小写无关的完整名称匹配，`S0` 不再被接受。
- 新增曲线快照公开校验入口，供后处理验证所选快照与目标方法版本、计算口径、谱线及可发布状态完全一致。
- S17–S21 定向回归共 `25 passed`，最终全量回归 `124 passed`；仅保留既有 Starlette TestClient 上游弃用警告。

更正结论：S17 映射阻断已关闭，状态继续为 `passed`。

## 2026-08-18 人工验收数据阻断更正

- 人工验收确认 S13 模拟采集此前对每帧重复使用同一 ACQ 数据；`seed` 只记录在事件详情中，没有改变谱点。燃烧帧与暗帧相同后，旧版平均口径把现有 S17 输入统一钳制为 `float32(0.1)`。
- 当前库中的 S1-S4 还被保存为 `normal`，不是方法标准点所需的 `standard`；现有 S17 分析运行、质控/曲线快照和合并结果均不得作为本次人工验收证据，记录本身保留用于审计，不删除或覆盖。
- S13 现采用 `seeded-acq-v1`：同一种子可跨任务重放，不同种子、重复序号、燃烧/暗帧会生成不同谱点；事件同时保留源 ACQ SHA-256 和生成谱点 SHA-256。
- 定向回归 `python -m pytest tests/test_s11_devices.py tests/test_s13_acquisition.py tests/test_s16_analysis.py tests/test_s17_quality_curves.py -q` 为 `21 passed`；新增端到端用例验证 S1-S4 标准强度互异、QA-1 三次重复互异且能进入 S17 标准点和重复质控。

更正结论：软件缺陷已修复，但既有验收数据不会被追溯改写。S17 暂改为 `in_progress`，须按 `standard` 类型重新采集 S1-S4（种子 101-104）和 QA-1 三次重复（种子 105），再完整复验五个页签、快照、PDF 与合并保存。

## 2026-08-18 人工验收数据复验确认

- 用户明确确认 S17 及其人工验收已经完成并通过；本次确认关闭上述重新采集与五页签人工复验待办。
- 既有错误输入及派生记录继续保留用于审计，不作为本次通过证据，也不追溯覆盖。

最终结论：S17 状态恢复为 `passed`，允许发布门禁按最新验收索引继续执行。
