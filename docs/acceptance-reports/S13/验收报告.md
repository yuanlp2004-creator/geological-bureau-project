# S13 验收报告

- 计划执行快照 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`passed`

## 目标与实际变更

完成蒸发与样品采集闭环（模拟器）：全帧/暗帧采集、CCD 原始帧留存、时间轴与帧间隔标记、峰值/积分曲线分析、平均值和完整区间保存、重复采集、预热、倒计时、暂停/恢复/停止、损坏帧留痕、S07 样品队列绑定及采集后改名。新增 S13 数据表、服务、版本化 API、权限/审计、前端样品采集页和模块清单注册。

真实串口协议、转角异常处理和旧 `.edt` 转换不在 S13 范围内，按计划留给后续步骤。

## 核心验收清单

- [x] 原始帧按 CCD 顺序、`uint16` 小端保存，帧长度和 SHA-256 可核验；预激发不计入正式燃烧帧。
- [x] 平均值按旧版口径计算：燃烧均值减暗帧均值，负值钳制为 0，结果为 `float32_le`；黄金样本通过。
- [x] 完整区间、区间标记、点均值、峰值、积分和曲线查询可用，曲线 API 返回可 JSON 化的点数组。
- [x] 空白/普通/标准/重复/预热及采集后命名路径可执行；队列绑定结果哈希与任务结果一致。
- [x] 暂停/恢复不重复或丢失帧；未完成重复不会发布结果。
- [x] 损坏帧标记并保留，不以替代帧覆盖；单任务失败不污染下一队列项。
- [x] 改名只更新名称，已保存 BLOB 的 SHA-256 保持不变。

## 命令与结果摘要

- `python -m py_compile ...`：后端、S13 测试和前端相关入口通过。
- `pytest app/tests/test_s13_acquisition.py -q`：`3 passed`。
- `pytest app/tests -q -p no:cacheprovider --basetemp app/.local/s13-full-run`：`69 passed`。
- `npm.cmd exec -- tsc --noEmit`：通过。
- `npm.cmd exec -- vite build --configLoader runner`：成功，Vite 转换 1591 modules，生成生产包。
- `python app/tools/generate_manifest.py`：生成 12 个模块，架构清单契约测试通过。

## 未解决问题与外部依赖

默认 `npm run build` 的 Vite 配置打包器在当前受限 Windows 沙箱中读取父级目录时被拒绝；使用 Vite `--configLoader runner` 完成了同等生产构建验证。真实仪器串口/转角协议与硬件验收依赖后续步骤和外部设备。

## 2026-08-12 更正复验

- 更正原报告中“负值钳制为 0”的表述和实现：旧版平均谱口径为 `Single((burn_sum / burn_count) - dark)`，下限使用 `LowAvg = 0.1`，不是 0；下限和最终结果均按 IEEE-754 `float32` 固化。
- 新增低于下限黄金断言，精确验证输出 `0.10000000149011612`；既有正常值、暂停恢复、损坏帧、队列绑定和改名哈希测试继续通过。
- 内置角色精确同步后，分析员可执行样品采集，方法管理员和操作员不能越权执行；陈旧内置授权会被撤销并记录审计。
- 复验命令：S13 及 S11-S16 定向集合包含在 `57 passed` 中；后端全量 `98 passed`；前端类型检查和 Vite 生产构建通过。

结论：S13 继续为 `passed`。真实硬件外部依赖不变。

## 2026-08-12 长信息完整性更正

- 任务名、当前阶段消息和消息代码增加完整值入口；采集帧 SHA-256 可复制完整值。
- 采集消息不再只渲染最后 8 条，任务载荷中的全部消息按倒序进入可滚动列表；S13 继续为 `passed`。

## 2026-08-13 UI 暂停恢复与方法绑定更正

- 样品采集页在任务进入“已暂停”后，将“暂停”操作切换为明确的“继续”按钮，调用已有审计化恢复 API。
- 新建样品摄谱任务必须选择已发布方法版本；页面提交 `method_id + method_version`，后端解析并固化不可变 `method_version_id`，完成样品可被 S16 分析选项枚举。
- 回归：`pytest app/tests/test_s13_acquisition.py -q` 为 `4 passed`；前端 `npm.cmd run build` 通过；只读 UI 复验确认方法版本选择器、暂停按钮和已绑定版本显示入口可用。S13 继续为 `passed`。

## 2026-08-18 模拟采集种子更正

- 修复模拟器在样品采集任务中反复使用同一 ACQ 帧、种子不影响谱点的问题。原始 S11 ACQ 解析和字节重放保持不变；S13 在任务层按种子、重复序号、帧序号和燃烧/暗帧阶段生成确定性变化。
- 同一种子可重复得到相同结果，不同种子和不同重复产生不同平均强度；燃烧帧与暗帧不再相同，事件保留源 ACQ 与生成谱点的 SHA-256。
- S11/S13/S16/S17 定向回归为 `21 passed`。S13 软件闭环继续为 `passed`；由旧模拟数据派生的 S17 人工验收记录需要重新采集和复验。

## 2026-08-21 BF-01 采样与数值输入更正

- BF-01 计划 SHA-256：`92483587B355806B3421FFADD956373077E31F712919E3AA5AE394DC82DBEC65`。
- 采样周期前后端统一为 `0.01–60 s`、`step=0.01`，默认 `1 s` 合法；零、负数、越界和非步进值由 API 明确拒绝。
- 样品采集、硬件采集、色散、分析、汞灯和后处理等可编辑数字框迁移到统一字符串编辑态，空值不再自动变零。
- 队列 1/3/10 次权威值、默认/最小/最大采样周期和非法步进回归通过；全量为 `134 passed`，前端数值组件 `4 passed`、生产构建通过。
- S13 软件闭环继续为 `passed`，真实硬件外部依赖不变。
