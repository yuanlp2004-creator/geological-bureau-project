# S11 验收报告

- 计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 状态：`passed`

## 目标与实际变更

完成 S11 最小闭环：新增 `DeviceAdapter` 契约、`DeviceEvent`/`AcquisitionState`、旧 ACQ little-endian 帧解析和确定性模拟器；按 3 组 × 2 CCD × 2048 点校验 24,579 字节帧，支持旧版非镜像/镜像 CCD 映射、峰值与位置；新增 SQLite `device_profiles`（schema 11）、串口/波特率、CCD 布局、保护时间和屏幕换算；提供连接诊断、实时调试开始/单步/停止/故障 API 与前端曲线工作台。调试会话仅驻留内存，审计所有控制动作，明确返回 0 样品/0 谱图记录。

## 核心验收

- [x] `Spec Source/Res` 与 `Spec Source/Bin/DATA` 的六份 `.acq` 资料副本长度、帧头、SHA-256、CCD 顺序和首点映射符合黄金值。
- [x] 不完整帧和非零帧头拒绝，并返回偏移、帧序号和原始头信息。
- [x] 设备档案边界、重复/越界 CCD、屏幕分辨率/宽度换算通过；连接失败不伪造成功状态。
- [x] 调试状态可连接、启动、单步、故障、停止；审计含关联 ID；样品队列和谱图表记录保持为 0。
- [x] 前端支持档案选择、连接诊断、CCD 切换、峰值/位置、十字线、横纵缩放、还原和停止状态。

## 命令与结果摘要

- `python -m pytest app/tests/test_s11_devices.py -q --basetemp app/.local/s11-test-tmp2`：`4 passed`。
- `python -m pytest app/tests -q --basetemp app/.local/s11-full-tmp3`：`63 passed`（仅测试运行器缓存目录权限警告）。
- `node_modules\\.bin\\tsc.CMD --noEmit`：通过。
- `npm.cmd run build`（前端 `tsc && vite build`）：通过，Vite 生产包生成成功。
- `python app/tools/generate_manifest.py`：清单生成成功，10 个模块。

## 未解决问题 / 外部依赖

真实串口、转角、电弧和汞灯协议仍按计划留给 S14/S15；本步骤不把模拟器结果作为硬件在环证据。

## 2026-08-12 更正复验

- 数据库初始化改为在同一事务内依次执行并登记 v11-v16 迁移；补充 v10 遗留库升级、失败回滚和版本连续性测试，不再用单个 v16 标记代表中间迁移。
- 调试轮询增加请求互斥，避免慢请求产生重叠单步；十字线开关现在同时控制显示和图表点击行为。确定性耐久测试连续处理 10,000 帧，覆盖超过 30 分钟虚拟设备时间，验证帧序严格递增、停止收尾和内存上界。
- 工作台模块数和 schema 版本改由运行时诊断/能力清单提供，不再硬编码。真实浏览器复验显示 15 个模块、SQLite schema v16；十字线关闭后点击曲线不再出现十字线，停止后仍为 `0 样品 · 0 谱图`，控制台无警告或错误。
- 内置角色改为精确权限同步：启动时撤销陈旧内置授权并补齐缺失授权；方法管理员、分析员、操作员按职责隔离，系统管理员保留完整权限。
- 复验命令：后端 S11-S16 定向集合 `57 passed`；后端全量 `98 passed`；前端 `tsc --noEmit` 与 Vite 生产构建均通过。

结论：S11 继续为 `passed`。真实设备证据仍按后续步骤门禁处理，不影响本步模拟调试闭环。

## 2026-08-12 实时曲线十字线更正

- 将仅在点击后显示的竖线改为随鼠标悬停更新的二维十字线：吸附最近 CCD 数据点，同时显示横线、竖线、交点以及 CCD、点位和强度读数；鼠标离开或关闭十字线时全部隐藏。
- 曲线抽稀后的 X 坐标继续使用原始点位索引，确保路径、十字线和读数在 2,048 点数据上保持一致；CCD 切换时清除旧悬停点，避免跨 CCD 显示陈旧读数。
- 真实浏览器复验同时检测到横线、竖线、读数；鼠标从点 715 移至点 1607 时强度随之由 1,389 更新为 570；关闭/开启十字线后对应元素分别为 0/1。
- 定向后端回归 `tests/test_auth.py tests/test_s11_devices.py`：`10 passed`；前端 `tsc --noEmit` 与 Vite 生产构建通过。

结论：S11 继续为 `passed`。

## 2026-08-12 实时状态卡自适应更正

- 将曲线底部的固定四列状态卡改为按可用宽度自动换列；空间不足时采用两列或一列，数值允许安全换行，不再用省略号隐藏峰值、哈希片段或记录写入结果。
- 为帧序、峰值/位置、帧哈希和记录写入增加完整内容悬停提示；界面仍只展示 16 位哈希摘要，悬停可查看完整 64 位哈希。
- 真实浏览器在约 346px 状态区宽度下复验为 2×2 布局、单卡约 168px；四项数值均满足 `scrollWidth == clientWidth`，“0 样品 · 0 谱图”完整可见。
- 前端 `tsc --noEmit` 与 Vite 生产构建通过。

结论：S11 继续为 `passed`。
