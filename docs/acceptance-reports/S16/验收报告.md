# S16 验收报告

- 计划执行快照 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`
- 执行日期：2026-08-12（Asia/Shanghai）
- 状态：`passed`

## 目标与实际变更

完成基于固化采集谱带的定量分析和逐谱线慢进干预闭环：

- 新增 schema v16 的分析运行、样品输入快照、逐谱线结果、慢进检查点、人工干预和消息表；分析运行永久引用方法版本、计算档案、输入清单及 SHA-256，谱线结果和干预记录不可修改或删除。
- 实现参考线校正、定位线偏移、扫描寻峰、最大单点、高斯峰、7 点背景、无内标/背景内标/普通内标线及单/多样品结果矩阵；任何谱线失败均保留稳定错误码和中间量，不写零值占位。
- `legacy_2_0_2` 保存并以旧版峰高参与定量；`modern_v1` 同时保存中心、峰高、Sigma、面积并以面积参与定量。旧迁移方法显式或按 `legacy_specdirect_202` 兼容标记选择旧档案，新方法默认现代档案。
- 慢进模式每条谱线生成唯一待确认检查点，显示峰位周边谱图；接受调整必须记录调整前后值和理由，放弃调整采用自动定位。未确认检查点不写结果，接受/放弃后从完整检查点继续；支持取消和超时失败。
- 注册 `analysis` 模块、三级权限、版本化 API 和 React 分析页，支持批次浏览、样品选择、档案选择、逐步执行、慢进干预及单/多样品矩阵查看。

## 核心验收清单

- [x] S00 旧版高斯 3/5/7 点、偶数点和非正值黄金向量逐项通过；通用加权二次最小二乘独立实现复算中心、峰高和 Sigma，结果一致。
- [x] 旧版最大值首个同值点、三角寻峰和无峰回退边界通过黄金向量；参考线与定位线校正按顺序应用，不重复叠加偏移。
- [x] 无内标、背景内标和普通内标线均产生可复核中间量；现代高斯保存中心/峰高/Sigma/面积并以面积定量，旧档案保存 float32 峰高并以峰高定量。
- [x] 两个样品的行列和谱线顺序稳定；同输入、方法版本和档案重放得到相同输入及结果 SHA-256。
- [x] 慢进暂停→放弃自动结果→人工调整→继续→最终矩阵写回通过；审计保存调整前后位置及理由。
- [x] 未确认取消不写结果；超时进入 `analysis_intervention_timeout`；高斯失败进入 `analysis_gaussian_fit_failed` 并返回输入窗口，不以零代替。
- [x] API 鉴权、模块权限和生成清单通过；分析结果及干预记录数据库不可变约束通过。

## 命令与结果摘要

- `python -m pytest tests/test_s16_analysis.py -q ...`（工作目录 `app`）：`6 passed`，覆盖黄金样本、独立交叉验证、双档案、三种内标、单/多样品、重放、慢进、取消、超时、失败和 API 权限。
- `python -m pytest -q ...`（工作目录 `app`）：`94 passed`；仅有 Starlette/httpx 弃用警告。
- `python -m py_compile ...`：通过。
- `node frontend/node_modules/typescript/bin/tsc -p frontend/tsconfig.json`：通过。
- `node node_modules/vite/bin/vite.js build --configLoader runner`（工作目录 `app/frontend`）：成功，转换 1597 modules 并生成生产包。
- `python tools/generate_manifest.py`（工作目录 `app`）：生成 15 个模块，架构清单契约通过。

## 未解决问题与外部依赖

- 没有阻断 S16 的核心缺陷或外部依赖。旧 EXE 同输入输出尚未取得，但本步已按计划允许的替代路径完成独立算法交叉验证，因此不阻断 S16。
- S17 的重复性质控、标准曲线拟合/编辑、不可变曲线快照和结果合并未提前实现；S18 的全时区间转换、重算和矩阵导出也不在本步范围。
- 现有 Starlette/httpx 弃用警告不影响本步结果，后续依赖升级时处理。

## 2026-08-12 更正复验

- 旧版 `legacy_2_0_2` 计算链补齐逐步 `Single` 语义：峰值、7 点背景、净强度、内标除法和 LowAvg 下限均在对应步骤固化为 IEEE-754 `float32`；现代档案仍使用现代精度链。
- 黄金测试不再只检查显示峰值，而是精确断言峰值、净强度和定量结果；背景内标及普通内标路径继续通过。
- 公开谱线枚举将定位类型改为 `positioning`、最大单点模式改为 `max_single_point`、普通坐标改为 `normal`，并同步 API schema、迁移映射、前端、方法打印和测试；旧持久化值只读映射，新写入旧值返回校验错误。
- 分析执行权限收紧到系统管理员和分析员；方法管理员、操作员不可执行，内置角色采用撤销陈旧授权的精确同步。
- 复验命令：S11-S16 定向集合 `57 passed`；后端全量 `98 passed`（仅测试配置/Starlette 弃用警告）；前端 `tsc --noEmit` 和 Vite 生产构建通过。

结论：S16 继续为 `passed`，未进入或提前实现 S17。

## 2026-08-12 阶段标签更正

- 修正 `/about` 遗留的 S15 阶段硬编码；“关于与诊断”现在显示 `S16 · 定量分析与慢进干预`，并增加 API 精确契约断言防止回退。
