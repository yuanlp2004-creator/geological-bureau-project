# 中高优先级缺陷修复复验（BF02）

- 日期：2026-09-05；范围：用户授权修复全功能测试报告 F-01～F-05。
- PLAN.md SHA-256：`91AC0678F4A18A06C23AC08E396D2B84C135EE185234829D56F7333236011BD7`。
- 当前结论：源码、后端、Rust、浏览器和新桌面 EXE 回归通过；用户授权后已恢复构建工具、生成新版 Windows EXE/离线 NSIS 安装包并同步根目录。仍为未签名内部测试包，外部发布门禁不变。下文首轮构建阻塞保留为历史记录。

## 修复与证据

| 编号 | 修改 | 复验 |
|---|---|---|
| F-01 高 | 退出接口显式返回无正文的 `Response(status_code=204)`，避免 FastAPI 根据 `None` 注解推导响应模型 | 普通 Python 导入通过；新增测试确认 204 空正文、退出后会话失效；完整 pytest 136 项通过 |
| F-02 高 | 谱线保存先校验当前编辑器的数值输入，有错误则聚焦并提示，不发出保存请求 | 浏览器输入 9999 后请求数为 0，输入仍为 9999；改为合法 254 后成功保存且只有 1 次写请求 |
| F-03 高 | 建立分析运行增加显式数值校验、表单内持久错误、处理中反馈及重复提交保护；禁用浏览器隐式校验，由提交处理器统一给出提示 | 空选择、空超时均被拒绝；模拟后端 `analysis_lines_missing` 显示持久错误；真实后端完成建立→开始→逐线推进→completed |
| F-04 中 | 通用“方法管理”导航明确选择 `lifecycle` 视图，不再按排序取第一个 methods 入口 | 全新数据库无当前方法时快捷入口可进入方法库；选择当前方法后仍进入方法库 |
| F-05 中 | 维护页读取软件设置中的备份目录；后端将相对路径锚定到数据库所在用户数据目录，持久化绝对路径 | 浏览器显示设置值；测试切换工作目录后备份仍位于用户数据目录，校验与恢复演练通过，启动目录没有创建 backups |

F-03 的事实边界：本轮修改前，用真实 AnalysisPage 组件在 Chromium 中已能够发出提交并显示后端拒绝信息，未复现先前打包程序的“完全无响应”。本次补强可见反馈并验证真实分析闭环，但不能据此认定旧 EXE 的具体根因已查明；仍需新版桌面包复验。前一报告以无分析线的方法采集的输入本就不能成功分析，应得到明确拒绝，而非绕过规则。

早期“134 passed”与原测试报告最终失败的差异也不应推定为导入顺序问题：原报告在系统 Python/FastAPI 0.115.0 下确认普通导入失败。本次修复后，同一普通 Python 环境的导入、退出契约和全量测试均通过，不使用 `-O` 绕过断言。

## 实际变更

- `app/backend/app/main.py`：204 退出响应。
- `app/backend/app/modules/maintenance.py`：备份绝对路径解析。
- `app/frontend/src/App.tsx`：方法入口和谱线数值校验。
- `app/frontend/src/AnalysisPage.tsx`：分析提交及错误反馈。
- `app/frontend/src/MaintenancePage.tsx`：读取备份目录设置。
- `app/tests/test_auth.py`、`test_s20_maintenance.py`：退出/备份行为回归。
- `app/tests/test_s16_analysis.py`：为浏览器夹具提供完整条件字段的可选参数，既有算法测试默认不变。
- `app/tests/seed_bf02_browser.py`、`bf02_browser_regression.mjs`：新数据库夹具与真实浏览器回归。

## 命令与结果

工作目录为 `app/`：

- `python -c "import backend.app.main"`：退出码 0。
- `python -m pytest -q -p no:cacheprovider --basetemp=.local/tmp/bf02-final`：退出码 0，136 passed，34.93 秒。
- 夹具完善后定向复验：`python -m pytest -q -p no:cacheprovider tests/test_s16_analysis.py tests/test_s20_maintenance.py tests/test_auth.py --basetemp=.local/tmp/bf02-targeted`：退出码 0，16 passed。
- `npm.cmd --prefix frontend test`：退出码 0，4 passed。
- `npm.cmd run frontend:build`：退出码 0，TypeScript/Vite 生产构建通过；既存大 chunk 提示仍在。
- `node tests/bf02_browser_regression.mjs`：退出码 0。使用 Edge 无头 Chromium、1600×1000、真实 Vite 与 FastAPI，以及一次受控的错误响应注入；最终新数据库运行中无 pageerror。
- `cargo test --manifest-path src-tauri/Cargo.toml`：退出码 1，缺少 `link.exe`，未完成 Rust 编译。vswhere 确认本机 Build Tools 为 `isComplete=false`，VC 编译工具目录缺失。

浏览器复验准备：先用 `python tests/seed_bf02_browser.py <新的隔离数据目录>` 创建测试数据库，再将 `SPECTRUM_DATA_DIR` 设为该目录启动端口 8787 的 uvicorn，启动端口 5173 的 Vite。夹具拒绝已有数据库。测试使用夹具账号；Playwright 可通过 `PLAYWRIGHT_MODULE` 指定已安装模块入口。最终隔离目录为 `app/.local/tmp/bf02-ui-verified/`。

## 首轮未完成事项（重构建结果见后续记录）

- 本轮没有更新根目录 EXE、sidecar 或 NSIS 安装包。需补齐 Visual C++ Build Tools 和 Windows SDK 后，重新构建配套 sidecar/Tauri/NSIS，并在新版桌面程序复验 F-01～F-05，特别是 F-03。
- F-06 低优先级状态展示问题不在本次用户指定范围，仍保留。
- 真实转角/汞灯硬件、签名证书及干净 Windows 安装升级门禁继续保留。
- 未修改历史数据；测试使用新建隔离数据库。项目现有未跟踪文件未清理或重置。

## 用户授权重构建：工具恢复与桌面复验

- 用户随后授权重新构建及补齐构建工具；计划 SHA-256 未变。
- Microsoft 官方签名的安装程序确认既有 Build Tools 注册记录存在但文件缺失；执行既有实例的 `repair`，退出码 0，`vswhere isComplete=1`。恢复 MSVC 14.44.35207 的 x64 `link.exe` 和 Windows SDK 10.0.26100.0；未重启系统。
- 使用 `app/.local/build-venv/` 的 Python 3.11.9、FastAPI 0.141.1、PyInstaller 6.22.2 重建 sidecar。`tools/build_sidecar.py` 与 `tools/verify_sidecar.py` 均通过，健康检查、模拟采集、旧格式读取能力及退出清理通过。
- 同一构建虚拟环境全量 pytest：136 passed，42.76 秒；Rust `cargo test --manifest-path src-tauri/Cargo.toml`：3 passed；TypeScript/Vite 生产构建通过。
- 新 release EXE 配合新 sidecar，在独立夹具数据库中通过实际 WebView2/CDP 回归：空选择、非法超时、后端错误提示、真实分析完成、非法波长零写入与合法保存、两种方法入口、备份目录读取均通过，无 pageerror。正常关闭窗口后桌面和 sidecar 进程均为 0，测试调试端口已关闭。`bf02_browser_regression.mjs` 新增 `QA_CDP` 模式以重复此验证；调试设置只用于本轮进程，不写入产品配置。
- GitHub Release 直连多次超时，改从同一官方仓库 API 下载 NSIS 3.11 和 nsis-tauri-utils 0.5.3。SHA-256 分别为 `C7D27F780DDB6CFFB4730138CD1591E841F4B7EDB155856901CDF5F214394FA1`、`5BA143B5DB4A87D32D6E7802E033330AAE56CBCEABE0D1E3BA41948385AD4709`，与官方 release digest 一致，放入 Tauri 工具缓存；没有关闭哈希校验或更改系统代理。
- 仍保留既有前端 chunk 大小提示及 pytest 配置/依赖弃用提示。重复 bundle 对已写入 NSIS 类型的二进制提示找不到原始占位变量，本项目未配置 updater；不据此声称已验收自动升级。
- F-03 新包的分析闭环与失败反馈已实际通过；这不追溯认定旧 EXE 的精确故障原因。F-06 以及真实硬件、签名、干净 Windows 安装升级门禁不变。

### 最终产物与交付

`npm.cmd exec -- tauri bundle --ci` 最终退出码 0，保留 WebView2 offlineInstaller。三个文件已同步根目录，逐一 SHA-256 与 release 构建目录相同。随后从根目录 EXE 使用第二个新隔离数据库再次运行完整 BF02 桌面回归，全部通过。`tools/build_release_manifest.py` 退出码 0，更新 `docs/releases/0.1.0/internal-test-manifest.json`。

| 根目录产物 | 字节 | SHA-256 |
|---|---:|---|
| GeoSpectrum.exe | 11753472 | `B0438D2D41B132A4B8239E8642A02E327B0A8873F798F04B3424485DA8DE35B4` |
| geospectrum-backend.exe | 24840959 | `978590C3E95381CD8C789FE86B9DF58A241EA22AB664B5D9F75A4416B2628592` |
| GeoSpectrum_0.1.0_x64-setup.exe | 289465761 | `FF54A41E0FF081B4FB98FE3B94A907F87A377239888D9D9CD1CC7384751ACD9A` |

旧三个文件备份于 `app/.local/rebuild-20260905/previous/`，可回退；未删除用户数据或历史资料。工具下载与测试数据均在系统临时目录、标准工具缓存或 `app/.local/`；检查项目根目录及 app 未新增散落产物。此次未执行干净 Windows 安装/升级验收，不将成功打包等同于正式发布批准。

### 重构建后再次检查使用风险（2026-09-05）

用户再次要求测试当前系统。根目录 EXE/sidecar 哈希仍与上表一致。本轮重新执行构建虚拟环境全量 pytest（136 passed，36.78 秒）、前端数值测试（4 passed）、Rust 测试（3 passed）及 `tools/verify_sidecar.py`（健康、模拟采集、旧读取器诊断和零残留通过），均退出码 0。pytest 仍有 cache_dir 配置及 TestClient 依赖弃用两项警告。

本轮没有修改产品源码或重新逐一手工点击全部桌面功能；桌面 BF02 证据引用上节同一哈希程序的实际回归。源码再次确认 F-06：`App.tsx` 服务通道固定输出“待连接”和“构建中”。当前结论是已执行的自动化及前述桌面核心流程通过，不是“没有任何使用问题”。真实设备、实体打印机、干净 Windows 安装升级以及完整现场使用仍不能据此宣布通过。
