# 2026-09-05 项目清理记录

用户授权整理文件并确保不影响运行。本次仅整理产物、周报、可再生缓存及文档路径，不修改产品源码或推进新步骤。

执行前 PLAN.md SHA-256：`91AC0678F4A18A06C23AC08E396D2B84C135EE185234829D56F7333236011BD7`。

## 文件归位

| 原位置（相对项目根目录） | 新位置 |
|---|---|
| `GeoSpectrum.exe` | `app/.local/releases/0.1.0/GeoSpectrum.exe` |
| `geospectrum-backend.exe` | `app/.local/releases/0.1.0/geospectrum-backend.exe` |
| `GeoSpectrum_0.1.0_x64-setup.exe` | `app/.local/releases/0.1.0/GeoSpectrum_0.1.0_x64-setup.exe` |
| `weeklyreport.md` | `docs/参考资料/工作汇报/weeklyreport.md` |

以上四个文件移动前后 SHA-256 一致。主程序和后端保持同目录；启动入口改为新位置的 `GeoSpectrum.exe`，正常运行仍使用 `%LOCALAPPDATA%\cn.geospectrum.desktop`。已有安装不受本次移动影响；指向原根目录 EXE 的外部自建快捷方式需要改指新路径。

同步更新 `.obsidian/workspace.json` 中周报路径，保持其他工作区设置；修复 `app/README.md`、`docs/README.md`、`docs/项目阅读指南.md` 和根目录 `PLAN.md` 中已失效的相对链接。PLAN 中“不是 Git 仓库”的旧记录改为当前事实：已有 `.git/`，新版源码和多数文档仍未跟踪。

## 可恢复清理

自动审批拦截批量永久删除操作，未给出具体原因；该操作没有执行。改为将以下 14 个目录、942 个文件（456.01 MiB）移至系统临时暂存区，未永久删除，也不计作磁盘空间释放：

- `app/.local/tmp/` 下的 `bf02-final`、`bf02-pytest`、`bf02-targeted`、`current-system-check-20260905`、`rebuild-20260905`：均已确认只有自动化测试生成的 `test_*` 子目录。
- `app/.local/pytest-cache/` 和 `app/backend/`、`app/tests/`、`app/tools/` 下 8 个 `__pycache__/`。

暂存区：`C:\Users\Leo\AppData\Local\Temp\geospectrum-cleanup-9951a9a3e3bd42ce8d89cebc6baf947d`。其中保留原项目相对路径；系统临时目录不是长期备份。需要恢复时按映射移回，若原路径已经重新生成则先核对，勿直接覆盖。

逐项路径、文件数和字节数记录在 `app/.local/releases/0.1.0/cleanup-20260905.json`；归位文件的大小与哈希记录在同目录 `relocation-20260905.json`。这两份本机清单受 `.local/` 忽略规则保护，不进入发布归档。

## 保留范围

- 历史源码、现场数据、SpecFile 分类副本、验收截图和正式报告。
- `.obsidian/` 作为已有笔记工具配置保留；项目治理入口 `从头搭建学习计划.md` 保留在根目录。
- `app/.local/runtime/`、人工/浏览器复验数据库及故障证据、`full-test-20260905-003551/`。
- `app/.local/rebuild-20260905/previous/` 的旧程序回退备份，以及独立便携演示版及其 `demo-data/`。
- Python 虚拟环境、Node 依赖、pnpm 存储、Rust target、前端 dist、sidecar 二进制和构建工具缓存，保持开发及离线构建条件。

## 验证结果

- 四个归位文件逐一校验哈希一致；三个程序产物继续匹配 BF02 重构建记录。
- 在新位置启动桌面程序，设置全新的系统临时 `SPECTRUM_DATA_DIR`；10 秒后窗口 `GeoSpectrum` 有响应，配套后端已启动，隔离 SQLite 已创建。正常关闭窗口后桌面退出、配套后端残留数为 0。
- 用现有 `tools/verify_sidecar.py` 将本次验证目标指定为新位置后端：退出码 0，健康状态 `ok`、schema 20、模拟器连接/采集/停止及 ACQ 哈希验证通过，旧 PowerShell 读取器可见，验证结束无后端残留。该脚本通过终止测试进程树收尾，不以进程退出码 1 宣称自然退出。
- `python -B -m pytest -q -p no:cacheprovider tests/test_architecture.py tests/test_api.py tests/test_config.py tests/test_runtime_resources.py --basetemp <system-temp>`：退出码 0，18 passed；保留既有 cache_dir 配置与 TestClient 依赖弃用两项提示。测试禁用字节码和缓存写入，避免重新散落产物。
- 核对文档入口链接、Obsidian JSON、产物/依赖 Git 忽略规则，并复查项目根目录和 app 根目录。

本次未重建安装包、未执行全量业务或真实硬件复验；验证范围为整理相关的文件完整性、启动、运行资源及 API/配置契约。既有业务缺陷和发布外部门禁不变。

## 用户指定启动位置（同日后续调整）

用户明确要求“主程序放到主文件夹”。已将 `GeoSpectrum.exe` 和必需的 `geospectrum-backend.exe` 一起移回项目主目录，逐一核对 SHA-256 与归位前一致；安装包继续保留在 `app/.local/releases/0.1.0/`。同步更新文档入口，并在 AGENTS.md 记录用户指定的目录规则例外，避免后续清理再次移走。此前归位表及本机 JSON 为历史操作记录，当前程序位置以本节为准。没有修改程序内容或用户数据，本次不重复执行业务测试。
