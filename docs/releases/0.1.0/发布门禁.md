# GeoSpectrum 0.1.0 发布门禁

发布通道：`internal-test`（未签名）
计划 SHA-256：`73C6896E3BC8A142531077FC6BEF726CDC0DE5FA60F26C2D718606F8BAD47E11`

## 构建结论

Windows x64 内部测试包已生成，但不得作为正式版本发布。`npm.cmd run release:internal` 已完成 sidecar 重建与 schema v20 健康检查、前端生产构建、Tauri release 编译、内嵌 WebView2 离线运行库的 NSIS 打包及 SHA-256 清单生成。安装包为 `GeoSpectrum_0.1.0_x64-setup.exe`，242591972 bytes，SHA-256 `D0D6DB7AAC128F3E7D888E7AC5528C9DCF051D9316DA2AE3BF673584ED0510F4`，Authenticode 状态 `NotSigned`。

## 步骤状态

| 范围 | 状态 | 结论 |
|---|---|---|
| S00、S02–S13、S16–S20 | passed | 已有分步报告 |
| S01 | deferred_external | 仍缺干净 Windows 10/11 GUI/安装复验 |
| S14 | deferred_external | 缺少真实转角协议、设备和安全断开证据 |
| S15 | deferred_external | 缺少真实汞灯协议、设备和安全关闭证据 |
| S21 | deferred_external | 软件、构建和本机安装闭环通过；正式发布仅剩外部门禁 |

## 已关闭的软件门禁

- 全量 pytest `117 passed`；前端生产构建、`cargo check --locked --offline` 和统一发布命令通过。
- sidecar 使用随机回环端口、一次性进程密钥和 schema v20 构建元数据；独立验证及桌面关闭后均无残留进程。
- 本机 Windows 11 Home x64 完成 0.0.9→0.1.0 安装升级、v19→v20 数据保留、冷启动、单实例、卸载和用户数据保留冒烟。
- 运行数据与安装资源分离；旧数据经验证复制到标识符目录，原目录保持不变。
- 正式模块清单为 18 个模块，不含测试扩展；NSIS 输入不含 `app/.local/`、测试、缓存、调试文件或非运行必需中间物。

## 仍需外部完成

- 在干净 Windows 10 与 Windows 11 x64 虚拟机分别复验安装、冷启动、升级保留数据、卸载和无残留进程。本机冒烟不能替代干净虚拟机证据。
- 提供代码签名证书并对正式安装包签名；当前包只能作为明确标记的内部测试包。
- 补齐 S14/S15 真实转角和汞灯协议、设备及硬件在环安全收尾证据。

可审计产物哈希与外部门禁状态见 [internal-test-manifest.json](internal-test-manifest.json)。
