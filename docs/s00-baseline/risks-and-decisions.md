# S00 风险、冲突与锁定决策

| ID | 状态 | 风险/冲突 | 锁定处理 | 阻塞范围 |
|---|---|---|---|---|
| R-S00-001 | resolved | 根目录 `PLAN.md` 曾在旧编号 S14 的第一条“变化：定义 `Device`”中途截断，原文件仅 214 行。 | 2026-08-05 已补齐计划并进一步收敛为完整 S00–S21及第 7–11 章；通过编号、追踪和 Markdown 完整性检查后，以当前 `PLAN.md` 为唯一实施依据。 | 已解除；不改变 S00 仍需明确确认的门禁 |
| R-S00-002 | release_blocker | 仓库缺少 README 所称的 `1单片机控制指令.doc`，尤其没有转角和汞灯切换协议。 | 只允许模拟协议；通过设备正式文档或通信抓取补齐前，不实现猜测命令。 | S14–S15、正式发布 |
| R-S00-003 | release_blocker | 当前无真实 CCD/电弧/转角/汞灯硬件在环结果。 | ACQ 回放只用于开发；最终验收必须在真实设备完成并保存命令、帧、异常和收尾证据。 | 采集/设备功能正式验收 |
| R-S00-004 | resolved_for_format | `3数据存储格式.doc` 称 `.cdt CcdAvgs` 每点 2 字节，与源码冲突。 | `uCcdBand`/`uDbPack` 使用 `Single`，且真实 BLOB 长度按每点 4 字节；锁定 `float32_le`。 | S08 |
| R-S00-005 | format_variant | 历史 `.cdt/.cmt` 的 `SampName` 为 8 字符，`SpecFile/03_方法与数据库/分时样品.mdb` 空模板为 10 字符，源码 `TSampName=string[10]`。 | 按源文件 schema 读取并同时保留 `LongName`，不得把单一字段宽度推广到所有版本。 | S07–S09 |
| R-S00-006 | open | 高斯“峰面积”要求与旧源码行为不同：旧 `TGaussCur` 只返回峰高。 | `legacy_2_0_2` 保存/使用峰高；`modern_v1` 另存中心、峰高、Sigma、面积并使用面积定量，报告显示档案。 | S04、S16、S19 |
| R-S00-007 | open | 独立基线谱线在旧版不存在；旧版背景只是分析线参数。 | 新版新增 `baseline` 类型，不声称能无损回写旧库；旧方法迁移时按旧背景语义保留。 | S04、S06、S14 |
| R-S00-008 | open | S00 算法黄金值从源码逐式派生，尚无相同输入的旧 EXE 输出作为独立判据。 | 首次移植可使用；S16–S17 正式关闭前补旧 EXE 输出或独立实现交叉验证。 | S16–S17 的最终关闭 |
| R-S00-009 | accepted_dependency | 旧 Access 依赖 32 位 Jet 4.0；64 位进程不能直接使用。 | S00 用 32 位 PowerShell 对临时副本只读探测；正式产品按计划使用独立 .NET 8 win-x86 只读工具。 | S06–S09 |
| R-S00-010 | release_blocker | 没有 Windows 代码签名证书和正式自动更新端点。 | 首版只生成内部测试离线包；自动更新关闭。证书到位后才允许正式发布。 | S21、正式发布 |
| R-S00-011 | open | 旧版部分采集、慢进、全时导出和调试流程只有源码依据，缺少完整成功运行截图。 | 以源码约束实现，后续用模拟器和真实设备分别补自动证据；错误截图不能当成功证据。 | S10–S18 |
| R-S00-012 | environment | 当前根目录不是 Git 仓库，无法用提交记录证明基线变更边界。 | S00 使用完整 SHA-256/mtime 双遍校验；是否初始化版本库由用户另行决定，不在本步骤擅自执行。 | 变更追踪，不阻塞 S00 |
| R-S00-013 | clarified | `PLAN.md` 曾称“960 行样例”，但 `测试15-5-960.sam` 实际 800 行；720×1 + 80×3 展开为 960 个谱带。 | 计划已更正；验收按 800 个源记录、960 个展开谱带同时断言，不伪造 160 行。 | S07 |
| R-S00-014 | format_variant | 方法库 `Stds` BLOB 同时存在旧 16 点（224 字节）和新 50 点（700 字节）布局。 | S06 依据 BLOB 长度解析并填充缺省项，未知长度整体拒绝并报告。 | S06 |
| R-S00-015 | evidence_gap | `Spec Source/Bin` 中存在 `.qct/.qct_`，其 Jet 表结构与 `.edt/.wdt` 同构，但旧源码 `TSdFileType/FileExts` 及已确认流程均未列出该扩展。 | 保留原件、来源和哈希，暂按待核验遗留资料处理，不宣称为独立兼容格式；取得旧版运行证据后先更新 S00 和 S08。 | S00、S08 |

## 不阻塞 S01 的结论

S01 的启动、关于页、最小 SQLite 元数据、React 壳和 Tauri sidecar 不依赖设备命令或算法行为。S00 通过并获明确确认后可以进入 S01；不得越过确认直接开始。
